# 分支代码缺陷审计报告

> 对象：`GipfelBusinessCompetitionManagerWeb`（Django 5 + DRF + daphne + Socket.IO 后端 / Vue 3 + TS + Element Plus 前端）
> 方式：按**分支归属**把全仓 446 个 `.py/.vue/.ts`（约 7.3 万行）切成 **21 个互不重叠的审计单元**，每个单元由独立审计员逐行读码取证；关键结论由主审计员**独立复核并实跑只读探针/仓库自带测试**。
> 详细逐条报告在 `code_audit/` 目录（21 份，含原始代码摘录）；本文件是汇总与复核结论。

---

## 0. 摘要

### 0.1 分支拓扑与归因（去重的前提）

| 分支 | 相对关系 | 本分支**独有**的代码 | 审计单元 |
| --- | --- | --- | --- |
| `master` | 基线 | 全部既有业务代码（后端 27 app + 前端） | U01–U14 |
| `origin/prep-export-import` | master + 602eab1 | 比赛准备（清单/体检/分组导出导入）后端 + 前端弹窗 + 5 个被改文件 | B02 |
| `bugfix` | 含 prep-export-import + 3dfc731 | 仅 `scripts/dev.py`、`start-dev.bat`、`stop-dev.bat` | B01 |
| `feature_wuhaoye` | **工作树与 `bugfix` 逐字节相同**（`git diff bugfix feature_wuhaoye` 为空） | 无新增代码 | 不重复审计 |
| `feature/contract-type-code` | 含上述 + 73e8e46 + 408de7e | `preparation/builder/**`、`contracts/builder/**`、两个 management 命令、examples、3 份测试、2 份文档 | B03、B04、B05 |
| `test` | 含上述 + 6 个提交 | `examples/excel/**`、`examples/competitions/auto_chain_*`、`examples/contracts/{auto_chain,mini}_*`、2 份文档 | B06 |
| `feature/contract-watcher` | 含上述 + 8b9dea0 等 | `contract_watcher/**`（独立监听程序） | B08 |

去重规则：**同一文件只由归属它的那一个单元审计**。例如 `backend/backend/settings.py` 中与 `preparation` 注册相关的行归 B02，其余归 U01；`contracts/` 顶层归 U05、`builder/` 归 B04/B05；`preparation/` 非 builder 部分归 B02、builder 部分归 B03。

已核实：`408de7e`、`73e8e46` 均为 100% 新增文件（未修改任何既有文件），因此分支间不存在"同一文件多版本"的重复面；`feature_wuhaoye` 与 `bugfix` 树完全相同，合并提交未引入独有改动。

### 0.2 缺陷统计

21 个单元合计 **366 条**缺陷（每条都有 `路径:行号` + 代码摘录），单元自评分级如下：

| 单元 | 内容 | 分支归属 | 合计 | P0 | P1 | P2 | P3 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| U01 | `backend/backend/*` + `apps/common/**` | master | 13 | 0 | 2 | 5 | 6 |
| U02 | auth/users/audit/realtime/files/widget_packages/announcements/messages/logviewer | master | 19 | 0 | 2 | 6 | 11 |
| U03 | competitions/companies/company_fields/industry_types/regions/maps/tech_tree | master | 20 | 0 | 5 | 11 | 4 |
| U04 | materials/parts/products/fuels/warehouses/vehicles/infrastructures/production_lines/consumer_demands | master | 11 | 0 | 2 | 5 | 4 |
| U05 | contracts 顶层（models/serializers/views/engine 2118 行） | master | 17 | 1 | 3 | 9 | 4 |
| U06 | stock 引擎/视图 + tests 股票脚本 | master | 16 | 1 | 5 | 6 | 4 |
| U07 | 前端 api/stores/router/realtime/utils/permissions/config/types | master | 14 | 0 | 2 | 6 | 6 |
| U08 | data-management 视图 A（合同/地图/科技树/合同类型/燃料） | master | 20 | 0 | 6 | 6 | 8 |
| U09 | data-management 视图 B（产业类型/原料/零件/产品/载具/仓库/基建/产线） | master | 16 | 1 | 3 | 7 | 5 |
| U10 | stocks/dashboard/regions 视图 | master | 17 | 0 | 2 | 8 | 7 |
| U11 | 账号/公司/比赛/消息/登录/审计/设置 视图 | master | 19 | 0 | 1 | 9 | 9 |
| U12 | 合同图编辑器/简单编辑器/公式输入/产业字段图编辑器 | master | 15 | 1 | 2 | 6 | 6 |
| U13 | layout/common/dashboard 组件 + 4 个全局弹窗 | master | 14 | 0 | 2 | 5 | 7 |
| U14 | scripts（除 dev.py）/deploy/tests/示例控件包 | master | 26 | 3 | 7 | 9 | 7 |
| B01 | `scripts/dev.py` + `start-dev.bat` + `stop-dev.bat` | bugfix / feature_wuhaoye | 11 | 0 | 2 | 5 | 4 |
| B02 | preparation 导出/导入/体检 + 前端弹窗 + 被改文件 | prep-export-import | 20 | 0 | 4 | 10 | 6 |
| B03 | `preparation/builder/**` + `build_competition` | feature/contract-type-code | 18 | 0 | 1 | 4 | 13 |
| B04 | `contracts/builder/**`（validate/CLI/tests 除外） | feature/contract-type-code | 17 | 1 | 3 | 8 | 5 |
| B05 | `builder/validate.py` + `build_contract_types` + tests + examples | feature/contract-type-code | 19 | 0 | 3 | 9 | 7 |
| B06 | `examples/excel/**` + auto_chain 示例 | test | 17 | 4 | 6 | 4 | 3 |
| B08 | `contract_watcher/**` | feature/contract-watcher | 27 | 2 | 7 | 11 | 7 |
| **合计** | | | **366** | **14** | **70** | **149** | **133** |

**本报告的重排口径**：第 3 节的 **9 条**是我按"数据损坏/静默丢失、跨租户越权写、模块持久不可用，且有一次请求或一次点击级的现实触发路径"重新收敛出的最高风险集（合并了跨单元同源项，例如 U01-C03/U03-O04/U04-P02 同根因合并为 D-08）；第 4 节按域重排 P1（含单元标 P1 与部分被我上调的 P0/P2）。单元自评与我的重排可能相差一级，逐条原始分级仍保留在各单元报告里。

### 0.3 最该先修的 12 条

1. **合同参与方公司未做归属校验** → 比赛管理员可对**任意公司（含其他比赛）**的产业字段加/减/置值（`contracts/engine.py:2243`、`serializers.py:211`）。
2. **金额精度在 SQLite 上从 15 位有效数字起静默丢失**（`DecimalField(max_digits=60)`；仓库自带 `tests/sqlite_decimal_roundtrip.py` 实测 FAIL）。
3. **股票撮合主循环可死循环**：量化把数量归 0 而索引不推进 → 事务与推进锁永不释放，比赛僵死（`stock/engine.py:1596-1643`）。
4. **原料"地点价"保存即被清空**：编辑弹窗只收 number 型价格，而新数据以字符串存储 → 一键把 `nodePrices` 覆盖成 `{}`（`MaterialsManager.vue:410`）。
5. **合同简单编辑器保存即清空全部效果**：写的是 `type` 而全库用 `kind`（`SimpleContractTypeEditor.vue:284`）。
6. **一个 crafted 请求即可让整场比赛的合同列表/详情永久 500**：`parties[].companyId` 为字符串时 `int()` 抛错（`contracts/views.py:672`）。
7. **地图/科技树节点写入 `"inf"`** → 出站渲染 `allow_nan=False` 抛错，该模块读接口持续 500（`maps/serializers.py:95` + `common/renderers.py:34`）。
8. **禁用账号 / 管理员重置密码都不吊销已签发 token**：JWT 与 WebSocket 继续可用最长 24h（`auth/authentication.py:104`、`users/views.py:159`）。
9. **登录失败限流可被用户名尾随空格绕过**（中间件用未 strip 的 body 值查锁、视图用 strip 后的值记账）→ 可无限爆破（`common/middleware.py:255`）。
10. **`bindFieldId` 改派漏了"高级管理"校验**：持 `stock:edit` 的账号可把账户现金指向任意字段 → 任意充值（`stock/views.py:684`）。
11. **合同类型建库器（分支独有）第二次 `build()` 必丢实体槽位** → CLI 导入的 `inputSchema` 缺 `__ref_*`，实体引用全部静默变 0（`builder/refs.py:422`）。
12. **部署脚本 `migrate-server.sh` 把含全部密钥的 `.env` 推到远端 `/tmp`（无 `-p`、umask 022、不清理）**，且变量未加引号可在目标机 root 注入命令。

分支独有代码中还应优先处理：`Z-01`/`Z-02`/`Z-03`（Excel 建包静默丢行、负距离/负价入包、文本被数字文本化）、`R-01`~`R-04`（归档导入"报成功实际回滚""整类跳过""合同合并改名"）、`CW-01`/`CW-02`（监听程序重复记账 / 永久静默漏账）、B01 的 `D-01`/`D-02`（dev 脚本无差别 taskkill 误杀无关服务，与第 3 节的 D-01/D-02 编号无关）。

---

## 1. 审计方法与证据等级

- **单元制、只读、可复核**：每个单元只读指定文件，结论必须带 `相对路径:行号` + 3–8 行原始代码摘录 + 触发条件 + 后果 + 修复建议；不确定的标 `[待确认]`，并单列"存疑"。
- **主动构造边界**：每单元被要求逐项验证 —— 数值（0/负数/极大值/浮点与 Decimal 混算/除零/取整/溢出/量化）、字符串（None/空串/纯空白/超长/中文与 emoji/大小写与全角/重名/注入）、网络与并发（超时、重试、断线重连、重复提交、竞态、事务与行锁、房间隔离）、多账号与权限（越权 IDOR、跨比赛/跨公司、按钮级权限、账号切换残留）。
- **强制去重**：每个单元的提示词里写明"不要审计其它单元的文件"，并要求核对 `git diff master -- <路径>` 以确认分支归属。
- **主审计员独立复核**：本报告中标【已复核】的条目，均由我亲自读码或运行探针/仓库测试确认；标【子代理取证】的条目保留单元报告的 `路径:行号` 与摘录，未逐条实跑。

证据等级标记：**【已复核】**（我读码+实跑）/ **【已读码】**（我读过相关代码确认机制，未实跑复现） / **【子代理取证】**（单元报告静态取证，含行号与摘录）。

---

## 2. 独立复现证据（我实际运行的探针与仓库测试）

探针脚本保留在 `code_audit/`（`_probe_engine.py`、`_probe_engine2.py`、`_probe_float.py`、`_probe_perms.py`），均为只读、不写业务库。

| # | 命令 | 实测结果 | 支撑缺陷 |
| --- | --- | --- | --- |
| E-1 | `backend\.venv\Scripts\python.exe tests\sqlite_decimal_roundtrip.py` | **FAIL（exit 2）**：写入 `12345678901234567890123.4567` → 读回 `12345678901234600000000.0000`（低 9 位整数静默丢失） | D-01、S-08 |
| E-2 | `_probe_float.py` | `MapNodeSerializer` 对 `x="inf"/"-inf"/"nan"/"1e400"` 全部 `valid=True`；`JSONRenderer.render({x: inf})` → `ValueError: Out of range float values are not JSON compliant` | D-08、P-02、C-03 |
| E-3 | `_probe_engine.py` | `safe_evaluate('log(0)')`→ValueError；`('sqrt(0-1)')`→ValueError；`('pow(10,1000)')`→OverflowError；`('asin(2)')`→ValueError；`('1/0')`→0；`('unknownvar+1')`→1；全角括号报错 | D-04、K-04、M-3 |
| E-4 | `_probe_engine2.py` | `apply_op('LOG',[10,1])`→**ZeroDivisionError**；`('EXP',[1000])`→OverflowError；`('EXP',['1e400'])`→`inf`；`('MIN',[inf,5])`→0；`dumps_engine_json(Decimal('Infinity'))`→OverflowError | D-04、M-3 |
| E-5 | `_probe_perms.py` | `COMPETITION_ADMIN` 默认持有 `contract:manage/audit/execute`+`stock:edit`；`company:manage`/`message:manage`/`industryType:manage`/`contractType:manage`/`data:region:edit`（扩展集）**永远无法授予**；未登记 action 键 fail-closed | §6.5 可达性校准、I-13 |
| E-6 | `backend\.venv\Scripts\python.exe tests\big_number_smoke.py` | `PASS=20 FAIL=0`，但第 6 组**恒 SKIP**（引用 `timer._to_num`，实际函数名是 `_parse_number_raw`） | M-2 掩蔽测试 |
| E-7 | 内联 bcrypt 探针（bcrypt 4.1.3） | `checkpw(b'a'*72+b'Y', hashpw(b'a'*72+b'X'))` → **True**（72 字节后截断）；`hashpw` 接受 NUL 字节（不抛错） | M-1 密码截断 |
| E-8 | 内联 Decimal 探针 | `format(Decimal('1e1000000'),'f')` → 1,000,001 字符、0.001s；指数线性放大 → `1e999999999` 即 ~1GB 分配 | O-05 单请求 OOM |
| E-9 | `manage.py check` / `makemigrations --check --dry-run` | 系统检查无问题、**无缺失迁移**（负向结论，排除该风险类） | — |
| E-10 | `manage.py check --deploy` | 4 条 W 级：无 HSTS、无 `SECURE_SSL_REDIRECT`、Session/CSRF cookie 非 Secure | §6.3 传输层加固 |

---

## 3. P0 缺陷（9 条）

### D-01 金额精度：SQLite 上 `DecimalField(max_digits=60)` 实际只保 15 位有效数字 【已复核】
- 位置：`backend/apps/{fuels,warehouses,stock,infrastructures,production_lines,vehicles}/models.py`（全部 `max_digits=60, decimal_places=4`）、`tests/sqlite_decimal_roundtrip.py`
- 证据：实跑仓库自带测试 FAIL（见 E-1）。根因：SQLite 的 `decimal` 列是 NUMERIC 亲和性 → 插入的十进制文本被强转 REAL 存储，读回时 Django 以 `Context(prec=15)` 还原。
- 后果：系统声称支持"千万京（10^23）级金额"，但在默认 SQLite 库上 **10^13 量级即开始出现分位误差**，10^23 完全不可用；所有金额守恒、对账、比拼都建立在被静默截断的数字上。
- 修复：为金额字段提供 TEXT 亲和存储（自定义字段 + 迁移），或明确"大额仅 PostgreSQL"并在写入侧对超 15 位有效数字直接报错（宁可报错不可静默丢值）。

### D-02 合同参与方公司无归属校验 → 跨公司/跨比赛越权改写产业字段 【已读码】
- 位置：`backend/apps/contracts/serializers.py:211`（`validate_parties` 只要求"至少一个非主办方"）、`backend/apps/contracts/engine.py:2243`（`_resolve_party_company` 直接用 `parties[].companyId`）、`engine.py:2266/2343`（`Company.objects.filter(pk=...)` 无 `competition_id` 过滤）、`views.py:803`（`_assert_execute_scope` 只校验**最后一个**参与方公司在本账号 `companyScopes` 内）
- 触发：持 `contract:manage`+`contract:audit/execute`（`COMPETITION_ADMIN` 默认具备）的账号构造 `parties=[{companyId: 受害者公司}, {companyId: 自己公司}]` → 执行 → 引擎对该合同所有参与方公司的产业字段做 ADD/SUB/SET。
- 后果：跨租户数据篡改；若受害公司在别的比赛，等于穿透比赛隔离。前端下拉限制了公司范围，但**后端零校验**。
- 修复：创建/更新合同时校验每个 `companyId` 存在且 `competition_id` 与合同一致；对照 `engine` 写入前对目标公司再做一次比赛域校验。

### D-03 股票撮合主循环死循环 → 事务与推进锁永不释放 【已读码】
- 位置：`backend/apps/stock/engine.py:1596-1643`
- 机制：现金夹紧后 `qty = buy_cash / pair_price`，守卫是 `qty <= EPS(1e-9)`；但随后 `:1612` 把 `qty` 按 6 位小数 `quantize`，落在 `(1e-9, 5e-7)` 的 `qty` 会被量化为 **0**，于是 `cash/holding/filled/buy_rem/sell_rem` 全部不变、`bi/si` 不推进，`while` 无出口（子代理已用 Decimal 逐步复现状态重复）。
- 后果：请求永不返回、`transaction.atomic()` 与进程内推进锁长期占用 → 该比赛股票系统僵死，只能重启进程。
- 修复：量化后再做一次推进判定（`qty_rounded = 0 → 视为剩余无效并推进索引`），并给主循环加迭代上限。

### D-04 公式/算子数学域错误未捕获 → 500；未定义变量静默 0 【已复核】
- 位置：`backend/apps/contracts/engine.py:1144-1148`（`EXP/LOG`）、`engine.py:715-743`（`sqrt/log/pow/asin/...`）、`engine.py:1791`（只捕获 `_SafeExpressionError`）、`engine.py:905`（未定义变量 `return 0`）
- 证据：E-3/E-4。`log(0)`、`sqrt(-1)`、`pow(10,1000)`、`asin(2)`、`LOG(10,1)`（底数为 1 → 除零）全部抛出**未被捕获**的 `ValueError/OverflowError/ZeroDivisionError` → 500（非统一错误信封）；`EXP('1e400')` 返回 `inf` 继续参与运算。
- 同时：公式里拼错的变量名静默取 0（`unknownvar+1 → 1`），配合 `op=SET` 会把字段直接写成 0，而合同照常置为"已执行"。
- 修复：在公式/算子入口统一捕获 `ArithmeticError` 转 `BusinessError`；未定义标识符改为报错（或在编辑器侧强校验）。

### D-05 原料地点价：编辑即清空 【已复核】
- 位置：`frontend/src/views/data-management/MaterialsManager.vue:410-417`（`openEdit` 只收 `typeof v === "number"`）对比同文件 `:245-248`（显示逻辑明确"兼容数字与数字字符串（大数安全：新数据以字符串形态存储）"）
- 后果：打开编辑 → 保存，`nodePrices` 被 PATCH 成 `"{}"`，**该原料所有地点价静默清零**，合同按 0 计价、均价失真。
- 修复：`openEdit` 与显示逻辑共用同一解析函数（保留字符串形态）。

#### D-05 补充说明（按你的要求：只解释，本次未改代码）

**1) 缺陷链路（同一份 `nodePrices` 在四处用了三种口径）**

| 位置 | 行为 | 口径 |
| --- | --- | --- |
| `MaterialsManager.vue:234-249` `getRowNodePrices()`（表格"价格（按地点）"列） | `typeof v === "number" \|\| (typeof v === "string" && /^[+-]?\d+(\.\d+)?$/)` | **兼容数字与数字字符串**（注释写明"大数安全：新数据以字符串形态存储"） |
| `MaterialsManager.vue:378-383` `onNodePriceChange()`（弹窗里改价） | `form.nodePrices[nodeId] = v`（`v` 是 `BigNumberInput` 回传的**字符串**） | **写入字符串** |
| `MaterialsManager.vue:402-424` `openEdit()`（打开编辑弹窗回填） | `if (typeof v === "number") np[Number(k)] = v;` | **只认 number，字符串被丢弃** |
| `MaterialsManager.vue:432-437` `handleSubmit()` | `nodePrices: JSON.stringify(form.nodePrices \|\| {})` → PATCH/POST | 把表单值整体覆盖写回 |

即：表格读得出、弹窗改得进，但**一打开编辑就丢**。`form.nodePrices` 的类型标注 `Record<number, number>`（`:269`）与实际存储（字符串）不符，正是这个错误标注让 `openEdit` 的类型过滤看起来"合理"。

**2) 触发条件（正常操作即可复现，无需异常输入）**
- 该原料的 `nodePrices` 是"新形态"（值为数字字符串，如 `{"12":"123.45"}`）——示例比赛包、Excel 建包、以及本页保存过一次之后都是这种形态；
- 打开该原料编辑弹窗（地点价区域因过滤而显示为空）→ 不做任何修改直接点"保存"；
- 结果：`nodePrices` 被整体覆盖为 `"{}"`，全部地点价静默清零，且不可撤销（无版本、无审计快照）。

**3) 后果（后端计价链）**：`backend/apps/contracts/engine.py:1267-1304 compute_material_list_price()`
- 规则：指定地点且有该地点价 → 用地点价；否则用**全部地点价的平均值**；无任何地点价 → 0；
- `nodePrices` 变成 `{}` 后均价与地点价都取不到 → `avg_prices.get(name, 0)` → **该原料按 0 计价**；
- 引擎不报错、合同照常执行 → 所有"原料清单总价格"口径的合同金额静默变 0（采购成本、碳费基数、按原料计价的结算等），属**静默错账**。

**4) 定级与修复建议（若后续要修）**
- 最小改动：`openEdit` 复用 `getRowNodePrices(row)` 的解析结果（保留 `string \| number` 原形态），并把 `:269` 的类型标注改为 `Record<number, string \| number>`，`onNodePriceChange` 里两处误导性 `as Record<number, number>` 一并更正。
- 加固建议：保存前比对"读入条数 vs 写出条数"，条数变少时阻止提交并提示（防同类"编辑即清空"再现）。
- 验证方法（改前/改后对照，无需起前端）：对同一份 `nodePrices` JSON（如 `{"12":"123.45","13":88}`）跑 `openEdit` 的回填逻辑——改前 `form.nodePrices === {}`，改后应为 `{"12":"123.45","13":88}`；再跑 `handleSubmit` 的 payload 构造——改前 `nodePrices === "{}"`，改后应保留两个地点价。本次按你的要求未改代码，故只有方法、无实测记录。

### D-06 合同简单编辑器保存即抹掉全部效果 【已复核】
- 位置：`frontend/src/components/contracts/simple/SimpleContractTypeEditor.vue:284`（`ct.effects.filter(e => e.type === 'FIELD')`）
- 机制：全库（`engine.py:1887` 的 `eff.get("kind")`、图编辑器、合同管理页）统一使用 `kind`；该过滤条件恒为假 → `form.effects = []` → 保存把 `effects` 写成 `[]`（连 IF/FOREACH 树一起丢）；新建时写入的 `{type:'FIELD'}` 又会让引擎抛"未知效果类型: None"。
- 修复：改用 `kind`，并在保存前比对"读入的效果数 vs 写出的效果数"做保护。

### D-07 一个请求让整场比赛的合同列表/详情永久 500 【已读码】
- 位置：`backend/apps/contracts/views.py:672` 与 `:687`（`int(p["companyId"])`，无类型校验）对比 `serializers.py:211`（创建时不校验 `companyId` 类型）
- 触发：`contract:manage` 账号 POST 一份 `parties=[{...,"companyId":"abc"}]` 的合同（写入成功），其后 `GET /api/contracts`、`GET /api/contracts/:id`（`:201/:214/:254` 都会调用 `_enrich_party_companies`）在 `int("abc")` 处抛 `ValueError` → 500；`contract_watcher` 的合同轮询同样失效。
- 修复：`companyId` 在序列化层强制为 int（拒绝 bool/str/float），补全函数用 `try/except` 或 `.get()` 容错。

### D-08 地图/科技树节点写入 `inf`/`nan` → 该模块读接口持续 500 【已复核】
- 位置：`backend/apps/maps/serializers.py:95-96`（`FloatField` 无范围校验）、`backend/apps/common/renderers.py:34`（float 分支无 `is_finite` 守卫）、`common/response.py:46`（`allow_nan=False`）
- 证据：E-2 —— `x="inf"` 通过校验；渲染时 `ValueError: Out of range float values are not JSON compliant` → 500 且非统一信封。写入一行即让 `/api/maps/full` 与所有地图/科技树读接口持续 500。
- 修复：序列化层拒绝非有限浮点（`min_value/max_value` + `math.isfinite`），渲染层对 `inf/nan` 兜底为 `null`。

### D-09 合同类型建库器：第二次 `build()` 必丢实体槽位（分支 `feature/contract-type-code` 独有） 【已读码】
- 位置：`backend/apps/contracts/builder/refs.py:404-426`（`pin()` 命中 `self._pinned` 缓存即早返回、不再 append；`drain_pinned_inputs()` 只清 `_pinned_inputs` 不清 `_pinned`）、`builder/builder.py:868-874`（唯一的 drain 消费点）、`management/commands/build_contract_types.py:101`（`static_check` 内先 build 一次）+ `:246`（导入前再 build 一次）
- 后果：`--import` 落库的 `inputSchema` 缺 `__ref_*`，而 effects 仍指向它 → 引擎 `entityRef` 取不到 id → **所有实体引用静默按 0 计算**；同一进程内多份合同共用快照时还会互相抽干/串槽位。
- 修复：`pin()` 改为每次 build 都重新登记，或 `drain_pinned_inputs()` 同时清 `_pinned`；并在 build 自检"inputSchema 必须覆盖所有 entityRef"。

---

## 4. P1 及需优先处理的高危项（按域）

> 本节按域重排，共 98 行条目（部分行合并了同源多条，例如 `I-09…I-18`、`X-04…X-10`）。每行给"位置 / 问题 / 证据等级"。

### 4.1 认证、账号与权限（master 基线）

| 编号 | 位置 | 问题 | 证据 |
| --- | --- | --- | --- |
| I-02 | `backend/apps/auth/authentication.py:104-114`；`backend/apps/realtime/gateway.py:_resolve_user` | 认证链路**从不校验 `is_active`**（唯一检查在 `auth/views.py:153` 登录时）；`PATCH /api/users/:id {"isActive":false}` 也不递增 `token_version` → 禁用账号的旧 JWT/socket 继续可用（JWT 最长 24h，socket 无限期） | 【已复核】 |
| I-03 | `backend/apps/users/views.py:159-165` | 管理员重置他人密码只写 `password_hash/must_change_password/updated_at`，**不递增 `token_version`**（改密 `auth/views.py:253`、登录 `:157` 都会递增）→ 被怀疑失窃的会话重置密码后依然有效 | 【已复核】 |
| C-01 / I-05 | `backend/apps/common/middleware.py:255-266` vs `backend/apps/auth/views.py:126` | 限流中间件用**未 strip** 的 body `username` 查锁，视图用 `strip()` 后的值记账 → `"admin "` 尾随空格即绕过锁定；且中间件只解析 JSON body，改 `Content-Type: form-urlencoded` 直接绕过；`_locks` 以攻击者可控的任意用户名为键且无上限 → 内存增长 | 【已复核】 |
| I-01 | `backend/apps/realtime/gateway.py:119-127` | "顶号"只 `emit auth:required`、**从不 `sio.disconnect`**，也不周期复核 `tv` → 被踢设备仍是 `user-{id}`/`comp-{id}` 房间成员，继续收到 `resource:changed` 与 `message:new`（含消息正文） | 【已复核】 |
| I-06 | `backend/apps/files/views.py:231-238` | 地图背景上传无 `f.size` 上限且整文件 `f.read()` 进内存 → 单请求可 OOM/写满磁盘 | 【子代理取证】 |
| I-07 | `backend/apps/widget_packages/views.py:86-95` | zip 只校验条目名不校验解压体积/条目数 → zip bomb；失败分支不清理已解压目录 | 【子代理取证】 |
| I-08 | `backend/logviewer/logviewer/views.py:97-106` | 日志查看器登录不在防直连网关内、无限流/锁定，且与 `/admin` 共用超管凭据 | 【子代理取证】 |
| I-13 | `backend/apps/common/permissions.py:388-401` | `grantExtras`（`company:manage`/`message:manage`/`industryType:manage`/`contractType:manage`/`data:region:edit`）恒被记违规 → **扩展集永远无法授予**，"超管按需放开"的设计与文档均不存在 | 【已复核，E-5】 |
| I-19 | `backend/apps/users/models.py:47-48,100-110` | 注释称 `permissions=null` 表示"按角色继承"，实际 `permissions_list` 返回 `[]` → 零权限（`ROLE_TEMPLATES` 只用于授予校验） | 【子代理取证】 |
| I-12 | `backend/apps/common/middleware.py:180-188` + `common/audit.py:57-75` | 审计/日志的操作者取自 `Authorization` 头里的 token 且**不复核 `tv`**；`/api/auth/login` 是 `AllowAny` → 持已作废 token 可冒名写入审计归属 | 【子代理取证】 |
| I-15 | `backend/apps/messages/views.py:196-202` | `selectable-users` 仅需 `message:view`（选手默认持有）→ 任意选手可枚举本比赛全部账号（username/displayName/role） | 【已读码】 |
| I-16 | `backend/apps/announcements/views.py:61-67,102`（`widget_packages/views.py:191` 同型） | `isActive` 无类型校验：非布尔值直接 500；字符串 `"false"` 经 `bool(...)` 反而存成"启用" → 停用静默失效 | 【子代理取证】 |
| I-09/I-10/I-11/I-14/I-17/I-18 | 见 `code_audit/U02-backend-identity.md` | 并发登录 `token_version` 竞态、seq 取号与环形写入分属两锁、socket 会话角色快照不刷新、PATCH 改权不推 `permissions:changed`、消息图片 `filename` 可控删他人文件、旧 token 反复 `connect` 可反复踢人 | 【子代理取证】 |

### 4.2 合同域（master 基线）

| 编号 | 位置 | 问题 | 证据 |
| --- | --- | --- | --- |
| K-01 | 见 **D-02**（P0） | 参与方公司无归属校验 | 【已读码】 |
| K-02 | `backend/apps/contracts/views.py:231-238`、`_assert_edit_party_scope`（`:844-859` 对 manage/execute 直接早退） | 创建时一次性代填全部会签编号即自动置 `PENDING_EXEC` → "各方各自签署"的业务控制可被创建者单人绕过（前端 `ContractManageView.vue:1443` 有"只填自己那方"的规则，后端没有对应实现） | 【已读码】 |
| K-03 | `backend/apps/contracts/views.py:319-327` | 执行接口接受任意 `inputs` 覆盖，**完全不按 `inputSchema` 校验**（required/min/max/enum/type 全无）→ 负数量/负单价可让付款方向反转 | 【已读码】 |
| K-04 | `backend/apps/contracts/engine.py:1774,441`、`:905` | 缺失/拼错 INPUT 键与未定义变量静默取 0，`op=SET` 会把字段清零，合同仍置 `EXECUTED`（与 D-04 同源） | 【已复核，E-3】 |
| K-12 | `backend/apps/contracts/views.py:668-690` | 脏 `companyId` 使整比赛合同列表 500（见 **D-07**） | 【已读码】 |
| K-05 | `backend/apps/contracts/engine.py:1670` | 同一次执行内 FIELD 读的是预加载缓存、写入不回写缓存 → 读到执行前旧值，金额算错无提示 | 【子代理取证】 |
| K-06 | `backend/apps/contracts/engine.py:1246,1897` | 聚合清单里拼错/不存在的原料零件名静默按 0 计碳排与价格（前置检查因此误通过）；`effects/conditions` 缺 `fieldKey/op/kind` 时 `KeyError` 未捕获 → 该类型所有合同 500 | 【子代理取证】 |
| K-07 | `backend/apps/contracts/engine.py:2317-2326` | 乐观锁二次冲突时**跳过写入却照写审计行并置 `EXECUTED`** → 审计与真实字段值永久不一致 | 【已读码】 |
| K-08 | `backend/apps/contracts/views.py:569` | 终止合同只需 `contract:manage`、**无执行方范围校验**，却会回滚他人字段并删除效果行 | 【子代理取证】 |
| K-09 | `backend/apps/contracts/views.py:832` | 详情/影响接口对 audit/manage/execute 账号不做公司范围过滤（列表隐藏、详情可读）→ 与列表口径不一致的跨公司读 | 【子代理取证】 |
| K-10 | 全仓无 `select_for_update`（`views.py:346` 等） | execute/terminate/delete 无行锁：终止与执行交错会导致状态与账目不一致（SQLite 下 `select_for_update` 也会静默失效，见 S-10） | 【子代理取证】 |
| K-11 | `backend/apps/contracts/engine.py:835` | `avg`/`^`/`pow` 走 float，2^53 以上静默丢精度，违背"任意精度"承诺 | 【子代理取证】 |

### 4.3 股票域（master 基线）

| 编号 | 位置 | 问题 | 证据 |
| --- | --- | --- | --- |
| S-01 | 见 **D-03**（P0） | 撮合死循环 | 【已读码】 |
| S-02 | `backend/apps/stock/serializers.py:97` + `:152,182` | `code` 声明为 `read_only=True`，`create()` 却取 `validated_data["code"]` → `KeyError` → **`POST /api/stocks` 恒 500，股票创建功能整体不可用** | 【已复核】 |
| S-05 | `backend/apps/stock/views.py:684-691` | `bindFieldId` 改派**漏了 `high` 校验**（相邻的 `cashBalance/companyId/userId` 都有，注释第 653-655 行还专门解释了该风险）→ 持 `stock:edit`（`COMPETITION_ADMIN` 默认具备）即可把账户现金指向任意产业字段 → 任意充值 | 【已复核】 |
| S-06 | `backend/apps/stock/views.py:852,958` | `_is_high_manager` 直接跳过账户可操作性校验且无比赛域校验 → 比赛 A 的 `stock:manage` 账号可对比赛 B 的账户下单/撤单 | 【子代理取证】 |
| S-03 | `backend/apps/stock/engine.py:1582-1596` | 撮合不检查同账户自成交：对倒净额恒 0 → 零成本刷 `volume` 并参与 `trade_price` 锚定 | 【子代理取证】 |
| S-04 | `backend/apps/stock/engine.py:1016` vs `:1755` | 做市商旧单在推进轮次时被改写成 `new_round`，而撤单条件是 `round__lt` → 永远失配 → 僵尸挂单逐轮累积并锚住成交价 | 【子代理取证】 |
| S-07 | `backend/apps/stock/views.py:884-898` | 挂单资金占用按 `stock_id` 过滤 → 同一账户在多只股票重复用满余额（也是 S-01 的现实入口） | 【子代理取证】 |
| S-08 | `backend/apps/stock/models.py:76,111,160-162,194-200` | SQLite 下 `DecimalField(60,4)` 只存 15 位有效数字（与 **D-01** 同源）→ "精确到分"不成立 | 【已复核，E-1】 |
| S-09 | `backend/apps/stock/engine.py:58-63` | `round2` 超精度静默返回 0 → 绑定字段 >70 位或 `1e100` 时结算把账户现金清零 | 【子代理取证】 |
| S-10 | `backend/apps/stock/views.py:874,951` | SQLite 上 `select_for_update` 静默失效（Django 有 features 守卫）→ 撤单与撮合并发互相覆盖 | 【子代理取证】 |
| S-11 | `backend/apps/stock/engine.py:1054-1076` | 做市商建仓凭空造股/销毁现金，且不受 `total_shares` 约束 | 【子代理取证】 |
| S-12 | `backend/apps/stock/engine.py:333-339` | `compute_init_price` 的 `quantize` 无精度保护，合法入参即 `InvalidOperation` → 500 | 【子代理取证】 |

### 4.4 基础数据与产业（master 基线）

| 编号 | 位置 | 问题 | 证据 |
| --- | --- | --- | --- |
| O-01 | `backend/apps/companies/serializers.py:78-91` | `save(update_fields=[...])` 用 **snake_case** 过滤 **camelCase** 的 `validated_data` → 只发 `regionId`/`industryTypeId` 的 PATCH **静默不落库**，但接口返回新值（`updated_at` 反而被刷新）；与 `base_crud.py:169-190` 的正确写法不一致 | 【已复核】 |
| O-02 | `backend/apps/company_fields/views.py:71-99` | `_write_field_value` 不校验 `industryField` 是否存在/属于该公司的产业类型 → 跨产业写值；不存在的 id 触发 FK `IntegrityError` 500；写入一条值即可阻断该字段的删除 | 【已读码】 |
| O-03 | `backend/apps/regions/views.py:138-150` | `_resolve_cards` 收了 `competition_id` 却**不用于过滤 `Company`** → 概览卡片可引用其他比赛的公司 id 并读到其字段值（跨比赛 IDOR） | 【已读码】 |
| O-04 | 见 **D-08**（P0） | `inf` 落库 → 模块持久 500 | 【已复核，E-2】 |
| O-05 | `backend/apps/company_fields/calc.py:427`、`timer.py:76` | NUMBER 值 `"1e999999999"` 经 `Decimal` → `format(raw,"f")` 展开定点表示 → 实测按指数线性放大（1e6 → 100 万字符，0.001s）→ 单请求 ~1GB 分配/OOM | 【已复核，E-8】 |
| P-01 | `backend/apps/fuels/serializers.py:14`、`warehouses:16-18`、`production_lines:14-16`、`infrastructures:19,23`、`consumer_demands:31` | 价格/容量/工时/需求量**全部无 `min_value`**（`vehicles:52-55` 写全了，属遗漏）→ 实测负值通过；合同引擎保号求和 + SUB 落账 → 负价把"扣钱"变成"加钱" | 【子代理取证】 |
| P-02 | `backend/apps/materials/serializers.py:17` 等 `FloatField` | 接受 `"1e400"/"Infinity"/"NaN"`（DRF 不做有限性校验，`min_value=0` 也拦不住 NaN）→ 出站裸 `Infinity` 让前端拦截器把整段判失败；`NaN` 在 SQLite 落库为 NULL → 引擎按 0 静默计费（与 D-08 同根因，公共层） | 【已复核，E-2/E-5 同型】 |
| P-03 | `backend/apps/parts/views.py:46-62`（products/vehicles 同构） | 嵌套关系/外键无存在性 + 比赛归属 + 重复校验 → 跨比赛引用被接受并随 `include` 泄漏；不存在的 id 与数组内重复 → `IntegrityError` 500 | 【子代理取证】 |
| P-04 | `backend/apps/parts/views.py:141-147`（products/vehicles 同） | 更新时**未剥离 `competitionId`**（`base_crud.py:173-177` 明确剥离，仅这三处漏）→ 基线数据静默跨比赛迁移 | 【子代理取证】 |
| P-05 | `backend/apps/contracts/engine.py:1372-1375` | 合同按**名称**实时解析原料/零件，基线改名或改类型后静默归零 | 【子代理取证】 |
| O-06 | `backend/apps/tech_tree/serializers.py:53` | 科技前置不校验存在/自依赖且 create 非事务 → 幽灵节点、500、成环（前端 `TechTreeManager.vue:337` 无环检测 → ECharts tree 无限递归卡死，见 V-02） | 【子代理取证】 |
| O-07 | `backend/apps/tech_tree/serializers.py:17` | `tier` 是 `FloatField` 而模型是 `IntegerField`：1.5 静默截断，`nan/inf`/超 int64 → 500 | 【子代理取证】 |
| O-08 | `backend/apps/company_fields/views.py:91-99` | 乐观锁默认关闭（`version` 缺省=当前版本，即无条件更新）且前端从不发 `version` → 409 冲突分支不可达，丢失更新 | 【已读码】 |

### 4.5 前端（master 基线）

| 编号 | 位置 | 问题 | 证据 |
| --- | --- | --- | --- |
| F-01 | `frontend/src/api/request.ts:87-95` | 响应拦截器把"非 `{code,message,data}` 信封"的 2xx 一律判失败：`responseType:"blob"` 时 `res.code === undefined !== 0` → **导出/下载 100% 失败**（调用点 `api/index.ts:545,557`） | 【已复核】 |
| F-02 | `frontend/src/utils/deleteConfirm.ts:36-48` | 级联删除确认用 `dangerouslyUseHTMLString` 拼**未转义**的 `name`（后端只校验名称非空）→ 有 `data:*:edit` 的账号写入 `<img onerror=...>` 名称，超管删除时触发存储型 XSS，可读取 `localStorage` 里的 JWT（调用点：`FuelManager:211`、`MapsManager:1470/1714/1791`、`TechTreeManager:544`、`MaterialsManager:463`、`PartsManager:400`、`ProductsManager:398`） | 【已复核】 |
| G-01 | 见 **D-06**（P0） | 简单编辑器 `type`/`kind` 不一致 | 【已复核】 |
| G-02 | `frontend/src/components/industry-types/IndustryFieldGraphEditor.vue:619` | 自动布局 `while` 无入度递减 → 含"从根可达的环"时队列永不空、层级无限递增 → **点一次「自动布局」浏览器标签页卡死** | 【子代理取证】 |
| G-03 | `frontend/src/components/contracts/ContractTypeGraphEditor.vue:1884,1950` | 加载只信 `graph` 画布、保存用画布反推四份 DSL → 「简单编辑改 effects → 可视化编辑 → 保存」或跑 `build_contract_types.py` 后，一次保存即**静默回滚他人/其他工具的配置** | 【子代理取证】 |
| W-01 | 见 **D-05**（P0） | 地点价编辑即清空 | 【已复核】 |
| W-02 | `frontend/src/views/data-management/VehiclesManager.vue:401-408` vs `backend/apps/vehicles/serializers.py:57` | 前端提交/回填 `pathTypeIds`，后端契约是 `vehiclePathTypes:[{pathTypeId}]` → **可通行路径类型永远存不进、界面永远为空**（而赛前自检又要求它非空） | 【已复核】 |
| W-03 | `frontend/src/views/data-management/PartsManager.vue:333,345`、`ProductsManager.vue:333,343` | 选项请求漏 `competitionId` → 超管（`apply_competition_scope` 不过滤）下拉混入其他比赛实体并写入跨比赛关联（CASCADE 删除还会连带清掉别比赛配比） | 【子代理取证】 |
| W-04 | `frontend/src/views/data-management/MaterialsManager.vue:378-385` | 地点价可存入非数字文本（`BigNumberInput` 只画红框仍提交，`Number("abc")→NaN` 绕过 `<=0` 判断），后端不校验 → 引擎按 0 计入均价 | 【已读码】 |
| T-01 | `frontend/src/views/stocks/StockMarketView.vue:355-362` | `slice(0, -0)` 在 JS 中等价 `slice(0, 0)` → 整数价+整数数量时 `intPart=""`、`decPart` 取到总数前两位：`97×100` 显示 **0.97**（真值 9700），`100×100` 显示 **0.1** —— 「预计金额」错 2~4 个数量级 | 【已复核】 |
| T-02 | `frontend/src/views/stocks/StockMarketView.vue:150-158,792-807` | 下单按钮无 `submitting` 闸门（无 `:loading`）→ 双击产生两笔 PENDING 委托（后端只校验余额） | 【子代理取证】 |
| M-01 | `frontend/src/components/dashboard/registerCustomWidgets.ts:80`（经 `main.ts:12` 在**登录前**执行） | 无 token 请求 `/widget-packages` 401 被 `catch{}` 吞掉且无重试 → 本会话自定义控件全部未注册；随后仪表盘把"未注册类型"的控件从 `localStorage` 剪掉 → **用户已保存的自定义布局被永久删除** | 【已复核】 |
| M-02 | `frontend/src/components/layout/TopBar.vue:108-111` | 主动退出只调 `authStore.logout()`，未派发 `auth:kicked` → message store 的 `initialized` 幂等锁不复位 → 同标签页换账号后实时消息弹窗/红点永久失效，且上一账号 toast 残留（跨账号信息泄露） | 【子代理取证】 |
| A-01 | `frontend/src/api/request.ts:336-343`（根因） | `reconstruct()` 在未传 `pageSize` 时按 **50** 切片，且 `normalizeListResponse` 只回 `items` → 大量列表**静默只显示前 50 条**：账号列表（`AccountManagementView.vue:365` 默认 20）第 21 个起不可见/不可管、公司 50、比赛 50、合同 50、科技树 50、燃料 100、收件箱 50（历史消息永久不可达） | 【已复核】 |
| F-06 | `frontend/src/stores/auth.ts:183-197` | 登出**不清理**请求层内存 memo（`request.ts:200/218`，键不含账号）→ 同 SPA 会话 15s 内换账号可命中上一账号的响应（如按 `viewCompanyScopes` 裁剪的公司列表）且不发网络请求；`resetRequestMemo()` 已实现但全仓无调用点（仅设置页） | 【已复核】 |
| F-04 | `frontend/src/permissions/catalog.ts:74-83` | 前端权限等级用 `?? 0` 兜底，未登记 action 退化为 0 → 同域任意权限即通过（fail-open），与后端 `permissions.py:274-277` 的 fail-closed 不一致 → 越权按钮/菜单闪现、点击必 403 | 【已读码】 |
| F-08 | `frontend/src/utils/format.ts:117-122` | `formatTime` 用 `toISOString()` 输出 UTC，而后端 `USE_TZ=True/TIME_ZONE=Asia/Shanghai` → 全站创建/更新/执行时间**少 8 小时** | 【子代理取证】 |
| V-02 | `frontend/src/views/data-management/TechTreeManager.vue:337` | 无环检测 + 后端不拦环 → 互设前置后 ECharts tree 无限递归，默认树视图持续卡死（数据已落库，刷新仍卡） | 【子代理取证】 |
| V-03 | `frontend/src/views/data-management/ContractManageView.vue:1321` | 仅对 `nodeRoute/list/dict` 套用 `inputSchema.default`，`number` 等标量默认值被丢弃 → 引擎按 `to_number(None)=0` 计算，合同静默按 0 落账 | 【子代理取证】 |
| V-05 | `frontend/src/views/data-management/ContractManageView.vue:806,1620` | 实体下拉缓存（原料/基建/科技/地图节点）不随切比赛失效 → 切比赛后仍展示并可提交上一比赛的实体，`nodeRoute` 连线校验用旧边表 | 【子代理取证】 |
| V-06 | `frontend/src/views/data-management/MapsManager.vue:1307,1595` | 双击/右键新建节点把 `stage.getPointerPosition()`（屏幕坐标）直接当世界坐标提交 → 缩放/平移后落点错误、常在视野外 | 【子代理取证】 |
| G-04 | `ContractTypeGraphEditor.vue:1175` | 公式只校验括号、不校验标识符；后端未定义变量回退 0（D-04）→ 改名/拼错即静默按 0 计算 | 【子代理取证】 |
| W-06/W-07/W-08/W-09/W-10/W-11、T-03…T-10、V-07…V-12、M-03…M-07、A-02…A-10、F-03/F-05/F-07 | 见 `code_audit/U0*.md` | 详见各单元报告的 P2 段 | 【子代理取证】 |

### 4.6 分支独有代码的 P1

| 编号 | 分支 | 位置 | 问题 | 证据 |
| --- | --- | --- | --- | --- |
| R-01 | `origin/prep-export-import`（含 bugfix/feature_wuhaoye） | `backend/apps/preparation/archive.py` 导入循环 | 循环吞掉数据库异常且无 savepoint：一次 DB 错误即污染整个事务，接口仍返回 200 与"新增 N"的成功统计 | 【子代理取证】 |
| R-02 | 同上 | `preparation/archive.py`（按 code/key 复用全局合同类型） | 追加模式下"合同实例"因全局合同类型已存在而被整类跳过 → 预设合同一份都不会导入 | 【子代理取证】 |
| R-03 | 同上 | `preparation/archive.py` | 同类型多份合同被合并成一条且被改名 → 合同实例导入丢数据 | 【子代理取证】 |
| R-04 | 同上 | `preparation/archive.py` | 追加模式下账号被"引用的公司已存在"整行跳过 → 分步导入时一个账号都不会创建 | 【子代理取证】 |
| N-01 | `feature/contract-type-code` | `preparation/builder/core.py:1214` | `account(username=...)` 产出的 USER 资金账户 `user_id` 恒为空（`archive._REF_FALLBACKS` 无 `users` 且顺序在后）→ 静默创建无归属账户，零 problem、退出码 0，官方示例 `demo_competition.py:268` 就是这个写法 | 【子代理取证】 |
| E-01 | `feature/contract-type-code` | 见 **D-09**（P0） | 槽位队列二次 build 丢槽位 | 【已读码】 |
| E-02 | 同上 | `contracts/builder/refs.py:409` | 槽位 `"default": ref.entity_id` 把**比赛局部主键**烧进全局合同模板 → 其他比赛引用同一合同类型时静默读到同 id 的另一条实体（串数据）或执行期 400 | 【子代理取证】 |
| E-03 | 同上 | `contracts/builder/values.py:494` | `concat_text()` 误用引擎的 `concat`（2 参列表拼接）→ 3 段以上运行期 `TypeError`（500），2 段得到列表 | 【子代理取证】 |
| E-04 | 同上 | `contracts/builder/values.py:378` | `_spec_to_expr()` 生成引擎沙箱里不存在的 `fieldValue(...)`（运行期 400）与 `inputs[...]`/`scope[...]`（未定义名回退 0）→ 命中全部 `and_/or_/not_` 组合 | 【子代理取证】 |
| L-01 | 同上 | `contracts/builder/validate.py:296,830,936` | "字段引用可达"只认 `kind==FIELD` 的效果与 `FIELD_COMPARE` 条件：**值源里的 FIELD（字段→字段效果）与 DICT/LIST_COMPARE 条件完全不校验** → 拼错字段名零报错，运行期才 400（头号卖点在最常见两类引用上失效） | 【子代理取证】 |
| L-02 | 同上 | `management/commands/build_contract_types.py:310-346` | `--import` 无 `transaction.atomic`，且 DRF `ValidationError`/`JSONDecodeError` 未归一化 → 第 N 个类型失败时前 N-1 个已提交（`ContractType` 是全局模板，所有比赛共用） | 【子代理取证】 |
| L-03 | 同上 | `examples/contracts/complex_contracts.py:309` | `default="[]"` 用字符串当 `list` 默认值，而 CLI `--trial` 正是用 `default_inputs()` 补齐 → 文档给出的标准流程**必然**报"科技前置校验未通过"（测试从不走默认值路径，完全掩盖） | 【子代理取证】 |
| Z-01 | `test` | `backend/examples/excel/build_from_sheets.py:326-336` | 首列以 `#` 开头的**真实数据行**被当注释整行丢弃（实测 `#N/A`、`#1 号矿区` 消失，stats 也不计数）→ 静默缺失资源 | 【子代理取证（含实测）】 |
| Z-02 | `test` | `backend/examples/excel/sheet_spec.py:530/543/575/617…` | 数值列零下界校验：负距离/负单价/负配比/负油耗直写归档，`distance=-3` 会让运输合同运费为负（委托方加钱、承运方扣钱），`validate()` 无一条提醒 | 【子代理取证（含实测）】 |
| Z-03 | `test` | `build_from_sheets.py:297/327` + `sheet_spec.py:131` | 单元格无条件 `strip()` 并数字文本化：全角 `１００`→100、`0010`→10（全局资源 code 撞车会覆盖既有产业类型），文档承诺的"按文本保精度"不成立 | 【子代理取证（含实测）】 |
| Z-04 | `test` | `sheet_spec.py:725-729/757-769` | 股票参数只写 `limitPct` 一个键即绕过全部交叉校验（`1e3`/`100` 均通过，只给提示），与规范"表格层会替你挡住 `limitPct<maxMovePct`"直接矛盾 | 【子代理取证（含实测）】 |
| Z-05…Z-10 | `test` | `build_from_sheets.py:114-134,149-153,229-247`、`xlsx_io.py:172-181` | 损坏/被占用的 xlsx 抛 Python 堆栈（与文档退出码承诺不符）；`--out` 无备份覆盖、配 `--sheets` 把完整归档换成残缺包；**`--inspect` 会执行工作簿指定的 Python 脚本**（实测产生副作用文件，违背"纯只读"承诺）；`--dry-run --create-competition` 会先把比赛真实建进库；写出的 xlsx 含 XML 非法控制字符（自己都读不回） | 【子代理取证（含实测）】 |
| CW-01 | `feature/contract-watcher` | `contract_watcher/contract_watcher.py:374` | 首次基线拉取失败即写 `lastExecutedAt=""` → 一次网络抖动把全部历史 `EXECUTED` 合同当 fresh，**全量重复记账**（且无按合同 ID 的幂等键） | 【子代理取证（含实测）】 |
| CW-02 | 同上 | `contract_watcher.py:402,284` | `dispatch` 吞异常 + 水位按列表 `max` 推进 → 处理失败/状态不符的合同**永久静默漏记**、永不重试、无告警 | 【子代理取证（含实测）】 |
| CW-03…CW-09 | 同上 | `contract_watcher.py:129,137,148,183,231,400-412`、`bookkeeping_example/shang.py:48,54,76` | 轮内单条失败 → 本轮已记账合同下轮重放；offset 分页漂移 + 未按合同 ID 去重 → 同轮重复分发；以 `executedAt` 字符串做水位（严格 `>`）→ 同秒/格式差异永久漏账（实测 `…46Z > …46.731818Z`）；自动生成的默认 handler 静默覆盖用户自定义函数（实测 registry 指向末尾自动 def）；key 无字符集校验，含 `"""`/换行即把 `handlers.py` 写成语法错误 → 全部 handler 失效；`shang.py` 在模块级 `getcontext().prec=2` → 金额量级算错（实测 `999999.99/3 = 3.3E+5`）；账本无合同 ID 判重列 + 打开失败泄漏隐藏 Excel 进程 | 【子代理取证（含实测）】 |
| CW-结构 | 同上 | `contract_watcher/handlers.py` | 该文件实为 **11 行空模板**，仓库内不存在任何记账规则，"写 xlsx"完全依赖用户自行接线 `bookkeeping_example/shang.py` → **默认状态下该程序不写任何 xlsx**；任务清单假设的 `.old-DATE.xlsx` 轮转逻辑在本分支不存在 | 【子代理取证】 |
| D-01/D-02 | `bugfix` / `feature_wuhaoye` | `scripts/stop-dev.bat:24-29`、`scripts/dev.py:273,391` | 端口兜底 `taskkill /T /F` 任何监听 8000/5173/8120 的进程（无身份校验）→ 误杀 Docker/其他项目；PID 文件只有裸 PID 且非优雅退出不清理 → PID 回收后误杀无关进程树（而 `dev.py:265` 的报错文案正是引导用户去跑它） | 【子代理取证】 |
| X-01 | master（部署脚本） | `scripts/verify-migration.sh:10,22-24` | `check_pass/check_fail/check_warn` 里的 `((PASS++))` 在 `set -euo pipefail` 下返回 1（首次自增时旧值为 0）→ 脚本在**打印第 1 个检查结果后立即中止**，`[2/5]`~`[5/5]`（服务/API/权限/配置）全部不执行、"验证结果"总结永不打印；按 bash 语义退出码为 1（单元报告写作 0，本机无可用 bash 未能实测，两种情形都使"迁移验证"失去意义） | 【已读码】 |
| X-02/X-03 | master（部署脚本） | `scripts/migrate-server.sh:39,147-163,211-242` | 含全部密钥的 `backend/.env` 被无 `-p` rsync 到远端 `/tmp`（umask 022 → 644 世界可读、无清理）；`$INSTALL_DIR`/`$SSH_OPTS` 未加引号 → `--install-dir "/opt/my app"` 静默错位，含 `;` 即在目标机以 root 执行任意命令 | 【子代理取证】 |
| X-04…X-10 | master（部署/同步脚本） | `scripts/*.sh`、`deploy/*` | 恢复数据库 `cp||true` 吞错仍报"已恢复"（可静默丢库）；`quick-sync.sh` 对运行中的 SQLite 活库单文件 rsync（丢 `-wal/-shm` 可致库损坏）；root `curl\|bash` 装 NodeSource 无校验；`git pull` 失败降级 WARN 后仍 migrate+restart；日志查看器端口三方不一致；验证脚本只测 db 文件大小；无回滚路径 | 【子代理取证】 |

---

### 4.7 本次审计额外发现（主审计员独立验证，不属于任何单元）

| 编号 | 位置 | 问题 | 证据 |
| --- | --- | --- | --- |
| M-1 | `backend/apps/users/models.py:80-97` | 业务 User 直接用裸 bcrypt（无 SHA256 预处理）→ **密码在第 72 字节处被截断**：`checkpw("a"*72+"Y", hash("a"*72+"X"))` 实测为 True。中文（3 字节/字）约 24 字、emoji（4 字节/字）约 18 字即可触达；`set_password` 侧无长度上限也无提示（`users/views.py:157` 与 `auth/views.py:241` 只校验 ≥8 位） | 【已复核，E-7】 |
| M-2 | `tests/big_number_smoke.py:100-104` | 测试第 6 组 `from apps.company_fields.timer import _to_num` 引用了**不存在的函数名**（实际是 `_parse_number_raw`），异常被宽 `except` 转成"SKIP"，于是该组断言从未执行：实跑输出 `PASS=20 FAIL=0` 却带着一个永久 SKIP 的盲区（`tests/*` 另有多份脚本是生产算法的"重抄版"，见 U14 X-16） | 【已复核，E-6】 |
| M-3 | `backend/apps/contracts/engine.py:1144-1148`、`:715-743` | 除抛错外还有两类**静默错值**：`LOG(x, base)` 在 `base<=0` 时直接退化为自然对数（不报错、不算该底数）；`EXP(1e400)` 返回 `inf` 并继续参与后续运算（`MIN([inf,5])` 实测得 0） | 【已复核，E-3/E-4】 |

---

## 5. P2/P3 全量索引

**282 条** P2/P3（149 + 133）的逐条清单（编号 / 级别 / 位置 / 一句话）见 **`code_audit/00-缺陷总索引.md`**；每条都能在对应单元报告里找到"代码摘录 + 触发条件 + 后果 + 修复建议"。单元规模与分级统计见 **§0.2** 的表。

单元报告文件：

- master 基线：`code_audit/U01-backend-core.md`、`U02-backend-identity.md`、`U03-backend-org.md`、`U04-backend-production.md`、`U05-backend-contracts-core.md`、`U06-backend-stock.md`、`U07-frontend-core.md`、`U08-frontend-views-data1.md`、`U09-frontend-views-data2.md`、`U10-frontend-views-biz1.md`、`U11-frontend-views-biz2.md`、`U12-frontend-components1.md`、`U13-frontend-components2.md`、`U14-scripts-deploy-tests.md`
- `bugfix` / `feature_wuhaoye`：`code_audit/B01-dev-scripts.md`
- `origin/prep-export-import`：`code_audit/B02-preparation-io.md`
- `feature/contract-type-code`：`code_audit/B03-preparation-builder.md`、`B04-contracts-builder-core.md`、`B05-contracts-builder-validate.md`
- `test`：`code_audit/B06-excel.md`
- `feature/contract-watcher`：`code_audit/B08-contract-watcher.md`

---

## 5.5 逐分支结论（每个分支的新增代码各自带来了什么）

分支是**递进包含**关系：`master` ⊂ `prep-export-import` ⊂ `bugfix` ≡ `feature_wuhaoye` ⊂ `feature/contract-type-code` ⊂ `test` ⊂ `feature/contract-watcher`。因此越靠后的分支承载的缺陷总量越大；下表只列"**该分支相对前一个分支新增的代码所引入的缺陷**"，避免重复计数。

| 分支 | 新增代码 | 该分支独有的缺陷 | 结论 |
| --- | --- | --- | --- |
| `master` | 全仓既有业务代码 | U01–U14 共 **237 条**（P0×7/P1×44/P2×98/P3×88） | 主要风险集中在：金额精度（D-01）、合同越权与静默错值（D-02/D-04/D-07）、股票撮合与账户（D-03/S-02/S-05）、部分视图"编辑即清空"（D-05/D-06）、以及部署脚本的密钥与注入（X-02/X-03） |
| `origin/prep-export-import` | `apps/preparation/{archive,checklist,plan,views,urls}.py` + 2 个前端弹窗 + 5 个被改文件 | B02 共 **20 条**（P1×4/P2×10/P3×6），无 P0 | 全部集中在**归档导入语义**：异常被吞导致"报成功实际回滚"（R-01）、追加模式把合同实例/账号整类跳过（R-02/R-04）、同类型多份合同被合并改名（R-03）。权限、dry-run、密码不导出这些"安全侧"经核对是正确的 |
| `bugfix` ≡ `feature_wuhaoye` | 仅 `scripts/dev.py`（397 行）+ `start-dev.bat`/`stop-dev.bat` | B01 共 **11 条**（P1×2/P2×5/P3×4），无 P0 | 新增开发启动器本体没有致命问题，但两个副作用必须修：`stop-dev.bat` 无差别 `taskkill` 任何占用 8000/5173/8120 的进程（会误杀 Docker/其他项目），以及 PID 文件无身份校验导致的误杀；`CTRL_BREAK` 优雅停止的核心目标经验证在按键路径上成立 |
| `feature/contract-type-code` | `contracts/builder/**`（8 文件）、`preparation/builder/**`（4 文件）、两个 management 命令、3 份测试、examples | B03 **18 条** + B04 **17 条** + B05 **19 条** = **54 条**（P0×1/P1×7/P2×21/P3×25） | 三个"构建期防错"卖点都有洞：实体槽位二次 build 必丢（D-09，CLI 导入路径 100% 命中）、静态体检 60 项**漏掉最常见两类字段引用**（L-01）、`--import` 无事务（L-02）；`preparation/builder` 的账号引用与子资源列缺失会在追加导入时静默改坏既有数据（N-01/N-02） |
| `test` | `examples/excel/**`、`auto_chain_*`/`mini_contracts` 示例 | B06 共 **17 条**（P0×4/P1×6/P2×4/P3×3） | Excel 建包链路的校验强度不足以支撑"读表建比赛"：首列 `#` 开头的真实数据行被当注释丢弃（Z-01）、数值列零下界导致负距离/负价进归档（Z-02）、单元格文本被无条件 strip+数字文本化（Z-03）、股票参数可绕过交叉校验（Z-04）；此外 `--inspect` 会执行工作簿里的 Python 脚本、`--dry-run` 会真实建比赛入 |
| `feature/contract-watcher` | `contract_watcher/**`（4 个 py + 样例/记录） | B08 共 **27 条**（P0×2/P1×7/P2×11/P3×7） | 水位线与幂等是硬伤：首次基线失败即清水位 → 历史合同全量重放重复记账（CW-01）；dispatch 吞异常 + 水位按 max 推进 → 合同永久静默漏记（CW-02）；另有以字符串时间做水位、自动生成 handler 覆盖用户代码、把 key 内插进 docstring 等。**注意**：本分支的 `handlers.py` 实为 11 行空模板，默认状态下程序不写任何 xlsx，上述后果以"按文档接线后"为前提 |

> 继承关系提醒：修 `master` 的缺陷等于同时修全部 5 个分支；反之只修 `feature/contract-watcher` 顶部提交不会让其余分支受益。若要合并分支，建议先修 §8"第一批/第二批"（都在 master 基线内）。

---

## 6. 横切主题分析

### 6.1 数值边界（用户重点）

- **精度承诺与存储现实脱节（最严重）**：全仓把金额改成 `DecimalField(max_digits=60)` 并配了 BigInt 化前端，但 SQLite 的 NUMERIC 亲和性把 >15 位有效数字静默转 REAL（E-1）。这不是某处写错，而是"任意精度金额"的设计假设在默认数据库上不成立。
- **浮点与 Decimal 混算**：`avg/^/pow/EXP/LOG` 与整个公式内置函数表都经 `float`（`engine.py:649-743`）；`apply_op` 的 `EXP` 还能产出 `inf` 继续参与运算（E-4）。
- **除零与域错误**：DSL 的 `DIV` 对 0 返回 **0**（静默错值而非报错），而 `LOG(x,1)` 除零、`log(0)`/`sqrt(-1)`/`asin(2)` 直接 500（E-3/E-4）—— 同一引擎里"静默错值"和"崩"两种策略混用。
- **量化为 0 的循环**：`stock/engine.py:1612` 的 6 位量化与 `:1599` 的 `EPS=1e-9` 守卫不匹配 → 死循环（D-03）。同类风险：`round2` 超精度返回 0（S-09）。
- **缺下界**：多个 `DecimalField/FloatField` 没有 `min_value`（P-01）；前端 `BigNumberInput` 的 `min` 只画红框不拦提交（M-05/W-06）。
- **JS 侧数值坑**：`slice(0,-0)`（T-01）、指数记法被正则判非法导致金额显示"—"（F-10）、`Number()` 化大数丢精度（B2 残留，前端图预览）。

### 6.2 字符串边界

- **None/空串/纯空白**：`_check_conflict` 在唯一字段为 None 时直接跳过冲突检测（`base_crud.py:106-111`）；`PATCH {"name":"   "}` 通过校验后被 strip 成空串（O-13）。
- **重名判定**：不折叠大小写、全角/半角、零宽字符、BOM（O-16、B-17、P-08、A-10）→ 视觉同名可重复创建，而唯一约束只在数据库层做字节比较。
- **长度与 255 上限**：`origin` 以节点名 JSON 存进 `max_length=255`，约 28 个产地即 400（W-14）；`nodePrices`/`note`/公告日期等自由文本无长度校验（P-06、P-09、A-19）。
- **注入面**：`deleteConfirm` 的 `dangerouslyUseHTMLString` + 未转义名称（F-02，实锤存储型 XSS）；`sanitizeHtml` 的 `href` 白名单可被实体编码 + 控制字符绕过（F-09/M-11）；`build_contract_types` 把 key 直接内插进生成的 docstring（CW-07）。

### 6.3 网络与并发

- **前端请求层**：全局 15s 超时套用到上传/归档导入（服务端可能已落库、重试即重复，F-03）；`_memo` 与 `_getInflight` 无账号维度且在登出时不清（F-06）；重连时对每个集合并发全量对账，无上限/无单飞（F-07）；各视图 `loadData` 普遍缺请求代次校验 → 旧响应覆盖新比赛（V-09/W-08/A-05/T-03）。
- **后端并发**：全仓无 `select_for_update`（K-10），SQLite 上即使写了也失效（S-10）；`transaction.atomic` 覆盖不全（K-07 的"跳过写入但仍记成功"、R-01 无 savepoint、L-02 导入无事务）；唯一性检查普遍是"先查后插"TOCTOU（C-08、O-09、O-11、F-05、I-09）。
- **实时层**：顶号不断连（I-01）、会话角色快照不刷新（I-11）、seq 取号与环形写入分属两锁（I-10）、房间隔离本身实现正确（`_can_join` 有白名单）。
- **Doze/OOM 类**：整文件 `f.read()`（I-06）、zip bomb（I-07）、`format(Decimal,'f')` 指数展开（D-08/O-05）、`LIST_RANGE` 无长度上限（K-16）。
- **部署脚本**：活库单文件 rsync、`cp||true` 吞错、无回滚、`curl|bash`（X-04…X-10）。

### 6.4 多账号与权限

- **禁用/改密/重置密码不吊销会话**（I-02、I-03）：系统只有"登录时 + `token_version`"两处控制，管理动作没有连接到吊销。
- **顶号机制只完成了一半**（I-01）：HTTP 侧靠 `tv` 生效，WS 侧只发通知不断连。
- **越权读**：`regions` 概览卡片不过滤比赛（O-03）、合同详情对操作类账号不过滤公司范围（K-09）、`selectable-users` 让选手枚举全比赛账号（I-15）、`/uploads/**` 无鉴权直出（C-11）。
- **越权写**：合同参与方公司无归属校验（D-02）、`bindFieldId` 漏校验（S-05）、跨比赛 `stock:manage` 下单（S-06）、三类资源更新未剥离 `competitionId`（P-04）。
- **权限模型自身**：扩展集永远授不出去（I-13）、`permissions=null` 实为零权限（I-19）、前端 fail-open 与后端 fail-closed 不一致（F-04）、`CompetitionScopePermission` 因"所有视图都显式声明 `permission_classes`"而**实际零挂载**（`settings.py:336-343` 的"全局兜底"运行时不生效，C-12）。
- **审计与身份绑定**：审计只挂 `post_save/post_delete`，`.update()/bulk_create` 全绕过（C-04，合同执行落账就无留痕）；含 `DecimalField` 的模型写审计时 `json.dumps(Decimal)` 抛错被 `except` 吞掉 → **零审计**（C-02）；异常审计无条件落库且未认证 401 也写（C-05）。

### 6.5 账号角色可达性校准（决定定级的关键）

由 `_probe_perms.py`（E-5）实测得到的真实可达性：

| 能力 | SUPER_ADMIN | COMPETITION_ADMIN | PLAYER |
| --- | --- | --- | --- |
| `competition:manage` / `account:manage` / `stock:manage` | ✔（隐式全权） | ✘ | ✘ |
| `data:*:edit`（原料/零件/地图/基建/科技/燃料/载具/仓库/产线） | ✔ | ✘ | ✘ |
| `data:region:edit`、`contractType:manage`、`industryType:manage`、`company:manage`、`message:manage` | ✔ | ✘（在"扩展集"里，但代码路径拒绝一切扩展集授权） | ✘ |
| `contract:manage` / `contract:audit` / `contract:execute` | ✔ | **✔（默认）** | ✘ |
| `stock:edit`（资金账户管理） | ✔ | **✔（默认）** | ✘ |
| `stock:view`（含下单/撤单） | ✔ | ✔ | **✔（默认）** |

推论（用于纠正定级）：
- 依赖 `data:*:edit` 的 P0/P1（D-08 地图 `inf`、O-05 大数 OOM、O-04 同源）当前**只有超管**能触发 → 属"超管自伤/持久污染"，仍应修但不必按外部攻击面计。
- 合同域（D-02/K-02/K-03/D-07）与股票域（S-05、S-06）的缺陷**比赛管理员即可触发**，且 `stock:view` 让最普通的选手账号能下单 → 这三处是真正的跨账号风险面。
- 后端的权限判定对拼错的 action 是 fail-closed（优于前端），但**扩展集不可授予**使"比赛管理员"实际权限被锁死在默认集内 —— 这是权限模型的功能缺陷（I-13），修复它之前不应假定 `company:manage` 等能力可用。

---

## 7. 已排除的误报（避免重复排查）

各单元在审计中主动证伪并排除的方向（节选，完整见各报告"已排除/已核对"段）：

- **后端**：JWT 算法白名单/`iss`/`aud`/`exp` 校验正确；密钥 fail-fast 且 `.env` 未入库；token 不含权限明细、授权每次现查 DB；强制改密在认证层拦截；审计 `changes` 有脱敏；永不信任 `X-Forwarded-For`；分页对 0/负数/超大值有夹紧；上传魔数校验与重命名、zip 路径穿越已被拦；Socket.IO 未认证拒绝、房间/重放过滤正确；logviewer `_safe_path` 与启动密钥校验；`DIV` 除零返回 0 属**有意设计**（但见 6.1 的评价）；`stock` 的除零/0 分母类边界（压力分母 +1、碳排均值 ≤0、相对排名 `len>2`、K 线 `open==0`）均已守卫；`_release_advance_lock` 的双重 release 因 guard 覆盖而无害；`makemigrations --check` 无缺失迁移（E-9）。
- **前端**：401 并发不重复刷新、不覆盖 token（无 refresh token + `curToken` 比对）；`useResourceChanged` 与 window 监听均有解绑；写后精确失效与强制重置；路由守卫对未登录/越权/直连 URL 处理正确；账号级 IndexedDB 分库与 localStorage 前缀迁移正确；版本封锁与 `Authorization` 剥离正确；`el-input` 的 IME 组合态有 `isComposing` 守卫；`removeNode` 已清悬空边、自环被拦、`zoom` 有 clamp（0.3–2.5）无除零/NaN、`uid` 同毫秒不冲突；控件包 `componentUrl` 由后端 `MEDIA_URL` 拼接且上传仅超管（非任意远端脚本）；`widget-package-examples` 的 zip 与源码逐字节等价。
- **建包/工具链**：`--export` 无文件写入路径（不存在路径穿越）；`--trial` 确实不落库；`ContractType` 是全局模板无租户越权写入；`preparation/builder` 的枚举与各模型 `choices` 全部一致、列名自描述与产物/导出双向一致、dry-run 确实回滚、日志不泄漏密码、无硬编码账号字面量。
- **脚本**：`CREATE_NEW_PROCESS_GROUP` + `CTRL_BREAK` 确实能穿透 venv redirector 打到真解释器（提交声称的修复在按键路径上成立）；Vite 的 readline 未切 raw，真实 Ctrl+C 有效。

---

## 8. 修复优先级建议

> **修复进度（分支 `bugfix-merged`）**：第一批 8 项已完成（D-01/D-02/D-03(+相邻缺陷)/D-04/D-07/D-08/D-09，D-05 按你的要求仅解释、D-06 忽略）；
> 第二批（会话与权限）**12 项全部完成**：I-02、I-03、I-01、C-01/I-05、S-05、S-06、I-13、I-19、C-02、C-04；
> 第三批已完成：13（列表分页静默截断）、F-01（blob 下载判失败）、F-02（删除确认存储型 XSS）、F-08（时间少 8 小时）、F-06（登出不清内存 memo）、M-01（控件包登录后重载 + 布局不再丢）、15（W-02 载具路径类型契约、W-03 选项下拉比赛域、V-05 切比赛缓存失效、W-08/V-09 并发请求代次守卫）、16（T-01 预计金额、T-02 下单防连点）、F-12（page/pageSize 非法值）； 第四批已完成：CW-01/CW-02（水位线与失败重试）、CW-04/CW-05（水位线按时间比较 + 合同 id 去重）、CW-06（自动生成不屏蔽用户 handler）、CW-07（生成代码转义/校验/原子写）、CW-08/CW-09（记账样例 Decimal 精度与 Excel 进程回收）、R-01（归档导入按资源 savepoint）、Z-01（Excel 建包注释行误判）； 归档导入追加模式语义：R-02/R-03/R-04；建包校验：Z-01/Z-02；建包 P0 收口：Z-03（全角/指数字面量不再被静默归一）、Z-04（股票参数交叉校验与上界）、Z-05（文件级异常中文化）；建包 CLI：Z-06（复用比赛可反复执行）、Z-07（--out 覆盖需 --force + 规模显示）、Z-08（--inspect 不执行脚本）、Z-09（--dry-run 不落库）；xlsx 写入与归档导入：Z-10（XML 控制字符）、R-05（状态不覆盖）、R-06（账号先于消费方导入）、R-07（消息写收件人）、R-08（概览卡片按模式处理）；R-09（重名公司确定性绑定 + 不再整类失败）、Z-11（工作表名校验）；R-10（覆盖模式更新股票/资金账户）、Z-16（CSV 表名安全化）；R-10 余项（地图节点坐标与区域说明）；Z-12（Excel 错误值显式拒绝）；Z-13（校验一次报全）；CW-10（凭据失效达阈值退出）；CW-13（进度文件原子写与损坏留证）；CW-11（--competition 校验）；CW-23（同秒存档不再互相覆盖）；CW-24（输出子目录名不得越出 out_dir）；CW-25（改名不再子串误伤）；CW-26（运行产物纳入 .gitignore）；CW-27（缺 executedAt 的合同告警）；
> **待办**：**§8 清单已全部清零**——第一～第九批列出的缺陷（含 X-01～X-26）均已修复并各自单独提交；第十批 X-27、第十一批 X-28、第十二批 X-29（见下）亦已修复。
> **第十二批（合并 master 后的回归收口，新增）已完成**：X-29（**三处回归用例本身失真，与 master 合并无关、合并前就已假红**）。① `test_x20_install_dir.py` 的 `test_quick_sync_keeps_crlf` 断言的是 X-27 **之前**的坏状态（`assertEqual(CRLF, LF)` = "必须是 CRLF"），而 `179712f` 只加了 `.gitattributes`、没同步改它 → `quick-sync.sh` 已是 `LF=315/CRLF=0`（正确）却**恒失败**，且失败信息"行尾不再是 CRLF"会把排查引向错误结论；改为断言 `CRLF == 0` 并改名 `test_quick_sync_is_lf_only`。② `test_x17_db_safety.py` / `test_x19_start_dev_exit.py` 把被测脚本的 UTF-8 输出与经 Windows 控制台（中文 Windows = GBK）的输出混在一个流里硬按 `encoding="utf-8"` 解码，中文变 `\ufffd` → 中文断言（如"超精度值必须显式报错"、正则 `dev\.py --check-only（端口被占）`）**永不匹配**，表现为"脚本坏了"而脚本完全正常；新增 `tests/fix_verify/scripts/_decoding.py`（`decode_output()` / `run_captured()`：UTF-8 优先、回退系统首选编码）并改用它。改后 **42/42 个用例文件全绿、`manage.py test tests_fix_verify` 280 条无 error**。
>
> **第十四批（强制改密流程，2026-09-24 真机实战新增）已完成**：X-31（**新部署的超管在「修改初始密码」界面必报「登录已过期」，初始密码永远改不掉 = 新部署无法登录**；顺带让种子口令在终端显示、并修掉"重部署不重启服务"）。四个缺陷叠加：① 前端 `authStore.login()` 成功后立即启动 **20s 会话心跳**打 `GET /api/auth/me`，而后端强制改密门禁（`authentication.py::_CHANGE_PASSWORD_PATHS`）只放行 `/api/auth/change-password` → 心跳收到 401「账号需先修改初始密码」；② 前端全局 401 拦截器（`request.ts`）把这枚 401 当成**会话过期** → 清 token、清缓存、跳 `#/login`、派发 `auth:kicked` → `logout()` 连内存 `user` 一起清空；③ 用户随后在仍开着的改密弹窗里提交时请求已**不带 Authorization** → 后端返回 401「登录已过期，请重新登录」（与用户所见逐字一致）；④ store 里「401 自动重登」的兜底依赖 `user.value?.username`，而它已被清空 → 不进兜底。**只要登录后超过 20 秒才提交改密就必然失败**（20 秒内提交才能侥幸成功，表现为时好时坏）。另有一个让修复"看起来无效"的部署缺陷：`deploy-linux.sh` 用 `systemctl enable --now` 启服务 —— 对**已在运行**的 unit 是空操作，重部署后 daphne 仍是旧进程，后端改动完全不生效（真机复现：改完 auth 层重跑部署，接口行为一字未变）。修法：① 后端把 `/api/auth/me` 纳入门禁豁免（只读自身资料），并让 401 响应体带上**增量**字段 `errorCode`（保留 DRF 机器码 `must_change_password` / `token_version_mismatch` 等；改前 envelope 只有 HTTP 状态码，前端只能按中文文案猜语义）；② 前端三层防护：401 按 `errorCode` 识别门禁（绝不清 token / 跳登录 / 派发 kicked）、`startHeartbeat()` 在 `mustChangePassword` 期间不启动、`changePassword()` 支持传入用户名并在无 token 时先用旧密码恢复会话；登录页预填刚输入的初始密码；③ `systemctl enable --now` 改为 `enable` + `restart`（首次安装等价于 start，重部署强制加载新代码）；④ 种子口令改为**终端（TTY）下默认显示**、非终端（管道/CI/重定向）仍绝不打印（`--print-seed-password` 退化为历史兼容开关，X-22 的"绝不进日志"不变式保持不变）。新增 `backend/tests_fix_verify/test_i04_must_change_password_gate.py`（4 用例）与 `tests/fix_verify/frontend/test_x31_forced_password_change_flow.mjs`（8 项跨层静态不变量）；`test_x22_secret_logging.py` 与本地探针 `code_audit/_x22_harness.sh` 同步到新口径（顺带修掉探针自身缺陷：按"下一个 `fi`"截片段 → 加一行后 `else/fi` 失配 → bash 语法错误被 `|| true` 吞掉 → 四行恒报"否"的假绿灯；现改为 if/fi 嵌套计数 + `bash -n` 校验 + 预置 `ADMIN_PW`/`_SEED_PW`）。真机验证（VMware Debian 13，TTY 部署）：`DEPLOY_EXIT=0` 且终端直接打印 `🔑 管理员密码：<20 位>`（非终端告警 0 次）；`GET /api/auth/me` 由 **401 → 200**、业务接口仍 401 且带 `errorCode=must_change_password`、改密 200 且标记清除、旧 token 401 + `token_version_mismatch`、admin 自身 `/me` 200（心跳不再杀会话）。
>
> **第十三批（运行用户创建路径，2026-09-23 真机实战新增）已完成**：X-30（**「系统里没有 `gipfel` 用户和组」时部署报错**，真机 Debian 13 + VMware 复现、定位并修复）。三个缺陷叠加：① `deploy-linux.sh` / `update-from-github.sh` 建用户写成 `useradd … || true`，而 `useradd` 位于 `/usr/sbin`；以 `su`（**不带 `-`**）部署时 PATH 为 `/usr/local/bin:/usr/bin:/bin:/usr/games`（`apt-get`/`chown`/`systemctl` 都在 `/usr/bin`，所以环境"看起来完全正常"），`useradd: command not found`(127) 被 `|| true` 吞掉 → 脚本跑完 migrate + 前端构建之后在 `chown -R gipfel:gipfel` 处以 `chown: invalid user: 'gipfel:gipfel'` 终止：现场留下「`.venv`/`db.sqlite3`/`frontend-dist` 都在、却没有 `gipfel` 用户/组与 systemd 单元」的半成品部署，且报错把排查方向引向文件权限；同一 PATH 依赖还会让 `nginx -t`（`/usr/sbin/nginx`）二次引爆。② 守卫 `id gipfel` 只查**用户**，而 `-U` 要求同名组未被占用：「组在、用户不在」时 `useradd -U` 退出 9（`group gipfel exists … use -g`），同样被吞掉；该状态真实可达 —— 组内还有其它成员时 `userdel gipfel` **不会删组**（`group … not removed because it has other members`）。③ `migrate-server.sh` 建用户**无 `-U`**（目标机 `USERGROUPS_ENAB=no` 时只建用户不建组 → `chown: invalid group`、unit `Group=gipfel` 使服务以 216/GROUP 启动失败），且 push 模式 chown 排在建用户**之前**并 `2>/dev/null || true` 吞错（文件留 `root:root`：服务 active 但写 `db.sqlite3`/`uploads` 权限拒绝），pull 模式的 chown 连 `|| true` 都没有。修法：三个脚本启动时 `export PATH=…:/usr/sbin:/sbin:…`；新增公共库 `ensure_runtime_user`（用户与组一起校验、组已存在改 `-g` 复用、用户已在而组缺失补 `groupadd`、失败立即 `err`），`deploy-linux.sh` / `update-from-github.sh` 改调它并在 chown 前加断言；`migrate-server.sh` 新增 `_ensure_runtime_user_cmd` 片段、把建用户移到 chown 之前并去掉 `2>/dev/null`；`docs/MIGRATION.md` 手工步骤补 `-U`；`deploy/README.md` 明确「必须 `sudo -i` / `su -`，不要用 `su`」。新增回归用例 `tests/fix_verify/scripts/test_x30_runtime_user.py`（含 16 项断言脚本 `code_audit/_repro_gipfel_user_group.sh` 的真机复现）。真机验证：Debian 13 上以 `su`（坏 PATH）+「组在、用户不在」状态重跑 `deploy-linux.sh` → **完整成功**（`gipfel:gipfel (997:989)`、三服务 active、`/api/health` 200、站点 200）。
>
> **master 合并（第十一批同期）**：`origin/master` 领先的 16 个提交已并入 `bugfix-merged`。判定方法：以 merge-base（`a4aac46`）为基准，master 改 36 个文件、bugfix-merged 改 321 个，**真重叠 25 个**；`git merge-tree` 预演得实际冲突 **3 文件 8 块**，与真实合并一致。**16 个提交中 10 个触碰了修复区域**，按 repo 主规则「被 master 改掉者以 master 为主、未被改掉者保留」解决（7 块取 master，1 块 `--origin-cert` 等新增参数与 `--allow-stale-code` 同类叠加故两者都留）。合并后 **X-11 / X-22 / X-14 三处修复被 master 覆盖**（详细取证见 `code_audit/U14-scripts-deploy-tests.md` 各条目的"master 合并后重做"段），已在本批**重新做回**：X-11 保留 master 的 `AH_ENTRIES` 数组收集逻辑、只把 `sed "…|&,${_ah}|"` 换成 lib 的 `append_env_entry`（去重合并 + 唯一行）；X-22 收尾口令回显补回 `PRINT_SEED_PASSWORD == "1" && -t 1` 守卫（非 TTY 下明确告警不打印）；X-14 以 master 重写版（域名形态/CSRF/证书诊断）为基底，合并回掩码、curl 超时、端口同源、`sudo -n` 降级提示、安装目录可传入 5 项加固。**X-08 经复核并未丢失**（master 新增 80/443+TLS 端口块，无域名块仍走 `log_viewer_port` 同源端口）。合并结果以真 Linux 复核：全部 `.sh` 的 `bash -n` **9/9 OK**、`_wsl_verify.sh` **PASS=45 FAIL=0**（45 = 原 44 + master 新增 `tests/https-443-diag.sh`）。
> **重要更正（两条）**：
> ① 本机存在可执行的 bash —— `D:\Git\bin\bash.exe`（Git for Windows 5.3.15）。此前"无可执行 bash（WSL 未安装）"的结论是因为 PATH 里的 `bash` 指向 WindowsApps 下的 WSL 存根（执行报 `Win32 error 5`）。自 X-15 起，X 系列改为**真实执行**验证。
> ② 更关键：**WSL 其实是可用的**（`Ubuntu-26.04`，WSL2，PID1 就是 systemd），此前的 `E_ACCESSDENIED` 是沙箱拦了命名管道而非没有 WSL。同时在**真 Linux bash 5.3.9** 下复验时立刻发现——Git for Windows 的 bash 容忍 CRLF，所以此前所有"`bash -n` 通过"的结论**不能**证明 Linux 可用（详见第十批 X-27）。
> **第五批（合同监听工具链）已完成**：CW-12（增量拉取 + 失败退避 + 类型目录节流）、CW-14（金额 Decimal 校验，不再静默漏账/丢精度）、CW-15（readable 时间本地化带时区标注、数字展示不再改写）、CW-16（槽位判定对齐模板，写不下报错而非覆写最后一行）、CW-17（账期取合同 executedAt）、CW-18（科目 Find 显式校验，未命中不再静默漏账）、CW-19（主循环心跳 + 停滞看门狗，Excel 挂起可被发现并自愈）、CW-20（共享锁互斥，跨机器不再重复记账）、CW-21（自测/浸泡脚本用退出码表达成败）、CW-22（运行后哈希真算、taskkill 不误杀用户 Excel）。
> 每个修复均单独提交，提交信息内含「改前/改后」实测输出与回归结果（`manage.py test apps` 153 用例、`tests_fix_verify` 用例）。
> **当前 HEAD 全量回归认证（179712f）**：`manage.py test apps` 153 OK、`tests_fix_verify` 280 OK（合并跑 433 OK）；watcher 22 个测试文件 153 项全绿；`tests/fix_verify/scripts/test_*.py` **20 个文件 168 项全绿**（B01 13、X-01/09 7、X-02/03/04 13、X-05 6、X-06/07/08 12、X-10 7、X-11/12 12、X-13/14 15、X-15 6、X-16 5、X-17 6、X-18 6、X-19 5、X-20 7、X-21 9、X-22 8、X-23 6、X-24 8、X-25 9、X-26 8）；`tests/deploy_public_ip_test.sh` 36 PASS/0 FAIL（三种 cwd 一致）；`tests/big_number_smoke.py` 22 PASS/0 FAIL、`tests/sqlite_decimal_roundtrip.py` 10 PASS/0 FAIL（均跑在一次性内存测试库上，`backend/db.sqlite3` 哈希不变）；`contract_watcher/selftest.py` 11 PASS/0 FAIL（退出码 0）。
> **新增认证（WSL 真 Linux，Ubuntu 26.04 / GNU bash 5.3.9 / PID1=systemd）**：`code_audit/_wsl_verify.sh` → **PASS=44 FAIL=0**，覆盖 8 个 `.sh` 的 `bash -n`、`deploy_public_ip_test.sh` 三种 cwd（各 36/0）、`deploy-common.sh` 纯函数、`snapshot_sqlite_consistent` 对活 WAL 库（`integrity=ok`，对照 `cp -a` 副本 `no such table`）、`migrate-server.sh` 退出码 0/2/1/2 与 `--dry-run` 零网络零残留、`quick-sync.sh` 源校验与 SSH 选项、`deploy-linux.sh` 帮助与口令守卫、`systemd-analyze verify` 无指令级错误、`print_rollback_hint` 六步齐全。记录见《WSL与生产环境验证结果记录.md》。
> **第十批（生产可用性，新增）已完成**：X-27（**Windows 检出把 `.sh` 变成 CRLF，导致 8/8 部署脚本在 Linux 上 `bash -n` 全部失败**——Git for Windows 的 bash 容忍 CRLF 所以此前从未暴露；提交 `179712f` 新增 `.gitattributes: *.sh text eol=lf` 固定行尾，对象库本来就是 LF 故无内容变更）。
> **第十一批（Windows 引导脚本，新增）已完成**：X-28（**`bootstrap-dev.bat` 的保窗守卫用裸 `shift`，cmd 下 `%0` 被一起移走 → 守卫之后的每个 `%~dp0` 都从"脚本所在目录"变成"第一个参数"**）。X-18 为消除父环境污染改用"专用参数 + `shift`"时引入：只要传了文档推荐给 CI 的 `--no-keep-open`，`%~dp0` 就解析到仓库**上一级**，`BACKEND` 变成 `...\GipfelBusinessCompetitionManagerWeb\..\backend`，实测在依赖自检阶段即 `[ERROR] missing ...\..\backend\requirements.txt` 退出 1；历史相对路径写法则表现为 `can't open file '...\backend\gen_logviewer_key.py'` 被上层显示成 `[ERROR] failed to ensure LOGVIEWER_SECRET_KEY`，真正原因完全不提示。且原回归测试 `test_no_keep_open_path_runs_inline` 断言的恰是 buggy 写法（`assertIn('...--no-keep-open" shift')`），等于把缺陷锁死。修法：在**任何 `shift` 之前** `set "SCRIPT_DIR=%~dp0"`，之后 `cd /d`、`BACKEND`、`FRONTEND`、`gen_logviewer_key.py` 调用一律改用 `%SCRIPT_DIR%`（不依赖 cwd、不受 shift 影响）；文件顶部补维护规则第 5 条，回归测试改为断言行为并新增两条（`shift` 之后不得出现 `%~dp0`、路径必须走 `SCRIPT_DIR`）。
> **本轮未覆盖**：① 前端 16 个 Node 用例文件本轮**未重跑**（`git diff --stat 7659d04 HEAD -- frontend/` 为空，本轮改动全部落在 `backend/` 之外或 `tests/`、`scripts/`、`deploy/`，未触碰任何前端源文件）；② 真实 systemd / nginx / SSH 远端路径未在真机验证（本机无 systemd，且不应真连生产机），相关结论基于 unit 文件静态解析、全仓库消费方扫描与真实 bash 下的函数级实测。
> **第六批（建包/示例层）已完成**：Z-14（运输合同补 `trips`/`carbon_tax_rate` 下界检查，冒烟真正跑到「超重 +10%」分支并同步文档金额）、Z-12 余项（空表段与隐藏工作表/行都给明确提示）、Z-15（数字文本口径回归用例，锁死 Z-03 的修复）、Z-17（建包文档表数量口径统一 + `--sheets` 过滤提示）。
> **第七批（归档导入导出）已完成**：R-11（归档截断写 `truncated/dropped`，导入侧显式提示「归档不完整」）、R-12（非空比赛探针补全消息/财年/节点类型/路径类型/地图连线，并加科技前置关系的间接探针）、R-13（消费者需求按 `(区域,产品类型)` 去重，改数量不再追加出重复需求）、R-14（`plan._DETAIL_LIMIT` 真正生效，明细截断但保留全量 `total`）。
> **第八批（dev 脚本）已完成**：B01 D-01 + D-02（`stop-dev.bat` 删除「按端口无差别强杀」与「裸 PID 强杀」两条路径，改为 `dev.py stop` 逐条校验进程身份后才停；端口占用只报告候选）。
> **第九批（部署脚本 X 系列，进行中）已完成**：X-01（`verify-migration.sh` 的 `((PASS++))` 在 `set -e` 下第一项检查后即静默中止且退出码 0）、X-09（同一脚本改为真正打开数据库校验 schema，并补日志查看器 HTTP 健康检查）、X-02（`migrate-server.sh` 的 `BACKUP_DIR` 改 `mktemp -d` + EXIT trap 清理，`.env` 传输强制 600）、X-03（`--install-dir` 白名单校验 + `SSH_OPTS` 改数组 + 远端 root 命令逐参数 `printf %q` 转义）、X-04（恢复阶段去掉 `cp -a … || true`，缺/空/非 SQLite 即中止，收尾按事实报告）、X-05（`quick-sync.sh` 改用 `VACUUM INTO` 一致性快照并双向校验）、X-06（NodeSource 改 apt-keyring + `signed-by`，不再 `curl | bash`）、X-07（`git pull` 失败默认中止 + `--allow-stale-code` + HEAD 核对）、X-08（新增 `scripts/lib/deploy-common.sh`，日志查看器端口在 URL/ufw/nginx 三处统一）、X-11（`.env` 每个键保持唯一一行，去掉 `sed …|&,…|` 追加写法）、X-12（`gen_logviewer_key.py` 的 GBK/BOM 容错与重复键折叠）、X-13（两个 systemd 单元运行/日志目录改 0750 且不再共用，`UMask=0027`，日志查看器 `ReadWritePaths` 收缩到自己的目录）、X-14（`gipfel-logviewer-diag.sh` 密钥掩码、curl 补超时、端口读 `LOG_VIEWER_PORT`、`sudo -n` 失败显式降级提示、安装目录可参数化）、X-15（`tests/deploy_public_ip_test.sh` 用 `BASH_SOURCE` 定位仓库根、环境错误用退出码 2、删掉对已废弃交互式 read 分支的镜像断言、改用生产 `log_viewer_port`；改前从 tests/ 目录跑是 `PASS=8 FAIL=14 exit=1`，改后三种 cwd 均 `PASS=36 FAIL=0`）、X-16（`tests/` 顶层 12 个"只打印、恒退出 0"的伪测试改名 `explore_*` 并加"非测试脚本"声明，含生产算法副本的 4 个额外标注漂移风险；剥掉横幅后与 HEAD 逐字节等价）、X-17（两个连库脚本改用 `tests/_bootstrap.py` + Django 一次性测试库，导入零副作用，去掉静默 `[SKIP]` 并改用真实函数名 `_parse_number_raw`，含 M-2）、X-18（`bootstrap-dev.bat` 的重入标记从全局环境变量改为专用参数，补 `--no-keep-open` 同步路径与 `exit /b`；改前第二次运行会跳过保窗守卫并被裸 `exit 1` 杀掉调用方）、X-19（`dev.py` 新增同步前置检查 `--check-only`，`start-dev.bat` 在 `start` 之前跑它并按退出码 `goto :fail`；改前 `start` 的启动结果被丢弃、恒返回 0）、X-20（`scripts/lib/deploy-common.sh` 新增 `absolutize_dir`，`quick-sync.sh` 把 `INSTALL_DIR` 规范成绝对路径并在 push 前校验源目录与"推错源"；改前相对路径按 `$PWD` 解析且源目录不存在时也报"同步完成"）、X-21（`migrate-server.sh` 的 `usage()` 接受状态码——`--help` 0 / 参数错误 2 / 必填缺失 1；`--dry-run` 不再发起真实 SSH（新增 `check_remote_conn`、`check_remote_command` 与 pull 的存在性探测都走 dry-run 分支）且不再残留临时备份目录；补 `--ssh-port` 1-65535 校验）、X-22（部署脚本默认不再把 `SEED_ADMIN_PASSWORD` 明文写进 stdout——新增 `--print-seed-password` 且仅在 TTY 下生效，非终端明确告警；`[DIAG]` 不再枚举密钥名）、X-23（`gipfel.service` 移除**没有任何消费方**的 daphne Unix socket——daphne 不校验对端 UID，同机任意用户可绕过 nginx 直连并自造 `X-Real-IP`；X-13 的 0750/UMask 收权保留为纵深防御）、X-24（`deploy-linux.sh` 把"降级继续"的分支折算成退出码：新增 `DEPLOY_PROBLEMS`/`problem()` 与 `--allow-partial`，nginx reload/start 失败、非 active、80 端口仍是默认页、`gipfel` 未运行全部入账，并在 reload 前做 `${LV_PORT}` 占用预检、reload 后做日志查看器与 `/api/health` 两个真实探针）、X-25（SSH 选项数组化；`quick-sync.sh` 支持 `--ssh-port`/`--ssh-key`/`--ssh-known-hosts` 并补 `-o BatchMode=yes`，带空格的私钥路径不再被分词；不再无条件 `accept-new`；`--ssh-port` 范围校验）、X-26（镜像建议改为**按仓库**生效并补信任风险与撤销命令，回滚章节补全"数据 + 代码"两侧共 6 步）、X-10（新增 `snapshot_sqlite_consistent` 用 `VACUUM INTO` 取代 `cp -a` 活库做回滚副本——实测裸 `cp` 的副本 `no such table`，而快照 `integrity_check=ok`；新增 `print_rollback_hint` 与两个脚本的 EXIT trap，`migrate` 后失败即打印确切回滚命令）。

**第一批（数据正确性与跨租户，应立刻修）**
1. D-01 金额精度：改用 TEXT 亲和字段或对超 15 位有效数字写入直接报错；同步评估切 PostgreSQL。
2. D-02 合同参与方公司归属校验（写入前后双向校验比赛域）。
3. D-05 / D-06 两处前端"保存即清空"（地点价、合同效果）——建议同时加"读入条数 vs 写出条数"保护。
4. D-03 撮合循环加量化后判定与迭代上限。
5. D-07 / D-08 序列化层类型与有限性校验（`companyId` 必须 int、Float 必须有限）。
6. D-09 builder 槽位重登记 + build 自检 `inputSchema` 覆盖全部 `entityRef`。

**第二批（会话与权限）**
7. 认证层补 `is_active` 校验；管理动作（禁用、重置密码、改角色）统一递增 `token_version` 并断开该用户 socket。
8. 顶号改为 `sio.disconnect` 所有旧 sid（而非只 emit）。
9. 限流键统一 strip 后的用户名、支持表单体、对 `_locks` 加上限与 LRU。
10. 修 `bindFieldId` 的 `high` 校验；为 `stock:manage`/合同执行补比赛域校验。
11. 决策并实现"扩展集授权"或删除该设计（I-13），并把 `permissions=null` 的语义与注释对齐（I-19）。
12. 审计落库改用 `dumps_json_safe`，并覆盖 `.update()/bulk_update` 路径。

**第三批（功能与体验）**
13. 列表分页：修 `reconstruct()` 默认 50 的静默截断（或在各视图显式传 `pageSize` 并给分页器）。
14. F-01 blob 拦截器、F-02 转义、F-06 登出清 memo、F-08 时区、M-01 控件包登录后重载。
15. W-02 载具路径类型字段名对齐；W-03/V-05/W-08/V-09 切比赛的状态与请求竞态。
16. T-01 预计金额 `slice(0,-0)`、T-02 下单防重复。

**第四批（分支独有工具链与部署）**
17. B02/B03/B06/B08 的导入幂等、事务、校验与水位线设计（尤其 CW-01/CW-02 的"重复记账/永久漏账"）。
18. X-02/X-03 部署脚本密钥推送与引号注入；X-04 恢复路径的 `||true`；X-01 的 `((PASS++))` 中止。

---

## 附录 A：分支 → 提交 → 文件归因

| 提交 | 分支 | 新增/修改 | 审计单元 |
| --- | --- | --- | --- |
| （基线 97e117e 及之前） | master | 全仓既有代码 | U01–U14 |
| 602eab1 | origin/prep-export-import（合入 bugfix/feature_wuhaoye） | A: `apps/preparation/{__init__,apps,archive,checklist,plan,urls,views}.py`、2 个前端弹窗；M: `backend/settings.py`、`backend/urls.py`、`frontend/src/api/index.ts`、`types/api.ts`、`SettingsView.vue` | B02 |
| 3dfc731 | bugfix / feature_wuhaoye（**树相同**） | A: `scripts/dev.py`、`scripts/stop-dev.bat`；M: `scripts/start-dev.bat` + 4 份文档 | B01 |
| 73e8e46 | feature/contract-type-code | A: `preparation/builder/**`、`build_competition.py`、`tests/test_builder.py`、`examples/competitions/demo_competition.py`、2 份文档 | B03 |
| 408de7e | 同上 | A: `contracts/builder/**`（8 文件）、`build_contract_types.py`、3 份测试、3 份 examples、2 份文档 | B04、B05 |
| 3aed64e/03dd8f1/7fec2bd/3011ef8/427b261/a6feab9 | test | A: `examples/excel/**`（5 py + 3 xlsx）、`examples/competitions/auto_chain_*`、`examples/contracts/{auto_chain,mini}_*`、3 份文档 | B06 |
| 8b9dea0（+622a748/0d38192） | feature/contract-watcher | A: `contract_watcher/**`（4 py + 样例/记录/报告） | B08 |

## 附录 B：审计单元与产物

- 单元报告：`code_audit/U01…U14-*.md`、`code_audit/B01…B08-*.md`（每条含 `路径:行号` + 代码摘录 + 触发条件 + 后果 + 修复建议 + 存疑项）
- 全量索引：`code_audit/00-缺陷总索引.md`（366 条，逐条带位置与标题；含"跨单元同源/重复项"聚类表）
- 索引生成器：`code_audit/_build_index.py`（机械抽取，可重跑：`backend\.venv\Scripts\python.exe code_audit\_build_index.py`）
- 独立复核探针（只读，不写业务库）：`code_audit/_probe_engine.py`、`_probe_engine2.py`、`_probe_float.py`、`_probe_perms.py`、`_probe_cw.py`
- 可删除的临时产物：早期子代理留下的 `code_audit/_probe_tmp/`（空目录，含受限 ACL，`Remove-Item` 可能需手工清理）

> 说明：单元报告中的分级为该审计员自评，本报告第 3/4 节按统一口径重排；未逐条实跑复核的条目一律标注【子代理取证】并保留原始行号，便于二次核实。
