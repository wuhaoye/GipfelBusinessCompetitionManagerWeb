Django==5.0.14
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
python-socketio==5.11.3
daphne==4.1.2
bcrypt==4.1.3
python-dotenv==1.0.1
Pillow==12.3.0  # 12.0 起官方支持 Python 3.14（11.x 无 cp314 正式 wheel，Windows 源码编译缺 zlib 必失败）

# ---- C1-a（HTTP/WebSocket 进程分离）新增依赖 ----
# gunicorn：生产用 WSGI 服务器，承载 /api/、/admin/ 等同步视图
#   （deploy/gipfel-wsgi.service：-w 4 -k gthread --threads 8，127.0.0.1:8002）。
#   sys_platform 标记：**只在非 Windows 安装** —— gunicorn 依赖 fork/信号模型，不支持 Windows；
#   Windows 开发机用 runserver（scripts/dev.py 拉起），装上也无用。
gunicorn==23.0.0; sys_platform != "win32"
# redis（**可选增强，不是必需依赖**）：装上它并在 .env 设 REALTIME_BUS=redis + REALTIME_REDIS_URL
#   时，apps/realtime 总线改用 AsyncRedisManager / Redis INCR+ZSET 做跨进程（乃至跨机）广播。
#   缺失时应用会**降级**为回环转发（forward）或进程内（local），实时功能不停摆、启动不受影响；
#   这也是为什么这里不加 marker、允许 Windows 开发机安装后跑 redis 模式的分支测试。
redis==5.0.8
