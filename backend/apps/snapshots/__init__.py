"""快照与回退系统（snapshots）。

能力总览：
- 全量数据快照：把业务库中每一张表的每一行原样落盘（gzip JSONL + sha256），
  可选一并归档 uploads 下的上传文件；比赛维度 / 全系统维度两种作用域。
- 及时回退：按快照把库内数据整表还原（保留主键与时间戳），回退前后各校验一次
  内容校验和，任何不一致都会让整个事务回滚（要么完全回退，要么完全不动）。
- 强制暂停 + 同步：内置全局工作门禁（SystemGate），可强制冻结所有人的写入，
  通过 Socket.IO 向所有在线客户端广播暂停 / 回退中 / 已恢复，并递增 dataVersion
  让所有客户端在回退完成后丢弃本地缓存、整体重载，保证全场数据一致。
"""
