# M1 部署/备份/版本类参照表：OPS 就绪报告 × master@97e117e

## 对照对象

| 项 | 内容 |
| --- | --- |
| 原件（待核报告） | `OPS_READINESS_REPORT.md`（现场运营就绪度评估报告），编写于 **bugfix-merged @ 5cb3468**（比 master 多 357 文件 / +62k 行）。凡引用其行号一律显式标注 **(bugfix-merged)** |
| 目标基线 | **master@97e117e8781e0c02462a24269a165dde08ecef44**（2026-09-10） |
| master 真源 | 快照 `C:\Users\wuhao\AppData\Local\Temp\dsh-Ec2sca\gipfel-master-lf`（LF、逐字节等于 blob，已用 `git hash-object` 与 `git cat-file -s` 校验）；行号交叉用 `git show master:<path>` / `git grep <pat> master`（cwd=工作区） |
| 本域 7 条 | O-05、O-09、O-11、O-15、O-21、O-22、O-26 |
| 方法 | 全程只读：`git ls-tree` / `git grep` / `git show master:<path>` / `read`。未执行任何脚本、未起服务、未做 git 写操作、未使用工作区（bugfix-merged）同名文件充当 master 证据；未把原件的 bugfix-merged 行号或探针数据沿用到 master |
| 分类词表 | **A** 仍存在（机制一致，仅行号/位置不同）· **B** 存在但形态或严重度不同 · **C** master 无法命中（区分「因此没有这个问题」/「能力比 bugfix-merged 更缺」）· **D** 已修复 |

## 总览表

| ID | 原件结论（bugfix-merged） | master 状态 | 一句话判定 |
| --- | --- | --- | --- |
| O-05 | 无定时备份/无赛前快照；文档指向的 `scripts/backup.sh` 不存在；示例热拷贝活库（P0） | **A**（更重） | 全部成立；且 master 连 `snapshot_sqlite_consistent` 都没有，部署脚本自己就是热拷贝活库 |
| O-09 | 前端无 SW/manifest/写队列/`navigator.onLine`（P1） | **A** | 0 命中，机制与严重度一致 |
| O-11 | 版本硬封锁：`VERSION.json ≠ /api/version` → 全客户端封锁；`index.html` 无 no-cache（P1） | **A**（+master 特有差异） | 封锁逻辑逐行同构；但 master 的部署脚本不同步 `VERSION.json`，rsync 模式下封锁退化为恒不触发 |
| O-15 | 生产真机从未验证；关键运行路径从未跑过；无压测（P1） | **A**（更重） | master 连那份「11 项待验证」的验证文档都不存在；唯一的验证脚本自身还会静默中止 |
| O-21 | 无业务数据 Excel/CSV 导出，只有 preparation 归档（P2） | **C**（能力更缺） | master 全仓零导出能力（后端+前端+依赖全 0 命中），连 preparation 归档也没有 |
| O-22 | 准备清单只读、全需超管、无负责人/状态（P2） | **C**（能力更缺） | master 无 preparation 应用，清单功能整体不存在——不是「只读」，是「没有」 |
| O-26 | `index.html` 无 `Cache-Control: no-cache`（P3） | **A** | 同一文件同一区域，SPA `location /` 无任何缓存头 |

---

### O-05 无定时备份 / 无赛前快照；文档指向的 `scripts/backup.sh` 不存在；示例热拷贝活库 → master 状态：**A（仍存在，且更重）**

- 原件结论（bugfix-merged）：生产环境无任何 systemd timer / cron 定时备份，唯一出现「定时备份」的地方是 `docs/MIGRATION.md:500-520` **(bugfix-merged)** 教你手写 `scripts/backup.sh` 再挂 crontab，而该脚本不在仓库里；`docs/OPS.md:103-111` **(bugfix-merged)** §8 只说「停服务→覆盖→重启」。
- master 证据：
  - `docs/MIGRATION.md:496-516`（master@97e117e）——`### 7.3 定期备份建议`，`:502` `cat > /opt/gipfel/scripts/backup.sh << 'EOF'`，脚本体 `:504` `BACKUP_DIR="/opt/gipfel/_backup/$(date +%Y%m%d_%H%M%S)"`、**`:506` `cp -a /opt/gipfel/backend/db.sqlite3 "$BACKUP_DIR/"`**，`:514-515` `# 添加 crontab（每天凌晨 3 点备份）` / `echo "0 3 * * * /opt/gipfel/scripts/backup.sh" | sudo crontab -`。
  - `git ls-tree -r --name-only master -- scripts/` → 仅 8 个文件（bootstrap-dev.bat、deploy-linux.sh、gen_logviewer_key.py、migrate-server.sh、quick-sync.sh、start-dev.bat、update-from-github.sh、verify-migration.sh），**无 `backup.sh`、无 `lib/`**。
  - 无任何调度设施：`git grep -i -E "OnCalendar|systemd\.timer|crontab" master -- docs scripts deploy` 只命中上述 MIGRATION.md 示例三行。
  - `docs/OPS.md:100-108` §8「备份与恢复」——只列需备份的目录（`:104-106`），`:108` 「恢复：停止服务 → 用备份覆盖上述目录 → 重启」，**不提备份由谁在何时产生**；`:108` 把备份来源指向 `deploy/README.md` 的更新部署章节（即「上次部署时的 `_backup/<时间戳>`」）。
  - 部署/升级脚本自身就是热拷贝活库：`scripts/deploy-linux.sh:175` `cp -a "$INSTALL_DIR/backend/db.sqlite3" "$BACKUP_DIR/" 2>/dev/null || true`、`scripts/update-from-github.sh:192` 同款；两脚本全文无 `systemctl stop`（`grep` 确认），而 `deploy/gipfel.service:32` 是 `Restart=always` → 备份必然发生在服务运行中。
- 差异说明：**A，且比 bugfix-merged 更重**。① 「无 backup.sh / 无 timer / OPS §8 不交代备份来源」三项逐条成立，只是行号平移（MIGRATION 500-520 → 496-516，OPS 103-111 → 100-108）。② 原件说「该反模式是本项目自己在 `deploy-linux.sh:321-330` 点名批评并已修复的 X-10 反模式」——这在 master 上**不成立**：master 的 `git grep snapshot_sqlite_consistent master` → 0 命中（该一致性快照函数只存在于 bugfix-merged 的 `scripts/lib/deploy-common.sh:180-181` **(bugfix-merged)**），master 的部署/升级脚本没有任何停服、没有 SQLite `.backup`/`VACUUM INTO`、没有 `-wal`/`-journal` 伴随文件、没有完整性校验，直接 `cp -a` 主库文件。也就是说：master 上「热拷贝活库」不只出现在文档示例里，而是**部署脚本本身的行为**，且不存在任何一致性快照实现可复用。③ 无「每财年/赛前快照」：全仓 `快照` 命中均为代码内的内存快照注释（`company_fields/timer.py:12,207`、`contracts/engine.py:2290` 等），无持久化备份语义；`_backup` 的唯一生产者是 deploy/update/migrate-server 三个脚本的临时副本（`deploy-linux.sh:80`、`update-from-github.sh:189`、`migrate-server.sh:351-356`）。
- 验证命令：`cd <workspace>; git ls-tree -r --name-only master -- scripts/; git show master:docs/MIGRATION.md | Select-Object -Skip 495 -First 21; git grep -n "snapshot_sqlite_consistent" master`（预期：脚本清单 8 项无 backup.sh；第 506 行为 `cp -a` 热拷贝；最后一条无输出）
- 置信度：**已验证**（文件是否存在、是否含 timer、示例内容、脚本是否停服均为直接读码/枚举结果）

### O-09 前端无离线降级（无 SW/manifest/写队列，无 `navigator.onLine`） → master 状态：**A（仍存在）**

- 原件结论（bugfix-merged）：前端 0 命中 `serviceWorker`/`manifest`/`offline`/`navigator.onLine`；IndexedDB 只是读缓存，无写队列；断网时写操作直接失败并 toast。
- master 证据：
  - `git grep -n -i -E "serviceWorker|service-worker|navigator\.onLine|workbox|manifest\.json|offline|writeQueue|写队列" master -- frontend` → **0 命中（exit 1）**（master@97e117e）。
  - `git ls-tree -r --name-only master -- frontend/public` → **空**（无 `manifest.json` / `sw.js` / 任何 public 资源）；`git show master:frontend/package.json | Select-String -Pattern 'pwa|workbox|service-worker'` → 无命中。
  - `frontend/src/api/cache.ts:1` `// 客户端本地持久化缓存（IndexedDB）`、`:12` 「IndexedDB 不可用时……自动降级为『直接走网络』」、`:17` 库名按「服务器身份 realm + 账号」分库 → 仅为读缓存；`git grep -n -i -E "writeQueue|pendingWrites|offlineQueue|retryQueue|队列" master -- frontend/src/api` → **0 命中（exit 1）**。
  - 断网写操作的失败路径：`frontend/src/api/request.ts:21-23` `axios.create({ timeout: 15000 })` + 统一错误提示拦截器（`request.ts:44-45` 注释「将任意错误统一转换为中文提示」）→ 断网时请求要么立即网络错误、要么等满 15s 超时，随后按普通错误 toast，**无「离线，稍后重试」语义、无本地暂存**。
- 差异说明：**A**，机制与严重度与原件一致（缺 SW/PWA/manifest/写队列/在线探测），未发现 master 上有任何补偿机制；`frontend/public/` 为空这一事实比原件描述（「无 manifest」）更彻底，但没有改变后果。P1 定级在 master 上同样成立：断网即丧失全部写操作，页面旧数据仍在但无法提交，且没有明确离线提示。
- 验证命令：`git grep -n -i -E "serviceWorker|navigator\.onLine|workbox|offline" master -- frontend; git ls-tree -r --name-only master -- frontend/public`（预期：第一条无输出，第二条无输出）
- 置信度：**已验证**（负向搜索 + 目录枚举）

### O-11 版本硬封锁：`VERSION.json ≠ /api/version` → 全客户端封锁；`index.html` 无显式 no-cache → master 状态：**A（仍存在；另有 master 特有的部署同步差异）**

- 原件结论（bugfix-merged）：`frontend/src/stores/version.ts:30-38` **(bugfix-merged)** 严格比对即硬封锁、`frontend/src/App.vue:20-27` **(bugfix-merged)** 启动即校验、`deploy/nginx-gipfel.conf:154-156` **(bugfix-merged)** SPA 无 no-cache → 任何后端升级/热修都会让 `VERSION.json ≠ /api/version`，全部客户端被封锁。
- master 证据：
  - 封锁判定：`frontend/src/stores/version.ts:31-32`（master）—— `const blocked = !!serverVersion.value && serverVersion.value !== client;` / `versionBlocked.value = blocked;`（严格不等，无兼容窗口）；`:33-37` 封锁时 `showDialog=true` 并 `startPolling()`，`:46` 轮询间隔 `5 * 60 * 1000`；`:52-54` 比对请求 `catch {}` → 服务端不可达时**不封锁**。
  - 强制阻断：`frontend/src/api/request.ts:27-33`—— `if (versionBlocked.value && !config.bypassVersionBlock) { return Promise.reject(new Error("客户端版本与服务端不一致，已禁用全部请求，请联系管理员获取最新版本")) }`；`frontend/src/realtime/socket.ts:28` 同样对 Socket.IO 生效 → 封锁期间**任何 API 与实时通道都不可用**。
  - 触发时机：`frontend/src/App.vue:20-27`—— `await versionStore.checkVersion()` 于挂载时执行，之后每 5 分钟复检 → 运行中的会话最迟 5 分钟被封锁。
  - 提示与出路：`frontend/src/components/VersionUpdateDialog.vue:13-29`—— `title="版本不一致"`、`:show-close="false"`、`:close-on-click-modal="false"`、`:close-on-press-escape="false"`（不可关闭），文案 `:23` 「客户端版本与服务端不一致，已禁用全部功能。」、`:29` 「系统每 5 分钟自动复核，或联系管理员获取最新版本后刷新页面。」
  - 版本真源：客户端 `frontend/src/data/version.ts:1-3`（`__APP_VERSION__`，由 `frontend/vite.config.ts:7-14` 在**构建期**读 `resolve(__dirname,"../VERSION.json")` 注入，异常回退 `"0.0.0"`）；服务端 `backend/apps/auth/views.py:32-44`（`_read_version()` 读 `Path(__file__)...parent.parent.parent.parent / "VERSION.json"`，异常回退 `"0.0.0"`，`:44` `VERSION = _read_version()`）。两侧都指同一份根 `VERSION.json`（当前值 `VERSION.json:2` `"version": "1.4.0"`）。
  - **master 特有差异**：`deploy-linux.sh:184-190` 只 rsync `backend/`、`frontend/`、`deploy/`；`update-from-github.sh:157-165` 只 rsync `backend/`、`frontend/`、`deploy/`、`scripts/` → **根目录 `VERSION.json` 从不进入 `$INSTALL_DIR`**（`git grep -n "VERSION.json" master -- scripts deploy` → 0 命中）。在 README 推荐的模式 B（clone 源码树 → rsync 到 `/opt/gipfel`）下，`/opt/gipfel/VERSION.json` 不存在：服务端读不到 → `"0.0.0"`，构建时 vite 也读不到 → 客户端 `"0.0.0"`，两侧恒等 → **硬封锁永不触发**（`/api/version` 也恒报 0.0.0）。在模式 A（`/opt/gipfel` 本身是 git clone）下 `git pull` 会把 `VERSION.json` 更新进来，封锁按设计生效：一次 pull 抬升版本后，已打开的旧页面（浏览器里仍是旧 bundle）最迟 5 分钟被硬封锁，全功能不可用，唯有加载到新 bundle 才解锁——与原件描述的场景一致。
- 差异说明：**A**。封锁机制（严格比对 + 网关级拒绝 + 5 分钟复检 + 不可关闭对话框）在 master 上逐条存在，文件名与行段几乎重合（`version.ts` 30-38 → 31-32 区域；`App.vue` 20-27 → 20-27；`nginx-gipfel.conf` 154-156 → 154-155），严重度一致（P1）。需要修正原件措辞的一点：**并非「任何后端升级」都会造成不一致**——两侧读同一份 `VERSION.json`，只有「该文件按新版本重建/更新（客户端 bundle 已换）而浏览器仍跑旧 bundle」才产生不一致；master 特有的放大器是「`VERSION.json` 不随部署同步」：模式 B 下封锁逻辑退化成永不生效的安全网（版本号也永远停在 0.0.0），模式 A 下则真实封锁。这属于 master 相对 bugfix-merged 的另一处运维缺口，已在本条内记录，未另立 O-XX。
- 验证命令：`git show master:frontend/src/stores/version.ts | Select-Object -Skip 28 -First 8; git show master:backend/apps/auth/views.py | Select-Object -Skip 31 -First 13; git grep -n "VERSION.json" master -- scripts deploy`（预期：第 3 条无输出，即部署脚本不同步版本文件）
- 置信度：**已验证**（逻辑、阻断点、版本来源、rsync 集合均为直接读码）；「模式 B 下 `/opt/gipfel/VERSION.json` 不存在」为**推断**（由 rsync 集合与脚本未提该文件推出，未在真机部署验证）

### O-15 生产真机从未验证；关键运行路径从未跑过；无压测记录 → master 状态：**A（仍存在，且更重）**

- 原件结论（bugfix-merged）：文档自述 11 项待验证 (`WSL与生产环境验证结果记录.md:118-142` **(bugfix-merged)**)；两条关键运行期路径从未落库（§5 探针：`timer_enabled` 字段=0、`contract_field_effects`=0）；无压测记录。
- master 证据：
  - master 的 `docs/` 只有 4 个文件：`git ls-tree --name-only master -- docs/` → `CUSTOM_WIDGET_GUIDE.md`、`MIGRATION.md`、`OPS.md`、`Vue-Django迁移设计.md`。**`WSL与生产环境验证操作手册.md` 与 `WSL与生产环境验证结果记录.md` 两个文件在 master 上都不存在**（`git ls-tree -r --name-only master | Select-String -Pattern 'WSL'` → 0 命中）。
  - 全仓 `docs`/`README` 无任何验证状态表述：`git grep -n -E "11 项|待验证|生产真机|压测" master -- docs README.md` → **0 命中**。
  - 无压测/负载工具：`git ls-tree -r --name-only master | Select-String -Pattern 'perf|load|locust|k6|benchmark'` 仅命 `frontend/src/composables/useCompetitionReload.ts`（文件名含 `load`，与压测无关）；`tests/` 16 个文件全部是 `test_*.py`/`.js` 与 3 个 shell 冒烟/诊断脚本，无并发或长稳测试。
  - master 侧唯一的现成验证工具本身有缺陷：`scripts/verify-migration.sh:10` `set -euo pipefail`，`:22` `check_pass() { echo -e "  ${GREEN}✓${NC} $*"; ((PASS++)); }`——`PASS` 初值 0 时 `((PASS++))` 的退出状态为 1，在 `set -e` 下函数返回 1 即中止；而 `:40` 的 `check_pass "数据库文件存在 (...)"` 是脚本的第一项检查 → 按 bash 语义（`((expr))` 在表达式值为 0 时返回 1，且函数在 `if` 分支体内受 errexit 约束），运行它只会打印第一行 ✓ 然后退出 1，后续 `:49/:56/:62/:72-100` 的 .env、uploads、frontend-dist、三个 systemd 服务、`/api/health` 检查全部不执行。**此行为为静态推断**（与既有审计 U14 X-01 对同一写法的判定一致，本机沙箱未执行 Unix shell 脚本；复核命令：`bash -c 'set -e; P=0; f(){ ((P++)); }; f; echo reached'` — 预期不打印 `reached`）。
  - 原件 §5 的探针数据（`timer_enabled` 字段=0、`contract_field_effects`=0、`audit_log=7118`、`fiscal_years` 两条 ACTIVE 等）取自 **bugfix-merged 的开发库 `backend/db.sqlite3`**，**不可迁移到 master**：master 快照内无任何 db 文件，工作区 `backend/db.sqlite3` 属 bugfix-merged，不能作为 master 证据。代码侧只能确认这两条路径**可达**：`backend/apps/industry_types/models.py:66` `timer_enabled = models.BooleanField(default=False)`（消费点 `backend/apps/company_fields/timer.py:172`）、`backend/apps/contracts/models.py:111` `db_table = "contract_field_effects"`。
- 差异说明：**A，且比 bugfix-merged 更重**。原件说「生产真机从未验证（文档自述 11 项待验证）」——master 上连这份**操作手册/结果记录都不存在**，即：既没有验证结果，也没有待验证清单可依，验证这件事在 master 上完全没有载体（工作区/分支更全的文档不能算 master 的证据）。「无压测记录」在 master 上同样是 0 证据（无脚本、无文档）。「关键运行路径从未跑过」这一条的**数据库证据部分在 master 上无法判定**（无 master 状态的库可查），但可以确认：master 的仓库内没有任何运行期结果记录，且唯一可跑的服务端验证脚本会静默中止，因此「master 是否真机验证过」的答案没有任何正向证据。运营后果与原件一致：开赛前的验证完全依赖运维临场手工执行，没有可复用的清单、没有历史结果可比对。
- 验证命令：`git ls-tree --name-only master -- docs/; git grep -n -E "11 项|待验证|生产真机|压测" master -- docs README.md; git show master:scripts/verify-migration.sh | Select-Object -Skip 19 -First 5`（预期：docs 仅 4 个文件；第二条无输出；第三条可见 `((PASS++))`）
- 置信度：**部分已验证**——「无验证文档 / 无压测记录 / verify-migration.sh 缺陷」为已验证；「关键运行路径从未落库」的 DB 探针部分在 master 上**无法判定**（缺 master 状态数据库，且不得沿用 bugfix-merged 数据）。

### O-21 无业务数据 Excel/CSV 导出（只有 preparation 归档 markdown/JSON） → master 状态：**C（master 无法命中；能力比 bugfix-merged 更缺）**

- 原件结论（bugfix-merged）：无任何业务数据的 Excel/CSV 导出，唯一出口是 preparation 归档的 markdown/JSON 且需超管（证据 `preparation/views.py:74-78,167-214` **(bugfix-merged)**）→ 闭幕式颁奖数据、赛后归档要手抄。
- master 证据：
  - 应用不存在：`git grep -n -i "preparation" master` → **0 命中（exit 1）**；`git ls-tree --name-only master -- backend/apps/` → 27 个应用中无 `preparation`（announcements/audit/auth/common/companies/company_fields/competitions/consumer_demands/contracts/files/fuels/industry_types/infrastructures/maps/materials/messages/parts/production_lines/products/realtime/regions/stock/tech_tree/users/vehicles/warehouses/widget_packages）。
  - 后端零导出能力：`git grep -n -i -E "openpyxl|xlsxwriter|pyexcel|pandas|csv\.writer|Content-Disposition|StreamingHttpResponse|FileResponse|import csv" master -- backend` → **0 命中（exit 1）**；补充 `git grep -n "content_type=" master -- backend/apps` → **0 命中**；`git show master:backend/requirements.txt` 无 openpyxl/xlsx/pandas/reportlab 等任何导出/报表库。
  - 前端零导出能力：`git grep -n -i -E "xlsx|exceljs|file-saver|jsPDF|html2canvas|window\.print|createObjectURL|download=" master -- frontend` → **0 命中**；放宽到 `git grep -n -i "download" master -- frontend/src` → 仍 **0 命中（exit 1）**（连 `<a download>` 式的文件下载都没有）。
- 差异说明：**C，属于「能力比 bugfix-merged 更缺」**（不是「因此没有这个问题」）。原件描述的症状在 master 上不但存在，而且少了一个出口：bugfix-merged 至少还有 preparation 的 markdown/JSON 归档（超管可用），master 是**全仓零导出**——既无 Excel/CSV，也无 PDF/打印、无文件流响应、无前端 Blob 下载。运营含义：闭幕式颁奖与赛后归档在 master 上只能靠人工抄录或直接读 SQLite，原件给该条定 P2 在 master 上偏轻（缺的是「唯一可用出口」而非「更好的出口」），但严重度词表由原件口径决定，此处只标注「更缺」。
- 验证命令：`git grep -n -i -E "openpyxl|xlsxwriter|Content-Disposition|FileResponse" master -- backend; git grep -n -i -E "xlsx|exceljs|download" master -- frontend`（预期：两条均无输出）
- 置信度：**已验证**（6 组负向搜索 + requirements.txt + 应用目录枚举）

### O-22 比赛准备清单只读、全需超管、无负责人/状态 → master 状态：**C（master 无法命中；能力比 bugfix-merged 更缺）**

- 原件结论（bugfix-merged）：准备清单只读且全部要超管权限，无负责人/状态/时间点，无法把 39 项分派给多人并行（证据 `preparation/views.py:150,203,233`、`preparation/checklist.py:16-41` **(bugfix-merged)**）。
- master 证据：
  - `git grep -n -i "preparation" master` → **0 命中（exit 1）**；`backend/apps/` 列表（同 O-21）无 preparation。
  - 清单概念整体不存在：`git grep -n -i -E "checklist|准备清单|Checklist" master` → **0 命中（exit 1）**。
  - INSTALLED_APPS 侧同样无对应项（应用目录枚举已足够；master 的 `backend/apps/` 27 项中无任何 preparation/checklist 模块）。
- 差异说明：**C，属于「能力比 bugfix-merged 更缺」**（不是「因此没有这个问题」，也不是「已修复」）。原件针对的是 bugfix-merged 里那份**存在但受限**的清单（只读、仅超管、无负责人/状态）；master 上这个功能整体不存在，因此「只读 / 全需超管 / 无负责人字段」这些**具体缺陷无法在 master 上命中**，但它们的**根因需求**（把开赛前的 N 项准备分派给多人并行、可勾选进度）在 master 上同样无任何实现——运营侧只能靠外部表格。按任务口径：master 状态为「能力整体缺失」，比 bugfix-merged 更缺，不构成本条的修复。
- 验证命令：`git grep -n -i -E "preparation|checklist" master; git ls-tree --name-only master -- backend/apps/`（预期：第一条无输出；第二条列出 27 个应用，无 preparation）
- 置信度：**已验证**（负向搜索 + 应用枚举）

### O-26 `index.html` 无 `Cache-Control: no-cache` → master 状态：**A（仍存在）**

- 原件结论（bugfix-merged）：`deploy/nginx-gipfel.conf:154-156` **(bugfix-merged)** 的 SPA 兜底未给 `index.html` 显式 `no-cache`，配合版本硬封锁带来「刷了还是旧版本」的排查成本（P3）。
- master 证据：
  - `deploy/nginx-gipfel.conf:37` `root __INSTALL_DIR__/frontend-dist;`、`:38` `index index.html;`、`:151` 注释 `# SPA 兜底：前端 router 的 history 模式，所有非 API/非静态路由返回 index.html`、`:154-155` `location / { try_files $uri $uri/ /index.html; }` —— **该 location 内无任何 `Cache-Control`/`expires`/`add_header`**。
  - 主 server 块里出现缓存头的只有两个 location：`:82-83`（`/uploads/`，`expires 7d` + `Cache-Control "public, max-age=604800, immutable"`）与 `:121-122`（`/static/` → `alias __INSTALL_DIR__/backend/staticfiles/`，`expires 30d` + immutable）；`:206/:262` 属日志查看器块。前端 `index.html` 与哈希化 `/assets/*` 都落在无缓存头的 `location /` 下 → nginx 只发 `Last-Modified`/`ETag`，浏览器按启发式新鲜度缓存 `index.html`。
  - 与 O-11 的联动点：`frontend/src/stores/version.ts:33-34` 封锁后弹出不可关闭对话框、`frontend/src/components/VersionUpdateDialog.vue:29` 的唯一出路是「刷新页面」；而 `index.html` 无显式 no-cache 时，普通导航（非强制刷新）可能直接从启发式缓存取旧 `index.html`，继续引用旧 `assets/*` → 用户停留在旧 bundle，表现为「刷了还是被封锁」，需 Ctrl+Shift+R。
- 差异说明：**A**，同一文件、同一区域（原件 154-156 → master 154-155），机制与 P3 严重度一致。master 侧另可注意的反向事实：因为哈希化 `/assets/*` 也在无缓存头的 `location /` 下，master 并没有给静态资源加 `immutable` 长缓存——这降低了陈旧资源风险，但也让每次加载都要回源校验（属性能话题，不改变本条结论）。
- 验证命令：`git show master:deploy/nginx-gipfel.conf | Select-Object -Skip 150 -First 6; git show master:deploy/nginx-gipfel.conf | Select-String -Pattern "Cache-Control|expires"`（预期：SPA location 内无缓存头；`Cache-Control` 仅出现在 /uploads/、/static/ 与日志查看器块及注释掉的 443 段）
- 置信度：**已验证**（配置原文 + 缓存头枚举）

---

## 汇总统计

| 分类 | 条数 | 条目 |
| --- | --- | --- |
| **A 仍存在** | **5** | O-05（更重）、O-09、O-11（另有 master 特有差异）、O-15（更重）、O-26 |
| **B 存在但形态/严重度不同** | **0** | —— （O-11 的 master 特有差异未单列为 B：封锁机制与严重度与原件一致，仅叠加了一处部署同步缺口） |
| **C master 无法命中** | **2** | O-21（能力更缺）、O-22（能力更缺） |
| **D 已修复** | **0** | —— |
| 合计 | 7 | |

结论：7 条中 5 条在 master 上原样成立（其中 2 条比原件描述更重），2 条因 master 缺少相应应用/文档而无法命中，且都属于「能力比 bugfix-merged 更缺」而非「已解决」——**没有任何一条在 master 上被修复**。

## 已核对无问题或无法判定

1. **原件引用的 bugfix-merged 文件在 master 的存在性**（逐条核对，避免把分支差异当成缺陷差异）：master **不存在** `backend/apps/preparation/`、`contract_watcher/`、`scripts/lib/`、`backend/examples/`、`backend/apps/contracts/builder/`、`backend/apps/contracts/management/`、`scripts/backup.sh`、`tests/verify-migration.sh`（master 的同名脚本在 `scripts/verify-migration.sh`）、`docs/WSL与生产环境验证操作手册.md`、`docs/WSL与生产环境验证结果记录.md`、`code_audit/`。其中**与 Lead 前提不一致的一处**：`WSL与生产环境验证操作手册.md` 在 master 上**同样不存在**（`docs/` 仅 CUSTOM_WIDGET_GUIDE.md、MIGRATION.md、OPS.md、Vue-Django迁移设计.md），O-15 因此按「更重」而非「原件引用文件在 master 存在」处理。
2. **原件 §5 的探针数据全部无法沿用到 master**（`timer_enabled` 字段=0、`contract_field_effects`=0、audit_log=7118、competitions/users/companies 行数、`fiscal_years` 两条 ACTIVE）：那些是 bugfix-merged 开发库的运行时数据；master 快照不含 db，工作区 `backend/db.sqlite3` 属 bugfix-merged 分支，不得充当 master 证据。master 侧只能确认代码可达（`industry_types/models.py:66`、`company_fields/timer.py:172`、`contracts/models.py:111`）。**判定：master 上无法判定**。
3. **`VERSION.json` 的两侧一致性（正常路径）无问题**：客户端与服务端读同一份根 `VERSION.json`（`frontend/vite.config.ts:7-14` / `backend/apps/auth/views.py:37`），当前值 `1.4.0`（`VERSION.json:2`，`frontend/package.json` 的 `version` 同为 `1.4.0`）→ 只要前端在**同一次部署中重建**，两侧必然一致，不会凭空产生封锁。缺陷只出现在「客户端 bundle 陈旧」这一侧（见 O-11）。
4. **封锁的可解锁性没有死锁**：`frontend/src/stores/version.ts:16-21` 的校验请求走 `axios.get(getApiBaseUrl()+"/api/version", { headers: {} })` 且 `request.ts:29` 用 `config.bypassVersionBlock` 放行，`:52-54` 服务端不可达时不封锁 → 版本恢复一致后能自动解锁（`version.ts:35-36` `stopPolling()`）。即 O-11 是「硬封锁但有出路」，不是永久砖化。
5. **master 的 `.sh` 行尾为 LF，与本次判定无关的伪差异已排除**：`scripts/deploy-linux.sh` 快照 36703 B / CR=0 / LF=652，等于 blob 大小；`verify-migration.sh` 摘录里的 `✓/✗` 等中文符号不涉及 CRLF 问题。凡 CRLF 类推断均不成立。
6. **`scripts/verify-migration.sh` 的 `((PASS++))` 缺陷已单独留证，未计入 O-15 的定级**：`scripts/verify-migration.sh:10` + `:22` + `:40`。它是独立于本 7 条的 master 缺陷（既有审计 U14 X-01 描述的是另一个 checkout 的 `tests/verify-migration.sh:22`，位置不同），建议由 Lead 决定是否另立条目——本报告只把它作为 O-15「master 侧验证能力」的旁证。
7. **无法判定项汇总**：① O-15 的「关键运行路径从未落库」数据库证据（无 master 状态库）；② O-11「模式 B 下 `/opt/gipfel/VERSION.json` 缺失」为静态推断（由 rsync 集合推出，未在真机部署验证）；③ 原件 O-05/O-11/O-15 中凡属「现场严重度/阻塞时长」的判断（如断电丢两天数据）均为原件基于 bugfix-merged 现场条件的推断，本报告不复核、不背书。
