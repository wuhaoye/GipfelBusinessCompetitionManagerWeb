# M4 业务能力缺失类对照（原件 bugfix-merged@5cb3468 → 目标 master@97e117e）

| 项 | 内容 |
| --- | --- |
| 待核原件 | `C:\Users\wuhao\.dsh\attachments\v1\files\80\803e2e7c2c566ad9271087a90eb7a644fcd04e9a6a1248b5ed9e3202907cfc7f\OPS_READINESS_REPORT.md`（标题自述审计对象 `bugfix-merged @ 5cb3468`，28 条缺陷；本次只核其中 7 条） |
| 对照目标 | `master@97e117e8781e0c02462a24269a165dde08ecef44`（`fix(frontend): 修复合约图自动布局分层错乱问题`） |
| 主真源 | `C:\Users\wuhao\AppData\Local\Temp\dsh-Ec2sca\gipfel-master-lf`（LF 快照，逐字节等于 master blob：`git cat-file -s` 与磁盘字节数一致、CR=0）；交叉验证用 `git show master:<path>` |
| 方法 | 全程**只读**：所有负向结论统一用 `git grep <pat> master`（覆盖 master 全部 445 个跟踪文件，而非只搜快照子目录），并用 `git grep -l` 列出命中文件以排除 `backend/logviewer/static/vendor/*` 第三方 bundle 造成的假命中；正向结论用 `git show master:<path>` 打印带行号原文。未改动任何代码，唯一写入 = 本报告。凡引用原件行号一律标注「(bugfix-merged)」 |
| 前提校正 | Lead 提供的 `master 的 contracts/views.py 只有 738 行`**不成立**：master 实际 **869 行**（`(git show master:backend/apps/contracts/views.py).Count` = 869）。本报告所有行号均以 master 实测为准 |

## 总览表

| 编号 | 原件级别 | 原件标题（摘要） | master 状态 | 一句话差异 |
| --- | --- | --- | --- | --- |
| O-03 | P0 | 无财务报表/对账/PDF，只有离散字段值 | **A 仍存在** | master 同样 0 命中；且**能力更缺**——原件里唯一的外部账套（`contract_watcher/`）与唯一归档导出（`preparation/`）在 master 都不存在 |
| O-07 | P1 | 无倒计时/时间盒/服务端计时/大屏/排行榜/定向广播房间 | **A 仍存在** | 全部子项一一对应；唯一命中在第三方 bundle（Element Plus 自带 ElCountdown）与图标包，应用代码 0 命中 |
| O-14 | P1 | 纸质合同与系统记录脱节：无打印/PDF/导出、无一式 N 份、编号可越范围填写 | **A 仍存在** | 三个子项全中；行号需重定位（越范围放行在 master `contracts/views.py:844-859`，调用点 `:417`） |
| O-16 | P2 | 财年编号两套口径（界面第 0 财年 vs Excel/建包 2026） | **C 无法命中** | 第二套口径不存在 → **因此没有"两套口径冲突"这个问题**；但 master 连建包能力都没有（更缺），且唯一那套口径仍是 0 基 |
| O-18 | P2 | 断线重连对账无并发上限/无单飞、产业字段按公司逐个请求 | **A 仍存在** | 机制与原件同源（无上限 `Promise.all` + 逐公司 `/company-fields/{cid}`），master 另有若干降噪缓解（无 token 跳过、silent、防抖） |
| O-20 | P2 | `contract_watcher` 现场门槛高（Windows+Excel+xlwings…） | **C 无法命中** | master **根本没有这条外部记账兜底路径**（`contract_watcher`/`xlwings`/`记账` 全仓 0 命中）→ **能力更缺**，叠加 O-03 后果更重 |
| O-23 | P2 | 消息中心可广播但需 `message:manage`（界面授不出去）、无已读确认、无定时重播、上限 500 | **A 仍存在** | 五个子项全中，含"界面无权限勾选、扩展权限授不出" |

---

### O-03 无任何财务报表 / 对账 / PDF 导出能力 → master 状态：A（仍存在，且能力更缺）

- 原件结论（bugfix-merged）：「系统不产生任何会计意义上的报表，一行都没有」；负向搜索 `报表/对账/资产负债表/利润表/现金流量/试算平衡/ledger/balance_sheet/income_statement/trial_balance` 在 `backend/apps/` 零命中，命中全部落在 `contract_watcher/`（外部程序）与测试文件；PDF 侧唯一 `Content-Disposition` 在 `preparation/views.py:116-122`（(bugfix-merged) 行号），导出格式只有 markdown/json。
- master 证据（正向，值模型仍是"裸字符串"）：`backend/apps/companies/models.py:55`（master@97e117e）—— `    value = models.TextField(default="")`；`backend/apps/company_fields/models.py` 全文仅 3 行 docstring：`"""公司产业字段值模型定义在 apps.companies（CompanyFieldValue）。…本应用 company_fields 复用该模型，无独立模型…"""`。全仓 **40** 个 `models.Model` 子类（`git grep -n -E "^class [A-Za-z_]+\(.*models\.Model" master -- backend/apps` → 40 行）中没有任何 ledger/account/statement/depreciation/appraisal 模型：逐个为 `Announcement/AuditLog/Company/CompanyFieldValue/Competition/FiscalYear/ConsumerDemand/ContractType/Contract/ContractFieldEffect/Fuel/IndustryType/IndustryField/Infrastructure/Map*/Material/Message*/Part*/Product*/Region/Stock*/Tech*/Vehicle*/Warehouse/WidgetPackage`。
- master 证据（负向，已用 `git grep -l` 排除第三方 bundle 后的空结果）：
  - `git grep -n -i -E "报表|对账|资产负债表|利润表|现金流量|试算平衡|ledger|balance_sheet|income_statement|trial_balance" master` → 仅 6 个文件、且**全部是技术语义**：`frontend/src/api/request.ts:193/630/648/741/747/749/750/845/847`、`frontend/src/api/cache.ts:189/285/293/325`、`frontend/src/realtime/resource-changed.ts:131/162/172`（均指"断线重连后客户端缓存对账"）、`backend/apps/announcements/management/commands/seed_announcements.py:54` 与 `frontend/src/data/announcement.ts:70`（同一句版本公告文案）、`frontend/src/views/stocks/StockManageView.vue:1185`（CSS 注释 `/* 管理报表表格：手机端改为卡片（标签:值竖向排列） */`，指股票管理弹窗表格）。**会计语义 0 命中。**
  - `git grep -n -i -E "reportlab|weasyprint|xhtml2pdf|fpdf|application/pdf" master` → 仅 `deploy/nginx-gipfel.conf:97` 与 `:359`（nginx 静态资源 `types` 里的 PDF MIME 声明，非生成能力）。
  - `git grep -n "Content-Disposition" master` → rc=1（**全仓无任何文件下载**）。
  - `git grep -n -i -E "openpyxl|xlsxwriter|pandas|to_csv|StreamingHttpResponse|csv\.writer" master -- backend` → rc=1（**后端无任何 Excel/CSV 导出**）；`git grep -n -E "导出" master -- "backend/**/*.py"` → 仅 `backend/apps/realtime/gateway.py:15` 与 `backend/backend/settings.py:213` 两处 Python 注释里的"导出（对象/别名）"。
- 差异说明：**「同样没有」+「更彻底没有」**。原件在 bugfix-merged 上至少存在两条系统之外的路径——`contract_watcher/` 的 12 张表账套（含资产负债/利润/现金流量表与平衡校验 `check()`）与 `preparation/views.py` 的 markdown/JSON 归档导出；master 既无 `contract_watcher/`（见 O-20）也无 `backend/apps/preparation/`（`git ls-tree --name-only master:backend/apps` → 27 个应用包 + `__init__.py`，其中无 preparation），连 `Content-Disposition` 都 0 命中。因此 O-03 的现场后果（18:30 无账表可核、第 2 财年贷款无财务报表前置条件）在 master 上**更严重**：连"外部兜底 + 归档导出"都没有。
- 验证命令：`git grep -n -i -E "报表|对账|资产负债表|利润表|现金流量|ledger|balance_sheet|income_statement|reportlab|weasyprint|fpdf" master; git grep -n "Content-Disposition" master; git grep -n -i -E "openpyxl|pandas|to_csv|StreamingHttpResponse" master -- backend`
- 置信度：**已验证**（负向搜索覆盖 master 全仓并已排除 vendor bundle 假命中；正向模型字段已读原文）。"现场后果更重"部分为推断。

### O-07 无倒计时 / 时间盒 UI、无服务端计时、无大屏 / 排行榜、无定向广播房间；`announcements` 只是版本更新公告 → master 状态：A（仍存在）

- 原件结论（bugfix-merged）：前端搜索 `倒计时/countdown/剩余时间/大屏/投屏/排行榜/leaderboard` **0 命中**，唯一周期行为是 20s 心跳与 5min 版本复核；`announcements` 实为软件版本更新公告（`announcements/models.py:8-19`、`seed_announcements.py:9-31` (bugfix-merged) 行号），仅超管可增删改；实时房间只有 `comp-{id}` 与 `user-{id}`（`realtime/gateway.py:156-159、193-208` (bugfix-merged)），无主席团/审计/大屏房间。
- master 证据（负向，含 vendor 排除）：
  - `git grep -l -i "countdown" master` → **仅 1 个文件** `backend/logviewer/static/vendor/element-plus.full.min.js`（第三方库自带 `ElCountdown` 组件与其 i18n 文案）；应用代码 `git grep -n -i "el-countdown\|ElCountdown" master -- "*.vue" "*.ts" "*.py"` → rc=1（**没有任何页面使用倒计时组件**）。
  - `git grep -l -E "大屏|投屏|排行榜|leaderboard|scoreboard" master` → **0 个文件**（rc=1）；`git grep -l -E "时间盒|计时|停表|stopwatch" master` → 仅 `backend/logviewer/static/vendor/icons.min.js`（第三方图标包）。
  - 无服务端调度设施：`git grep -n -i -E "celery|APScheduler|apscheduler|django-cron|django_cron|schedule\." master -- backend frontend/src scripts` → rc=1。（注意：master **有** `backend/apps/company_fields/timer.py`（245 行，`_fiscal_locks` 在 `:61`）——那是**由财年状态迁移触发的字段定时器**，不是墙钟倒计时/时间盒，属 O-02 域，本条不重复。）
  - 唯一周期行为（与原件一致）：`frontend/src/App.vue:18,24`（版本复核定时器）、`frontend/src/stores/auth.ts:168`（`window.setInterval` 会话心跳）、`frontend/src/stores/version.ts:14,46`（`setInterval(..., 5 * 60 * 1000)`）——`git grep -n "setInterval" master -- frontend/src` 的全部命中就是这 3 处。
- master 证据（正向）：`backend/apps/announcements/models.py:8-19`（master@97e117e）—— `class Announcement(models.Model):` / `version = models.CharField(max_length=32, help_text="版本号，如 1.4.0")` / `title` / `date` / `content`（"公告正文，支持受信任 HTML 片段"）/ `is_active`；默认内容见 `backend/apps/announcements/management/commands/seed_announcements.py:9-31`（v1.4.0 功能清单：股票系统、安全加固、计算字段引擎…）；写权限仅超管：`backend/apps/announcements/views.py:44-45`（`post` 内 `if getattr(request.user, "role", None) != "SUPER_ADMIN":`）、`:88-89`（patch）、`:106-107`（delete）。
- master 证据（房间）：`backend/apps/realtime/gateway.py:189-204` —— `def _can_join(room: str, session: dict) -> bool:` / `if room.startswith("user-"):`（仅允许本人） / `if room.startswith("comp-"):`（同一比赛或 SUPER_ADMIN） / `:204 return False`。即除 `user-{id}`、`comp-{id}` 之外**任何房间名一律拒绝**——不存在主席团/审计/大屏房间，无法定向广播。
- 差异说明：与原件**同一形态、同一严重度**；大屏/排行榜/倒计时/定向房间在 master 上全部缺失，"时间盒只能靠主持人看表喊"的结论不变。广播侧的 `message:manage` 门槛见 O-23。
- 验证命令：`git grep -l -i -E "倒计时|countdown|剩余时间|大屏|投屏|排行榜|leaderboard|scoreboard" master; git grep -n -i "el-countdown\|ElCountdown" master -- "*.vue" "*.ts"; git show master:backend/apps/realtime/gateway.py | sed -n '189,204p'`
- 置信度：**已验证**

### O-14 纸质合同与系统记录脱节：无打印 / PDF / 导出文档、无「一式 N 份」概念、编号可被任意比赛管理员越范围填写 → master 状态：A（仍存在，行号需重定位）

- 原件结论（bugfix-merged）：前端搜索 `window.print/打印/jsPDF/html2canvas/exceljs/xlsx` **0 命中**（唯一 `MapsManager.vue:1926` 是地图画布导出）；无「一式三份/四份/六份/copies/counterpart」概念；系统只有合同实例（参与方+输入项+效果），没有可签署的合同正文，`signed_at` 是点击执行时服务端自动打的时间戳；编号补全的范围校验 `_assert_edit_party_scope` 在持有 `contract:execute`/`contract:manage` 时**直接放行**（`contracts/views.py:411-460、896-911` (bugfix-merged) 行号）。
- master 证据（负向，全仓）：`git grep -n -i -E "window\.print|jspdf|html2canvas|exceljs|打印|一式|counterpart" master` → 应用代码与文档 **0 命中**；`copies` 仅命中 `LICENSE`（GPL 正文，10 处）。即 master 连原件提到的那一处地图画布导出也不存在。
- master 证据（正向）：
  - 合同实例字段：`backend/apps/contracts/models.py:66-75`（master@97e117e）—— `# JSON 文本：[{role,companyId,isHost?,contractNumber?}]` / `parties = models.TextField(default="[]")` / `inputs` / `execution_log` / `execution_result` / `signed_at = models.DateTimeField(null=True, blank=True)` / `executed_at`；合同类型 `backend/apps/contracts/models.py:13-34` 只有 `key/name/description/party_count/party_roles/input_schema/effects/conditions/graph/schema_version/enabled`——**没有任何合同正文/模板文本字段**。
  - `signed_at` 由服务端在执行时自动写入：`backend/apps/contracts/views.py:329` —— `        signed_at = contract.signed_at or timezone.now()`，随后 `:340`（原子 UPDATE 抢占 EXECUTED 时写入）、`:362` —— `        contract.signed_at = signed_at`。
  - 编号补全的范围校验（原件指控点，master 位置）：`backend/apps/contracts/views.py:844-859` ——
    ```python
    def _assert_edit_party_scope(user, company_id, role: str) -> None:
        """单公司编辑范围校验（用于分步补全编号）。"""
        if not user:
            return
        can_execute = has_permission(user.role, user.permissions_list, "contract:execute")
        can_manage = has_permission(user.role, user.permissions_list, _CONTRACT_MANAGE_PERM)
        if can_execute or can_manage:
            return          # ← 直接放行
        can_audit = has_permission(user.role, user.permissions_list, _CONTRACT_AUDIT_PERM)
        if not can_audit:
            raise BusinessError("无权修改合同编号", code=403, status_code=403)
        scopes = user.company_scopes_list
        if company_id not in scopes:
            raise BusinessError(f"无权修改角色 {role} 所属公司的合同编号", code=403, status_code=403)
    ```
    调用点 `backend/apps/contracts/views.py:415-419` —— `            # 权限隔离：只能改自己审核范围内公司的编号` / `            _assert_edit_party_scope(request.user, p.get("companyId"), role)` / `            p["contractNumber"] = v ...`。
  - 谁能持有这两个权限：`backend/apps/common/permissions.py:341-353` —— `ROLE_TEMPLATES["COMPETITION_ADMIN"]["defaultPermissions"] = BASE_VIEW_PERMISSIONS + ["contract:manage", "contract:audit", "contract:execute", "stock:edit"]`（`grantCeiling` 同）→ **每个比赛管理员默认就带 `contract:manage`+`contract:execute`**，因此对任何一个参与方都能改编号。
- 差异说明：三个子项在 master 上全部成立，机制相同（`can_execute or can_manage → return`）；只有位置不同：原件引用的 `:411-460`/`:896-911` 在 master 上分别对应 `:415-419` 与 `:844-859`（master `contracts/views.py` 共 869 行，不是 Lead 前提里的 738 行）。
- 验证命令：`git show master:backend/apps/contracts/views.py | sed -n '844,859p'; git show master:backend/apps/contracts/views.py | sed -n '415,419p'; git grep -n -i -E "window\.print|jspdf|一式|counterpart" master`
- 置信度：**已验证**

### O-16 财年编号两套口径（界面建的首个财年是第 0 财年，Excel/建包建的是 2026） → master 状态：C（无可命中：第二套口径不存在）

- 原件结论（bugfix-merged）：DB 实测 `comp=4 year=0`、`comp=189 year=2026`，两套口径并存（界面建 0 基财年、Excel/建包建 2026），与公告里的「第一~第六财年」对不上；证据 `CompetitionListView.vue:440-444`、`stores/competition.ts:129` (bugfix-merged) 行号。
- master 证据（唯一那套口径仍是 0 基）：
  - `frontend/src/views/competitions/CompetitionListView.vue:437-447`（master@97e117e）—— `async function startNextFiscalYear() {` / `// 按当前列表的实际年份推算下一个财年（取最大年份 +1）…` / `    const nextYear = fiscalYears.value.length ? maxYear + 1 : 0;` / `    await api.post(\`/competitions/${selectedComp.value.id}/fiscal-years\`, { year: nextYear });`
  - 界面直接把它渲染成「第 0 财年」：`CompetitionListView.vue:148` —— `<template #default="{ row }">第 {{ row.year }} 财年</template>`。
  - `frontend/src/stores/competition.ts:127-130` —— `      const active = fys.find((f) => f.status === "ACTIVE");` / `      // 注意：首个财年 year = 0，必须用 active ? ... 判断对象存在性，不可对 year 做真值判断。` / `      currentFiscalYear.value = active ? active.year : null;`（master 代码里**显式承认**首个财年是 0）。
  - 后端不参与编号决策：`backend/apps/competitions/models.py:34` —— `    year = models.IntegerField()`（无 default、无校验、无序列生成），`unique_together = (("competition", "year"),)`（`:41`）→ `year` 完全由客户端传入。
- master 证据（第二套口径的载体不存在，负向）：`git ls-tree -r --name-only master:backend | Select-String "management/commands|examples/"` → 全部 management command 只有 2 个：`apps/announcements/management/commands/seed_announcements.py`、`apps/common/management/commands/rundaphne.py`；master 顶层目录（`git ls-tree --name-only master`）为 `.gitignore/LICENSE/README.md/VERSION.json/architecture_diagram/backend/deploy/docs/frontend/scripts/tests/widget-package-examples`——**无 `contract_watcher/`、无 `backend/examples/`、无 `backend/apps/contracts/management/`**；另有两条全仓 0 命中佐证"建包通道整体不存在"：`git grep -n -i -E "checklist|准备清单" master` → rc=1（无 39 项准备清单、无 `preparation/checklist.py`）、`git grep -n -E "建包|导入包|合同包" master` → rc=1（无 Excel 建包/合同包导入导出）。即 master 上不存在任何可写入另一个 year 值的"Excel/建包"通道。
- 差异说明：**C，属「因此没有这个问题」**——"两套口径互相冲突"在 master 上不可能出现，因为第二套口径（Excel/建包）整体不存在。**但必须同时写清这不是"没问题"**：① master **根本没有建包能力**（比 bugfix-merged 更缺：没有 39 项准备清单、没有 Excel 建包与归档导入导出、没有 `preparation/`）；② master 唯一那套口径本身就是 **0 基**，界面会显示"第 0 财年"（`CompetitionListView.vue:148`），与比赛规则/公告的"第一~第六财年"仍然对不上——这是同一缺陷的**更轻形态（单口径）**，不是消失。
- 验证命令：`git show master:frontend/src/views/competitions/CompetitionListView.vue | sed -n '437,448p'; git show master:frontend/src/stores/competition.ts | sed -n '127,130p'; git ls-tree -r --name-only master:backend | grep -E "management/commands|examples/"`
- 置信度：**已验证**（"两套口径不复现"由缺目录 + 无第二写入通道双重确认；0 基口径由 UI/Store 原文确认）

### O-18 断线重连对账无并发上限、无单飞，产业字段按公司逐个请求 → master 状态：A（仍存在）

- 原件结论（bugfix-merged）：`request.ts:753-788` (bugfix-merged) 的断线重连补拉无并发上限、无单飞，且 `/company-fields/${cid}` 在 `map` 内逐个公司请求，单人最多 = 已访问公司数 个请求。
- master 证据（同一函数、同一写法）：`frontend/src/api/request.ts:746-855`（master@97e117e）——
  - `:746` `export async function reconcileAllIncremental(): Promise<void> {`
  - `:754` `    [cols, maps] = await Promise.all([listFullCollections(), listMapSyncKeys()]);`
  - `:757-758` `    await Promise.all(` / `      cols.map(async (c) => {` —— **对全部已加载集合无上限并发**；
  - `:760-767` `        if (c.resource === "companyField") {` / `… const cid = m ? Number(m[1]) : null;` / `            const vRaw = await (api as any).get(\`/company-fields/${cid}\`, {` —— **逐公司一个请求**；
  - `:817-818` 地图再一轮 `await Promise.all(` / `      maps.map(async (m) => {`。
  - **无单飞、无并发上限**：`git grep -n -i -E "reconciling|singleflight|p-limit|pLimit|concurrency|并发上限|Semaphore" master -- frontend/src` → rc=1（0 命中）；唯一调用点 `frontend/src/realtime/resource-changed.ts:163-174`（`onRealtime("connect", …)` → `:173 void reconcileAllIncremental();`）。
- 差异说明：机制与原件**相同**，故记 A。master 侧存在若干**降噪缓解**（不足以改变结论，但说明严重度可能略低于原件描述）：`:747-748` 无 token 直接返回（"避免匿名客户端轰炸服务器、产生大量 401 噪声与审计日志"）、`:769/:799` 请求带 `silent: true`、`:849-853` 派发 `sync:reconciled` 事件带 400ms 防抖、请求均为 `updatedAfter=<baseline>` 的增量（`:768/:798`）。另可注意：master 自己的版本公告写着「修复断线重连后客户端对服务器发起大量并发对账请求」（`backend/apps/announcements/management/commands/seed_announcements.py:54`、`frontend/src/data/announcement.ts:70`），但代码里并没有并发上限/单飞——**公告与实现不符**，建议 Lead 作为一致性问题转交（见文末移交项）。
- 验证命令：`git show master:frontend/src/api/request.ts | sed -n '746,758p;760,768p;817,818p'; git grep -n -i -E "reconciling|singleflight|p-limit|concurrency|Semaphore" master -- frontend/src`（预期后者无输出）
- 置信度：**已验证**（"是否真的打爆单线程服务端"为**推断**：本机不能起服务压测）

### O-20 `contract_watcher` 现场门槛高（Windows+Excel+xlwings、每家一本 xlsx、单实例、只覆盖一个账号范围、记账规则手写 Python） → master 状态：C（无法命中：master 没有这条外部记账兜底路径，能力更缺）

- 原件结论（bugfix-merged）：`contract_watcher` 能用但有前提——Windows + 已装 Excel + `pip install xlwings`，每个被记账公司一本 `books/company_<id>.xlsx`，单实例锁 `data/watcher.lock`，只覆盖账号 `companyScopes` 范围内的公司，记账规则要人手写 Python（`contract_watcher/README.md:35-57,110-140,257-290` (bugfix-merged) 行号）。
- master 证据（负向，全仓）：
  - `git grep -n -i -E "contract_watcher|xlwings|bookkeeping|记账" master` → **rc=1，0 命中**；
  - master 跟踪文件共 445 个，顶层目录只有 `.gitignore/LICENSE/README.md/VERSION.json/architecture_diagram/backend/deploy/docs/frontend/scripts/tests/widget-package-examples`（`git ls-tree --name-only master`）→ **无 `contract_watcher/`**；
  - 后端亦无任何 Excel/CSV 写入能力（见 O-03：`openpyxl|xlsxwriter|pandas|to_csv` 在 `backend` 下 rc=1），即 master 上不存在"以表格为载体的账"这一实现路径。
- 差异说明：**C，属「能力比 bugfix-merged 更缺」**。原件把 `contract_watcher` 记为"P2 门槛高"，隐含前提是"这条路存在但难用"；在 master 上这条路**根本不存在**——没有外部记账程序、没有账套模板、没有 readable 合同记录导出。与 O-03 叠加后的后果：bugfix-merged 至少还能给出"12 张表的账套 + 平衡校验 + 每份合同的中文可读记录"作为唯一可用的对账/取证底稿，master 上对账与赛后取证**只能靠人工抄字段值**（`CompanyFieldValue.value` 是裸 TEXT，无科目/借贷/期初期末）。因此该条在 master 上不再是"门槛高"，而是"完全缺失"。
- 验证命令：`git grep -n -i -E "contract_watcher|xlwings|bookkeeping|记账" master; git ls-tree --name-only master`
- 置信度：**已验证**（目录缺失为确定事实；"后果更重"为推断，依赖现场流程）

### O-23 消息中心可当广播但需 `message:manage`（扩展集，界面授不出去）、无已读确认、无定时重播、收件人上限 500 → master 状态：A（仍存在）

- 原件结论（bugfix-merged）：`POST /api/messages` 支持"全体（可选范围内用户）"+ 在线实时弹窗 `message:new`，但需要 `message:manage`（扩展集，**界面授不出去**），收件人上限 500，**没有已读确认/回执**、没有定时重播；证据 `messages/views.py:323-403`、`common/permissions.py:326-332` (bugfix-merged) 行号。
- master 证据（逐子项）：
  1. **上限 500**：`backend/apps/messages/views.py:64` —— `_MAX_RECIPIENTS = 500`；强制点 `:349-351` —— `        if len(recipient_ids) > _MAX_RECIPIENTS:` / `                f"收件人数量超过上限（{_MAX_RECIPIENTS}）", code=400, status_code=400`。
  2. **广播需 `message:manage`**：`backend/apps/messages/views.py:323-324`（`CollectionView.post` 上的 `@require_permissions("message:manage")`，模块头 `:3` 亦自述 `manage=message:manage（发布 / 删除 / 上传图片）`）；读侧为 `message:view`（`:307`）。
  3. **属扩展集**：`backend/apps/common/permissions.py:326-332` —— `_COMPETITION_ADMIN_EXTRAS = ["message:manage", "contractType:manage", "industryType:manage", "company:manage", "data:region:edit"]`；`ROLE_TEMPLATES["COMPETITION_ADMIN"]`（`:341-356`）的 `defaultPermissions`（`:342-347`）与 `grantCeiling`（`:348-353`）都**不含** `message:manage`，它只出现在 `grantExtras`（`:354`）。
  4. **界面授不出去**：`frontend/src/views/account-management/AccountManagementView.vue:258-312` 的 `derivePermissions(role, companies)` 按角色硬编码权限——COMPETITION_ADMIN 分支 `:298` `      permissions: [...baseView, "contract:manage", "contract:audit", "contract:execute", "stock:edit"],`（**无 message:manage**），PLAYER 分支 `:306-307` 只有 `baseView`；表单里也没有任何权限勾选项（`:133-165` 只有"角色"下拉与"公司范围"多选，`:148-151` 的"账号权限"分隔区是派生结果提示），提交时直接 `permissions: derived.permissions`（`:462`、`:474`）。后端其实**支持**授扩展权限（`backend/apps/users/views.py` 的 `UserPermissionsView` = `POST /api/users/:id/permissions`，走 `assert_grant_allowed` + `grantExtras`），但**界面没有入口**——与原件结论一致。
  5. **无已读确认/回执**：`backend/apps/messages/serializers.py:51-54` —— `def serialize_sent_item(instance: Message, sender_name: str, recipients_count: int) -> dict:` / `    data["_count"] = {"recipients": recipients_count}`（**只回总人数**）；`read`/`readAt` 只出现在收件人侧 `serialize_inbox_item`（`:59-64`）→ 发布者看不到谁读过。
  6. **无定时重播**：`git grep -n -i -E "scheduled|publish_at|定时重播|重播|cron" master -- backend/apps/messages frontend/src/views/messages` → rc=1；`Message` 模型字段（`backend/apps/messages/models.py:10-37`）只有 `title/content/sender/competition/targets_all/target_user_ids/images/created_at/updated_at`，**无任何定时字段**。
  7. 广播范围与原件一致：`backend/apps/messages/views.py:94-108` `_selectable_user_ids(actor, competition_id)` 限定本比赛（超管可显式传 competitionId），"全体"= 该可选集合（`:346`）。
- 差异说明：五个子项与原件完全对应，严重度不变（运维要发全场广播必须由超管通过 API 授 `message:manage`，界面无法完成；且发布后无法确认是否被看到、无法定时重播；100 人规模虽未触 500 上限，但跨比赛"全场"广播受比赛范围约束）。
- 验证命令：`git show master:backend/apps/messages/views.py | sed -n '64p;323,324p;346,352p'; git show master:backend/apps/common/permissions.py | sed -n '326,332p;341,356p'; git show master:frontend/src/views/account-management/AccountManagementView.vue | sed -n '296,303p;455,476p'`
- 置信度：**已验证**

---

## 汇总统计

| 状态 | 条数 | 编号 |
| --- | --- | --- |
| A 仍存在 | 5 | O-03、O-07、O-14、O-18、O-23 |
| B 存在但形态/严重度不同 | 0 | —— |
| C master 上无法命中 | 2 | O-16（**因此没有这个问题**：无第二套口径）、O-20（**能力更缺**：无外部记账路径） |
| D 已修复 | 0 | —— |
| 合计 | 7 | |

补充结论（对 Lead 定级有影响）：
- 两条 C 类**都不等于"master 更安全"**：O-16 的载体（Excel/建包）在 master 整体缺失，O-20 的外部记账兜底在 master 整体缺失，二者与 O-03 叠加后，master 的"现场无账表可核"比 bugfix-merged 更严重（bugfix-merged 至少还有 `contract_watcher` 账套与 `preparation` 归档导出）。
- O-03 / O-07 / O-14 / O-18 / O-23 五条在 master 上均**原样成立**，可直接按原件级别（P0/P1/P1/P2/P2）沿用，无需按 master 形态降级。

## 已核对无问题或无法判定

1. **不是"master 没有编号范围校验"**：`_assert_edit_party_scope` 在 master 上存在（`backend/apps/contracts/views.py:844-859`），且对 `contract:audit` 账号会真正按 `company_scopes_list` 拦截（`:852-859`）；缺陷只在 `can_execute or can_manage → return`（`:850-851`）这一分支，措辞不应写成"完全没有校验"。验证：`git show master:backend/apps/contracts/views.py | sed -n '844,859p'`。
2. **不是"实时通道没有房间鉴权"**：master `backend/apps/realtime/gateway.py:189-204` 的 `_can_join` 对 `user-`/`comp-` 之外一律返回 False、`comp-` 还校验比赛归属，O-07 的结论只是"没有第三类业务房间"，不能引申为"可任意订阅"。验证：`git show master:backend/apps/realtime/gateway.py | sed -n '189,204p'`。
3. **`announcements` 确为版本更新公告而非业务公告**（原文与 seed 内容均为产品功能清单，标题形如"更新公告 v1.4.0"）：`backend/apps/announcements/management/commands/seed_announcements.py` 头部 `SEED_DATA` 与 `frontend/src/data/announcement.ts` 同源。验证：`git show master:backend/apps/announcements/management/commands/seed_announcements.py | sed -n '1,30p'`。
4. **"countdown 0 命中"的口径已收紧**：`countdown` 在 master 全仓命中 1 个文件，但它是 `backend/logviewer/static/vendor/element-plus.full.min.js`（第三方库自带组件，未被任何页面使用）。验证：`git grep -l -i "countdown" master; git grep -n -i "el-countdown" master -- "*.vue"`（后者空）。
5. **"copies/counterpart/一式 0 命中"的口径**：`copies` 命中 11 行但全部在 `LICENSE`（GPL 正文），`counterpart`/`一式`/`打印` 全仓 0 命中。验证：`git grep -n -E "一式|counterpart" master; git grep -n "copies" master`（预期前者无输出、后者只列 `LICENSE`）。
6. **Lead 前提校正**：master `backend/apps/contracts/views.py` = **869 行**（不是 738 行）；master 的 `frontend/src/views/account-management/AccountManagementView.vue` = 572 行，路径是 `views/account-management/`，**不是** bugfix-merged 的 `views/system/`（master 的 `views/system/` 只有 `AuditLogView.vue`）。验证：`(git show master:backend/apps/contracts/views.py).Count; git ls-tree -r --name-only master:frontend/src/views | Select-String account-management`。
7. **无法判定（需运营确认）**：O-16 中"首个财年 year=0"是否构成缺陷，取决于赛事方能否接受界面显示"第 0 财年"（`CompetitionListView.vue:148`）；代码事实确定，业务可接受度本报告不判定。另：O-18 的"重连风暴强度"、O-03/O-20 的"现场后果加重幅度"均为**推断（不可复核：本机不能起服务/无现场数据）**。
