# M2 master 对照：运行时 / 并发 / 财年类（O-02 / O-04 / O-13 / O-17 / O-19 / O-24 / O-25）

## 对照对象与方法

- **原件（待核报告）**：`OPS_READINESS_REPORT.md`（编写于 `bugfix-merged @ 5cb3468`，比 master 多 357 文件 / +62k 行）
  路径：`C:\Users\wuhao\.dsh\attachments\v1\files\80\803e2e7c2c566ad9271087a90eb7a644fcd04e9a6a1248b5ed9e3202907cfc7f\OPS_READINESS_REPORT.md`（只读引用；其行号一律标注 **(bugfix-merged)**，不得沿用）
- **目标（唯一真源）**：`master@97e117e8781e0c02462a24269a165dde08ecef44`，快照 `C:\Users\wuhao\AppData\Local\Temp\dsh-Ec2sca\gipfel-master-lf`（LF、逐字节等于 master blob，439 文件）
- **方法**：
  1. 只用 `read`/`grep` 工具读 LF 快照取行号与原文行；
  2. 框架语义（Django 5.0.14 / daphne 4.1.2 等）以 `backend/requirements.txt` 锁定版本对应的**本机 venv 内源码**为证据，并在条目中明确标注这是**框架语义、不是仓库代码**；
  3. 负向结论用全仓 grep 给出（0 命中）；
  4. 只读：未改任何代码，未执行部署/`systemctl`/写库命令。
- **引用体例**：`master 证据` 中的多行原文以 ` / ` 分隔表示同一条目的连续多行；跨行省略处用 `...` 标记；行号区间均为 master 快照的真实行号。
- **方法学提醒（影响本次对照的一个坑）**：PowerShell `Get-Content` 对含超长行的文件会**漏计行数**（实测 `backend/apps/competitions/views.py` 真实 268 行、`Get-Content` 计数 250；`backend/apps/contracts/views.py` 真实 **869 行**、`Get-Content` 计数 810）。本报告行号全部来自 `read`/`grep`，并用 LF 字节数交叉验证。**任务书里"master 的 `contracts/views.py` 只有 738 行"不成立——它是 869 行，报告描述的内存范围过滤段确实存在。**
- **分类词表**：A 仍存在 / B 存在但形态或严重度不同 / C master 上无法命中 / D 已修复

## 总览

| ID | 原件级别 | 结论 | master 一句话状态 |
| --- | --- | --- | --- |
| O-02 | P0 | **A** | 财年推进仍是两个端点 + 无 ACTIVE 唯一性 + 定时器请求内同步 + 抢锁失败静默 return + 单公司失败吞掉 + 无回滚；行号整体平移，另发现"同年份并发创建 → 500"这一 master 特有后果 |
| O-04 | P0 | **B** | 单进程 / 无 WAL / 无 busy_timeout / 每次写多一条审计 / 4xx·5xx 也插审计 / 无 nginx 限流 / 20s 心跳 / 15s 超时 / 重连补拉风暴全部成立；**但"全部同步视图共用一个线程"在 master 上被证伪**（Django 5.0.14 + asgiref 每请求一个线程池），机制与量化需改写 |
| O-13 | P1 | **A**（合同侧更重） | `CompanyFieldValue`/`ContractFieldEffect` 映射为 None → 字段值变更不进审计；审计仅超管、无公司/时间过滤、只增不删、20 条/页。**master 额外：合同执行走 `.update()` 绕过信号且无显式审计 → 执行事件本身也无审计行** |
| O-17 | P2 | **A** | PATCH 无状态机，CLOSED→ACTIVE 仍会再触发一次 FY_START；前端文案仍是"此操作不可撤销"（同一行号 461） |
| O-19 | P2 | **A** | 限流仍只覆盖 `POST /api/auth/login`、键 (IP+用户名)、5 分钟 10 次锁 15 分钟、锁存进程内存重启清空；README 自述仍在（master `README.md:200`） |
| O-24 | P2 | **A** | 默认 `pageSize=50`、审计页 20 条/页、非超管合同列表整表进内存过滤；另外增量分支与 `requireExistingIds` 也各有一条整表进内存路径 |
| O-25 | P3 | **A** | 结束财年后（本端乐观 + 全场广播）顶部栏显示"未开启财年"；master 另有 `fiscalYearLoading` 三态保护，抑制的是首次加载/切比赛的瞬时跳变 |

---

### O-02「推进财年」非原子：两次点击两个端点、后端无 ACTIVE 唯一性校验、定时器请求内同步且抢不到锁静默跳过、单公司失败被吞、无回滚 → master 状态：**A**

- **原件结论**：财年只有 `(competition, year, status)` 三列、无「当前财年」唯一约束；「开始新财年」与「结束财年」是两个独立端点、无事务/锁/幂等；定时器在请求线程内同步跑完全部公司，抢不到比赛级非阻塞锁就 warning + return，单公司异常只记 warning 继续。bugfix-merged 证据：`backend/apps/competitions/models.py:26-57`、`backend/apps/competitions/views.py:206-262`、`backend/apps/company_fields/timer.py:147-196,239-245`、`frontend/src/views/competitions/CompetitionListView.vue:130-174,437-473`。
- **master 证据**（master@97e117e）：
  - `backend/apps/competitions/models.py:39-42`：
    `class Meta:` / `db_table = "fiscal_years"` / `unique_together = (("competition", "year"),)` / `indexes = [models.Index(fields=["competition", "updated_at"])]` —— 无「每比赛唯一 ACTIVE」约束；`FiscalYear.STATUS_CHOICES` 只有 `ACTIVE`/`CLOSED`（`models.py:27`），**压根没有状态机**。
  - `backend/apps/competitions/views.py:192-193`：`if FiscalYear.objects.filter(competition=comp, year=year).exists():` / `raise BusinessError("该财年已存在", code=409, status_code=409)` —— 只按 `(competition, year)` 去重，**不校验是否已存在 ACTIVE 财年**。
  - `views.py:194-198`：`fy = FiscalYear.objects.create(competition=comp, year=year, status=serializer.validated_data.get("status", "ACTIVE"),)`
  - `views.py:200-202`：`_broadcast_fiscal_year(fy)` → `_apply_fiscal_year_timer(comp.id, "FY_START")`（**同请求同步调用**）
  - `views.py:233-236`：`if prev_status != "ACTIVE" and new_status == "ACTIVE": _apply_fiscal_year_timer(fy.competition_id, "FY_START")` / `elif prev_status != "CLOSED" and new_status == "CLOSED": _apply_fiscal_year_timer(fy.competition_id, "FY_END")`
  - `backend/apps/company_fields/timer.py:159-163`：`if not lock.acquire(blocking=False):` / `logger.warning(` / `"财年定时器：比赛 #%s 的 %s 已在执行中，跳过本次并发触发", competition_id, trigger` / `)` / `return`
  - `timer.py:189-196`：`try: _apply_timer_to_company(c, fields) ... except Exception as e: logger.warning("财年定时器：公司 #%s 处理失败：%s", c.id, ...)`（吞掉、继续下一家）
  - `timer.py:239-242`：`if pending:` / `with transaction.atomic():` / `for industry_field_id, value in pending:` / `_write_field_value(company.id, industry_field_id, value, version=None)`（**逐公司一个小事务，无整体回滚、无进度标记**）
  - `frontend/src/views/competitions/CompetitionListView.vue:134` `:disabled="hasActiveFiscalYear"`；`:260-262` `const hasActiveFiscalYear = computed(() => fiscalYears.value.some((f: any) => f.status === "ACTIVE"), );`（取自**本地列表**）；`:131-137` 开始按钮；`:165-171`/`:191-197` 结束按钮
- **差异说明**：核心机制**逐条一致**，属 A；行号整体平移（bugfix-merged `views.py:206-262` → master `181-237`；`timer.py` 行号基本一致：`147-196`、`239-245` 在 master 上都对得上）。master 上新增/更清晰的两点：
  1. **同年份并发双击 → 500 而不是 409**：去重 `exists()`（`views.py:192`）与 `create()`（`:194`）非原子，而 `(competition, year)` 是 **DB 级 UNIQUE**（`models.py:41`）→ 两个请求都通过 exists 检查时，第二个抛 `IntegrityError`（非 DRF 异常）→ 由 `backend/apps/common/exceptions.py:52-55` 兜成 500，并额外落一条 `kind="error"` 审计。也就是说"两个 ACTIVE 财年"只在两次点击**算出不同年份**（本地缓存陈旧，如 `startNextFiscalYear` 用本地 `maxYear+1`，`CompetitionListView.vue:440-444`）时出现；同一年份时表现为 500。
  2. FY_END 与 FY_START **共用同一把锁**（`timer.py:154-158` 的 `_fiscal_locks[competition_id]`，两个 trigger 同 key）→ 上一财年结束定时器未跑完时，新财年开始定时器必然被静默丢弃，这一条与原件结论完全相同。
- **验证命令**（只读，快照内）：
  `grep -n "unique_together\|该财年已存在\|_apply_fiscal_year_timer\|blocking=False\|跳过本次并发触发\|with transaction.atomic" backend/apps/competitions/models.py backend/apps/competitions/views.py backend/apps/company_fields/timer.py`
- **置信度**：已验证

### O-04 单进程 daphne + 全部同步视图共用一个线程 + SQLite 无 WAL/无 busy_timeout + 每次写多一条审计 INSERT + 每个 4xx/5xx 也插审计 + 无 nginx 限流 → 财年边界与重连风暴下大面积超时 → master 状态：**B**

- **原件结论**：后端单进程 daphne；所有 API 同步视图在 ASGI 下被 `sync_to_async(..., thread_sensitive=True)` 包裹 → 全部共用一个线程；SQLite 无 OPTIONS/WAL/busy_timeout；每次业务写额外插一行审计、每个 4xx/5xx 也插一行；nginx 无限流；客户端 20s 心跳、15s axios 超时、断线重连对每个已加载集合并发补拉。bugfix-merged 证据：`deploy/gipfel.service:26-31`、`django/core/handlers/base.py:129,250`、`backend/backend/settings.py:305-310`、`apps/common/signals.py:114-125`、`apps/common/exceptions.py:43-50`、`deploy/nginx-gipfel.conf:9-13`、`frontend/src/api/request.ts:39,753-788`、`frontend/src/stores/auth.ts:165,190`、`README.md:206`。
- **master 证据**（master@97e117e）——**成立的部分**：
  - 单进程：`deploy/gipfel.service:18-24` 只有一条 `ExecStart=__INSTALL_DIR__/backend/.venv/bin/daphne \ -u /run/gipfel/gipfel.sock \ -b 127.0.0.1 \ -p 8000 \ --access-log /var/log/gipfel/access.log \ --proxy-headers \ backend.asgi:application`，全文 55 行、无 `--workers`/进程数参数（daphne 4.1.2 本身也没有 worker 选项）。
  - SQLite 未调优：`backend/backend/settings.py:304-309` `DATABASES = { "default": { "ENGINE": "django.db.backends.sqlite3", "NAME": BASE_DIR / "db.sqlite3", } }`（**无 `OPTIONS`**）；全后端 `journal_mode|PRAGMA|wal|busy_timeout` 0 命中 → 回滚日志模式、无 WAL。框架侧：`django/db/backends/sqlite3/base.py:154-174` 只把 `settings_dict["OPTIONS"]` 透传给 `sqlite3.connect(**kwargs)`，`:184` 仅执行 `PRAGMA foreign_keys = ON` → **busy timeout 落到 Python `sqlite3.connect` 的默认 `timeout=5.0`**（框架语义，非仓库代码）。
  - 每次写多一条审计：`backend/apps/common/signals.py:114-125` → `backend/apps/common/audit.py:62-75` `AuditLog.objects.create(...)`。
  - 每个 4xx/5xx 也插审计：`backend/apps/common/exceptions.py:43-50`（`log_exception(request, exc, response)` 在 `exception_handler` 入口无条件调用）。
  - 无 nginx 限流：`deploy/nginx-gipfel.conf:9-13`（`upstream gipfel_django { server 127.0.0.1:8000 fail_timeout=3s max_fails=3; keepalive 32; }`）+ `limit_req|limit_conn` 0 命中。
  - 客户端：`frontend/src/api/request.ts:22` `timeout: 15000,`；`frontend/src/stores/auth.ts:155` `const HEARTBEAT_INTERVAL_MS = 20 * 1000;`（`:168-180` 每周期打 `/auth/me`）；`request.ts:746-790` `reconcileAllIncremental()`：`[cols, maps] = await Promise.all([listFullCollections(), listMapSyncKeys()]);` + `await Promise.all(cols.map(async (c) => { ... await api.get('/company-fields/${cid}', ...) ... }))`（无并发上限、公司字段逐个请求）。
  - 无全局限流自述：`README.md:200`（末句"项目目前**没有**全局 HTTP 限流中间件"）。
  - `/api/` 读超时 120s：`deploy/nginx-gipfel.conf:55` `proxy_read_timeout  120s;`。
- **master 证据（反证）——"共用一个线程"不成立**（框架语义，非仓库代码；Django 5.0.14 = master `backend/requirements.txt:1` 锁定版本，本机 `backend/.venv` 同版本；asgiref 3.12.1，Django 约束 `asgiref<4,>=3.7.0`）：
  - `django/core/handlers/asgi.py:169`：`async with ThreadSensitiveContext():` —— **每个请求**进入自己的 thread-sensitive 上下文。
  - `asgiref/sync.py:110-121`：`ThreadSensitiveContext` 文档自述"By default, a single thread pool executor is shared within a process. The ThreadSensitiveContext() context manager may be used to specify a thread pool **per context**"。
  - `asgiref/sync.py:467-478`：`elif self.thread_sensitive_context.get(None): thread_sensitive_context = self.thread_sensitive_context.get(); if thread_sensitive_context in self.context_to_thread_executor: executor = ... else: executor = ThreadPoolExecutor(max_workers=1); self.context_to_thread_executor[thread_sensitive_context] = executor` —— 每请求一个 `max_workers=1` 的执行器，**请求之间并发**，只有"单个请求内部"的同步调用串行。
- **差异说明**：判 B。除"所有同步视图共用一个线程"外，其余事实全部成立；该子结论在 master 上属 **C（无法命中/证伪）**，随后的机制推断需要改写：
  - 原表述：1100 个重连请求"全部涌向一个线程"，期间任何人无法操作 → **偏重**：读请求（心跳、列表）在各自线程并行，不会排在一条队列后面。
  - master 实际压力点：并发请求数 ≈ 同时在途请求数（每请求一个 `ThreadPoolExecutor(max_workers=1)` + 各自一条 SQLite 连接），所有**写**在 SQLite 单写者上串行；回滚日志模式下写事务会阻塞读 → 真正会大面积超时的是"财年推进/批量字段写入这种长写事务 + 并发写"组合，表现为 `database is locked`（5s busy timeout 之后直接 500），而不是"单线程排队"。线程数无上限（每个在途请求一条线程）也是 master 特有的形态。
  - 另一处反向差异：只有走 `save()/create()/delete()` 的写才触发信号 → 才多一条审计 INSERT；走 `.update()` 的写（如合同执行 `backend/apps/contracts/views.py:333-345`、部分字段写）**不产生审计行**，所以"每次业务写多一条审计"在 master 上要按写路径区分。
- **验证命令**（只读）：
  `grep -rn "limit_req\|limit_conn\|journal_mode\|busy_timeout\|OPTIONS" deploy/nginx-gipfel.conf backend/backend/settings.py; grep -n "timeout: 15000" frontend/src/api/request.ts; grep -n "HEARTBEAT_INTERVAL_MS" frontend/src/stores/auth.ts; grep -n "ThreadSensitiveContext" backend/.venv/Lib/site-packages/django/core/handlers/asgi.py`
- **置信度**：已验证（仓库事实）；"单线程"反证部分为**框架语义 + 依赖版本**，见"无法判定"第 3 条

### O-13 公司字段值变更与合同落账不进审计（`MODEL_TO_RESOURCE[...]=None` 被信号跳过）；审计日志只增不删、无按公司/时间过滤、仅超管可见 → master 状态：**A**（合同侧在 master 上更重）

- **原件结论**：`MODEL_TO_RESOURCE["CompanyFieldValue"]=None`，信号连接会跳过映射为 `None` 的模型 → 现金/库存/配额/碳排的数字改动查不到；审计日志只有超管能看、只支持 `kind/model/operatorId/competitionId` 四个过滤、前端 20 条/页、只增不删且无清理命令。bugfix-merged 证据：`backend/apps/realtime/emit.py:149,152`、`backend/apps/common/signals.py:198-202`、`backend/apps/audit/views.py:20-56`、`frontend/src/views/system/AuditLogView.vue:166`。
- **master 证据**（master@97e117e）：
  - `backend/apps/realtime/emit.py:147` `"CompanyFieldValue": None,`、`:150` `"ContractFieldEffect": None,`（`:122` `"AuditLog": None,`）
  - `backend/apps/common/signals.py:103-105`：`resource, _is_global = resolve_model_info(sender)` / `if not resource:` / `return`（映射为 None → 既不审计也不广播）；`:199-200`：`# None 表示该模型只列在映射里但显式标记为「不广播」` / `if MODEL_TO_RESOURCE.get(name) is None: continue`（连信号都不 connect）
  - `backend/apps/audit/views.py:25` `@require_permissions("account:manage")`；`:29-49` 过滤项只有 `kind`/`model`/`operatorId`/`competitionId`；`:52` `total = qs.count()`
  - `backend/apps/audit/models.py:5-33`：无 company 字段（只有 `competition_id`/`record_id`），索引为 `kind+created_at`/`operator_id+created_at`/`model+created_at`/`request_id`
  - `backend/apps/audit/urls.py:7-9`：只有 `path("audit-logs", views.AuditLogListView.as_view(), ...)`，无删除/清理路由；`backend/apps/audit/` 下**无** `management/` 目录（无清理/归档命令）
  - `frontend/src/views/system/AuditLogView.vue:166` `const pageSize = 20;`（`:102-103` 传给 el-pagination 的 `:page-size`/`:total`）
- **差异说明**：判 A，且**合同侧比原件更重**：
  1. 公司字段值：`CompanyFieldValue` 映射为 None + `company_fields/views.py` 内无任何显式 `log_write`（全后端 `log_write|AuditLog.objects.create` 只命中 `common/audit.py:48,62,93`（定义与实现）、`common/signals.py:117,158`（调用）、`apps/stock/engine.py:1914-1916`（唯一一处业务显式调用），另有 `apps/audit/serializers.py:5` 一句注释）→ 字段值改动（含财年定时器批量写入）在审计里完全不可见，与原件一致。
  2. 合同执行（master 特有差异）：`contracts/views.py:333-345` 用 `Contract.objects.filter(pk=pk).exclude(status__in=[...]).update(status="EXECUTED", ...)` 抢占状态，**绕过 `post_save`**（代码注释 `:342`、`:381-383` 自述"`.update()` 不触发 post_save"，但只补了广播、没补审计），且 master 的 `contracts/views.py` 内**没有** `log_write` 调用 → **一次成功执行不产生任何审计行**（`ContractFieldEffect` 明细即使触发 `post_save` 也因映射 None 被跳过）。原件称 bugfix-merged 在执行后**显式补写审计（含落账字段摘要）**，因此这一半在 master 上是"能力更缺"，不是"已修复"。
  3. `changes` 只有"全字段快照"没有 before/after diff（`signals.py:116` `changes = {action: _summarize_instance(instance)}`，模块自述 `:14-15`"updated 动作由于 Django 不给 diff…审计仅记录「有更新」"）→ 即使有审计行也答不出"从 X 改成 Y"。
- **验证命令**（只读）：
  `grep -n "CompanyFieldValue\|ContractFieldEffect" backend/apps/realtime/emit.py; grep -n "if not resource\|MODEL_TO_RESOURCE.get(name) is None" backend/apps/common/signals.py; grep -rn "log_write" backend/apps | grep -v common/audit.py; grep -n "require_permissions\|query_params.get" backend/apps/audit/views.py`
- **置信度**：已验证

### O-17「结束财年」提示不可撤销但 API 允许改回 ACTIVE，且回切会再触发一次 FY_START 把全场字段重置 → master 状态：**A**

- **原件结论**：前端确认框写"此操作不可撤销"，但 `PATCH` 允许把 `CLOSED` 改回 `ACTIVE`，而该回切会再触发一次 `FY_START` 定时器，把全场字段再重置一遍。bugfix-merged 证据：`frontend/src/views/competitions/CompetitionListView.vue:461`、`backend/apps/competitions/views.py:242-261`。
- **master 证据**（master@97e117e）：
  - `backend/apps/competitions/views.py:216-227`：`prev_status = fy.status` / `if "status" in serializer.validated_data: fy.status = serializer.validated_data["status"]` / … / `fy.save()` —— **没有 `CLOSED → ACTIVE` 的禁止分支，也没有状态机**（`models.py:27` 仅两个 choice）
  - `views.py:233-234`：`if prev_status != "ACTIVE" and new_status == "ACTIVE":` / `_apply_fiscal_year_timer(fy.competition_id, "FY_START")` —— 回切必然重跑 FY_START
  - `frontend/src/views/competitions/CompetitionListView.vue:461`：`await ElMessageBox.confirm("确定结束当前财年？此操作不可撤销", { type: "warning" });`
- **差异说明**：判 A，语义完全一致（文案行号在 master 上**同为 461**；后端段位 bugfix-merged `views.py:242-261` → master `212-237`）。master 无任何新增防护（既没有"已 CLOSED 不可回 ACTIVE"的后端校验，也没有二次确认）。补充一个 master 侧可验证的放大点：回切 ACTIVE 后前端可再次点"结束财年"，FY_START/FY_END 会交替重放，而每次重放都走 O-02 的同步定时器路径。
- **验证命令**（只读）：
  `grep -n "不可撤销" frontend/src/views/competitions/CompetitionListView.vue; grep -n "prev_status != \"ACTIVE\"\|prev_status != \"CLOSED\"" backend/apps/competitions/views.py`
- **置信度**：已验证

### O-19 登录限流只覆盖 `/api/auth/login`，键 (IP+用户名)，同一学生失败 10 次锁 15 分钟；锁定存进程内存，重启即清空 → master 状态：**A**

- **原件结论**：`LoginRateLimitMiddleware` 只拦 `POST /api/auth/login`，5 分钟窗口 10 次失败锁 15 分钟，键为 (IP+用户名)；锁定状态在进程内存，重启后端即清空。bugfix-merged 证据：`backend/apps/common/middleware.py`、`README.md:206`、`docs/OPS.md:158-160`。
- **master 证据**（master@97e117e）：
  - `backend/apps/common/middleware.py:203-205`：`_locks: dict[tuple[str, str], dict] = defaultdict(lambda: {"fails": 0, "first_at": 0.0, "locked_until": 0.0})`（**模块级进程内存**）
  - `middleware.py:206-208`：`_FAIL_WINDOW = 5 * 60` / `_FAIL_THRESHOLD = 10` / `_LOCK_DURATION = 15 * 60`
  - `middleware.py:230`：`key = (ip, username)`；`:245-249` `is_login_locked` 直接查内存
  - `middleware.py:252-256`：`class LoginRateLimitMiddleware(MiddlewareMixin):` / `"""仅拦截 POST /api/auth/login，锁定期间返回 429。"""` / `if request.method == "POST" and request.path == "/api/auth/login":`
  - `README.md:200`：`- **登录限流**：`LoginRateLimitMiddleware` **只拦截** `POST /api/auth/login`——同一 IP + 用户名在 5 分钟窗口内累计失败 10 次即锁定 15 分钟并返回 429。…锁定状态存进程内存，**重启后端即清空**。项目目前**没有**全局 HTTP 限流中间件`
- **差异说明**：判 A，机制与常量逐条一致（报告引 `README.md:206` → master 为 `README.md:200`，因为 master 少 6 行）。master 唯一新增的是一层内存卫生：`_cleanup_locks()`（`middleware.py:209-225`，10 分钟清理过期条目）防止 `_locks` 无限增长，但**不改变**"进程内存 + 重启清空 + 键=(IP+用户名)"三个被点名的性质。注意该键是 (IP+用户名)，故"共享 Wi-Fi 下同一学生的 10 次失败"才触发锁定，同 IP 的其他学生账号不受影响——原件表述与 master 一致。
- **验证命令**（只读）：
  `grep -n "_FAIL_WINDOW\|_FAIL_THRESHOLD\|_LOCK_DURATION\|key = (ip, username)\|request.path == \"/api/auth/login\"" backend/apps/common/middleware.py; grep -n "没有.*全局 HTTP 限流" README.md`
- **置信度**：已验证

### O-24 列表默认 pageSize=50、审计页 20 条/页；合同列表对非超管把整表拉进内存做范围过滤 → 大比赛翻页慢 → master 状态：**A**

- **原件结论**：`common/pagination.py` 默认 50；`AuditLogView.vue` 20 条/页；非超管的合同列表因需解析 JSON `parties` 无法下推 SQL，改为整表进内存过滤。bugfix-merged 证据：`backend/apps/common/pagination.py:9-11`、`frontend/src/views/system/AuditLogView.vue:166`、`backend/apps/contracts/views.py:205-216`。
- **master 证据**（master@97e117e）：
  - `backend/apps/common/pagination.py:9-11`：`MAX_PAGE_SIZE = 200` / `DEFAULT_PAGE = 1` / `DEFAULT_PAGE_SIZE = 50`（`:26` `int(query_params.get("pageSize", DEFAULT_PAGE_SIZE))`，`:34-35` 上限 200）
  - `frontend/src/views/system/AuditLogView.vue:166`：`const pageSize = 20;`
  - `backend/apps/contracts/views.py:196`：`needs_scope = _needs_scope_filter(request.user)`
  - `views.py:204-215`：`# 有范围限制：需解析 JSON parties 做内存过滤，无法下推到数据库（已知限制）。` / `all_rows = list(qs.order_by("-created_at"))` / `serialized = [_serialize_contract(c) for c in all_rows]` / `filtered = _filter_by_scope(serialized, request.user)` / `total = len(filtered)` / `page, page_size, skip = parse_pagination(request.query_params)` / `paged = filtered[skip : skip + page_size]`
  - 辅助：`views.py:733-752` `_needs_scope_filter(user)`（非超管持合同权限即 True）、`views.py:755-790` `_filter_by_scope(items, user)`
- **差异说明**：判 A，代码逐行同构（报告引 `contracts/views.py:205-216` ↔ master `:204-215`，同一段）。行号平移原因：**master 的 `backend/apps/contracts/views.py` 是 869 行，不是任务书说的 738 行**（LF 字节数交叉验证，`read` 工具报 869 行，末尾符号 `_recompute_calc_fields_safe` 在 `:862`）——报告描述的那段在 master 上确实存在。master 侧另有两条同类内存放大路径（同一缺陷的更多位置）：
  1. 增量分支 `views.py:178-193`：`updated_qs = qs.filter(**where)...` → `changed = [_serialize_contract(c) for c in updated_qs]`（把整个增量结果集物化后再内存过滤）；`requireExistingIds=true` 时再拉一次全量 `base_rows = list(qs.order_by("-created_at"))`（`:184-189`）。
  2. `contracts/views.py:157` 起始查询是 `Contract.objects.select_related("contract_type").all()`，范围过滤前无任何 SQL 级公司裁剪。
- **验证命令**（只读）：
  `grep -n "DEFAULT_PAGE_SIZE" backend/apps/common/pagination.py; grep -n "const pageSize" frontend/src/views/system/AuditLogView.vue; grep -n "all_rows = list(qs\|_filter_by_scope(serialized" backend/apps/contracts/views.py`
- **置信度**：已验证

### O-25 两次点击之间全场顶部栏短暂显示「未开启财年」 → master 状态：**A**

- **原件结论**：两个手工动作之间有窗口期，全场顶部栏短暂显示"未开启财年"（只有本地乐观更新，无占位状态）。bugfix-merged 证据：`frontend/src/views/competitions/CompetitionListView.vue:466`。
- **master 证据**（master@97e117e）：
  - `frontend/src/views/competitions/CompetitionListView.vue:465-466`：`// 乐观更新顶部栏财年显示（结束后无进行中财年，将显示“未开启财年”），随后回源校准` / `compStore.applyFiscalYearChange(selectedComp.value.id, null);`
  - `frontend/src/stores/competition.ts:161-162`：`} else if (fy.status === "CLOSED" && currentFiscalYear.value === fy.year) {` / `currentFiscalYear.value = null;`（**其他客户端**收到 `fiscal-year:changed` 广播后同样置空 → "全场"成立）
  - `frontend/src/components/layout/TopBar.vue:35`：`<el-tag v-else type="warning" size="small" effect="plain" class="ctx-tag">未开启财年</el-tag>`
- **差异说明**：判 A（同一行号 466、同一行为）。master 侧需要修正原件两个措辞：
  1. **不是"短暂"跳变，而是持续整个两次点击间隔**：`currentFiscalYear = null` 会一直保留到主持人点"开始新财年"（`CompetitionListView.vue:437-453` 成功后才置为新财年），中间可能是几十秒到几分钟。
  2. master 已有三态保护 `fiscalYearLoading`（`stores/competition.ts:35-36,104-105,134-136`；`TopBar.vue:19-26` 渲染"财年加载中…"），它抑制的是**首次加载/切比赛**时的 null 跳变，与"两次点击之间"这一真实状态无关。因此剩余问题只是"两段式流程 + 真实无财年状态"的体验/文案问题（P3），不是竞态。
- **验证命令**（只读）：
  `grep -n "applyFiscalYearChange(selectedComp.value.id, null)" frontend/src/views/competitions/CompetitionListView.vue; grep -n "currentFiscalYear.value = null" frontend/src/stores/competition.ts; grep -n "未开启财年\|fiscalYearLoading" frontend/src/components/layout/TopBar.vue`
- **置信度**：已验证

---

## 汇总统计

| 结论 | 条数 | 条目 |
| --- | --- | --- |
| A 仍存在 | **6** | O-02、O-13、O-17、O-19、O-24、O-25 |
| B 存在但形态/严重度不同 | **1** | O-04（其余子句成立，"全部同步视图共用一个线程"在 master 上被证伪 → 机制与量化需改写；同时发现 `.update()` 路径不产生审计 INSERT 的反向差异） |
| C master 上无法命中 | **0** | —（只有 O-04 的一个**子结论**属 C：`django/core/handlers/base.py` 单线程语义） |
| D 已修复 | **0** | — |

按原件严重级看：P0 两条 = 1×A（O-02）+ 1×B（O-04）；P1 一条 = A（O-13，且合同侧更重）；P2 三条 = 全 A（O-17/O-19/O-24）；P3 一条 = A（O-25）。**没有任何一条在 master 上被修复**，其中 O-13 的合同执行审计在 master 上比原件描述的更缺。

## 已核对无问题或无法判定

**无法判定 / 依赖外部条件：**

1. **bugfix-merged 侧的具体行号、文件规模、库内数据**（`audit_log` 7118 行、`timer_enabled=0`、`fiscal_years` 两行等）无法在 master 侧核对——本报告只把原件当作"结论来源 + 行号基准"，未读取 bugfix-merged 树；所有引用均已标注 (bugfix-merged)。
2. **现场量化结论不可复核**：100 客户端稳态 5 req/s、重连一次产生 ~1100 请求、财年定时器耗时是否 >120s，均需部署 + 写库 + 压测才能实测；原件自己也标注为"结构性推断"（其 §5 E-5）。本报告只能确认各**机制**在 master 上存在/不存在。
3. **O-04 的线程语义依赖 asgiref 版本**：master `backend/requirements.txt` 只锁 `Django==5.0.14`，**未锁 asgiref**（Django 元数据要求 `asgiref<4,>=3.7.0`）；本机 venv 为 asgiref 3.12.1。"每请求一个 thread-sensitive 执行器"自 asgiref 3.2（`ThreadSensitiveContext`，Django 3.1 起在 `ASGIHandler.__call__` 中按请求进入）即成立，但若生产锁到异常版本需按 `python -c "import asgiref; print(asgiref.__version__)"` 复核。此条判为**推断**（依赖框架版本，非仓库代码）。
4. **master 的 `gipfel.service` 是否真在生产以该形态启动**：仓库模板正确（单 ExecStart），但部署产物是 `scripts/deploy-linux.sh` 渲染后的 `/etc/systemd/system/gipfel.service`，是否与模板一致需在部署机上 `systemctl cat gipfel` 复核（本报告只读仓库）。
5. **O-25 的"全场"范围**：已确认广播链路（`views.py:29-42` → `stores/competition.ts:150-166`）覆盖同比赛的在线客户端；未登录/未订阅该比赛房间的客户端不在内（不影响结论）。

**已核对、无额外问题（master 侧）：**

1. **"无 ACTIVE 唯一约束"不是"约束被绕过"而是压根没有**：`FiscalYear.STATUS_CHOICES` 仅 `ACTIVE`/`CLOSED`（`models.py:27`），`Meta` 只有 `(competition, year)` 唯一（`:41`）→ O-02 的定性成立。
2. **"审计只增不删"不是"有命令没跑"**：`backend/apps/audit/` 目录下无 `management/`，`urls.py:7-9` 只有列表路由 → master 没有任何清理/归档入口。
3. **审计"按公司过滤做不到"在模型层即成立**：`AuditLog` 无 company 字段（`models.py:5-33`），只有 `competition_id`/`record_id`；视图也只开放四个过滤项（`views.py:29-49`）。
4. **单进程确为 master 唯一形态**：`deploy/gipfel.service` 全文 55 行、单 `ExecStart`；仓库内无 `--workers`/`num-procs`/多进程扩容配置（`grep -rn "workers\|num-procs" deploy scripts docs` 无相关命中）→ O-04 的"单进程 daphne"成立。
5. **nginx 侧确无任何限流**：`deploy/nginx-gipfel.conf` 中 `limit_req`/`limit_conn` 0 命中，只有一个 upstream（`:9-13`）→ O-04 的"无 nginx 限流"成立。
6. **审计页 20 条/页确已生效**：`AuditLogView.vue:166` 定义、`:102-103` 传入 el-pagination 的 `:page-size`/`:total`（不是定义了没用）。
7. **报告的行号"错位"是文件版本差异而非引用错误**：本任务 7 条在 master 上都能定位到对应代码（多为行号平移，O-17/O-19/O-24/O-25 甚至同行号），未出现"master 无此文件/无此逻辑"的情形。
8. **任务书中的两处前提需要修正**（已在本报告方法学提醒与 O-24 中给出证据）：`backend/apps/contracts/views.py` master 为 **869 行**（非 738 行）；`deploy/gipfel.service` 的 ExecStart 在 master 为 `:18-24`（bugfix-merged 的 `:26-31` 是版本差异）。
