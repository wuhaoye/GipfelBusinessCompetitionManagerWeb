# M3 master 对照：权限 / 角色 / 账号 / 会话类（O-01 / O-06 / O-08 / O-10 / O-12 / O-27 / O-28）

## 对照对象

- **原件**：`OPS_READINESS_REPORT.md`（《Gipfel 商赛系统 · 现场运营就绪度评估报告（OPS READINESS）》），编写于 **bugfix-merged @ 5cb3468**（比 master 多 357 文件 / +62k 行）。本报告中凡引用其行号/结论一律标注 **(bugfix-merged)**。
- **目标**：**master@97e117e8781e0c02462a24269a165dde08ecef44**
- **master 快照（唯一真源）**：`C:\Users\wuhao\AppData\Local\Temp\dsh-Ec2sca\gipfel-master-lf`（LF、CR=0、439 文件，逐字节等于 master blob）。下文 `$SNAP` 即指该目录，所有「验证命令」均以 `$SNAP` 为根，只读。
- 已核实 master **不存在**：`backend/apps/preparation/`、`contract_watcher/`、`code_audit/`（`Get-ChildItem $SNAP/backend/apps -Directory` 列出 27 个 app，无 preparation）。

## 方法

1. **只读**：仅 `read`/`grep`/`glob` + 只读文本比对；未运行任何脚本、未改任何代码或配置。唯一写入 = 本文件。
2. **行号一律重核**：原件行号只在标注「(bugfix-merged)」时原样引用；作为 master 证据的 `file:line` 全部来自 master 快照（新 LF 快照）。
3. **不采用原件 §5 E-1 的角色能力矩阵探针结论**——那是在 bugfix-merged 的库里实跑的，不能当作 master 结论。master 的权限派生改为**读码推导**，推导链如下（每条结论都可独立复核）：
   - `PERMISSION_CATALOG`（`backend/apps/common/permissions.py:30-202`）→ 39 个 key（`ALL_PERMISSION_KEYS`，`:204`）；
   - `ROLE_TEMPLATES`（`:334-366`，含 `defaultPermissions`/`grantCeiling`/`grantExtras`）**只在 `assert_grant_allowed`(`:369-402`) 里被读取**（`grep -rn "ROLE_TEMPLATES" $SNAP` 仅命中 `permissions.py:334`、`:384`），**运行时授权不读它**；
   - 运行时真源 = `User.permissions`（DB JSON 字符串）→ `User.permissions_list`（`backend/apps/users/models.py:100-110`）→ `has_permission(role, perms, required)`（`permissions.py:253-291`，仅 `SUPER_ADMIN` 隐式全放行）；
   - 所有消费点都直接读 `user.permissions_list`，无任何「空则按角色继承」的兜底（`grep -rn "permissions_list" $SNAP/backend` 25 处，逐一确认无角色兜底）。
   - **结论：master 上非超管的有效权限 = DB 里那份数组本身；`role` 只决定「授予上限」，不决定「有效权限」。**
4. 分类 **A 仍存在 / B 存在但形态或严重度不同 / C master 上无法命中（并区分「没这个问题」还是「能力更缺」）/ D 已修复**。
5. 置信度：**已验证** = 可直接读码/只读命令复核；**推断** = 依赖运行环境或框架语义。

## 总览表

| ID | 原件结论（bugfix-merged） | master 状态 | 一句话 |
| --- | --- | --- | --- |
| O-01 | P0 选手无任何写权限、业务全靠 staff 代做、唯一能填字段的超管又单会话 | **B（更重）** | 选手无写权限照旧；且 master 的「扩展权限」**根本授不出去**（无 `allowExtras` 通道），原件给的 API 绕过方案在 master 上不成立 |
| O-06 | P1 超管单会话（登录即顶号） | **A（范围更广）** | 顶号机制逐行一致，但 master 对**全体角色**生效，非超管专属；`contract_watcher` 那半条 → C |
| O-08 | P1 无产业隔离 / 无只读审计 / 审计看不到审计日志 / 无评估机构 | **A** | 四条全部在 master 复现，并多一处前端路由级收紧 |
| O-10 | P1 界面只有 3 角色、无权限勾选、硬编码派生、保存静默清掉扩展权限 | **B** | 前三项 A 级复现；第四项在 master 上**不可达**（扩展权限无法授予）→ 问题从「被清掉」变成「根本授不出去」 |
| O-12 | P1 签到高峰 bcrypt 串行 | **A（略重）** | cost=12、强制改密、顶号写库全在；master 的改密接口是 **2 次** bcrypt（校验+哈希），比原件描述更重 |
| O-27 | P3 无赛前一键自检入口 | **C（能力更缺）** | master 连 preparation 应用/清单都没有，只剩 `/api/health`、`/api/version` 两个探针 |
| O-28 | P3 文案无操作指引、401/超时只有统一 toast | **A** | `未开启财年` 无指引（且多一处 Sidebar），错误提示仍是固定文案表 |

---

### O-01 选手账号无任何写权限，全场业务动作必须 staff 代做 → master 状态：**B（仍存在，且比原件更重）**

- **原件结论（bugfix-merged）**：`PLAYER` 只读（唯一可写是 `stock:view` 下单撤单）；写公司字段要 `company:manage`（属扩展集，「超管之外默认无人持有」）；建合同要 `contract:manage`、执行要 `contract:audit`；唯一能填字段的 `SUPER_ADMIN` 又单会话。绕过方案是「赛前用 API `POST /api/users` 带 `"allowExtras": true` 授 `company:manage`」。证据位置 **(bugfix-merged)**：§4 P0 表 O-01、§2.6 `188-199` 行；`company_fields/views.py:141,167`、`contracts/views.py:218,297`、`auth/views.py:161-168`、`AccountManagementView.vue:296-312`；§5 E-1 探针。
- **master 证据**（master@97e117e）：
  - `backend/apps/common/permissions.py:326-332` —— `_COMPETITION_ADMIN_EXTRAS = [` / `    "message:manage",` / `    "contractType:manage",` / `    "industryType:manage",` / `    "company:manage",` / `    "data:region:edit",` / `]`
  - `backend/apps/common/permissions.py:342-347` —— `        "defaultPermissions": BASE_VIEW_PERMISSIONS + [` / `            "contract:manage",` / `            "contract:audit",` / `            "contract:execute",` / `            "stock:edit",` / `        ],`（**`COMPETITION_ADMIN` 默认不含 `company:manage`**）
  - `backend/apps/common/permissions.py:361-363` —— `        "defaultPermissions": BASE_VIEW_PERMISSIONS,` / `        "grantCeiling": BASE_VIEW_PERMISSIONS,` / `        "grantExtras": [],`（`PLAYER` 上限 = 17 个 `*:view`，**无任何写权限**）
  - `backend/apps/common/permissions.py:389-401` —— `    ceiling = set(template["grantCeiling"])` … `        if perm not in ceiling:` / `            if perm in extras:` / `                violations.append(f"{perm} 在扩展集中，需超管显式放开")` → **扩展权限也被计为 violations**，`allowed = len(violations) == 0` 恒 False；函数签名 `assert_grant_allowed(actor_role, target_role, permissions)`（`:369`）**没有 `allowExtras` 参数**，全仓库 `grep -rn "allowExtras" $SNAP` **0 命中**。
  - `backend/apps/users/views.py:20` + `:35-42` + `:38` —— `from apps.common.permissions import assert_grant_allowed` / `    allowed, violations = assert_grant_allowed(` / `        raise BusinessError("；".join(violations), code=400, status_code=400)`（**唯一调用点**，即 `POST /api/users`、`PATCH /api/users/:id`、`POST /api/users/:id/permissions` 都过同一道闸）
  - 写接口权限键：`backend/apps/company_fields/views.py:25-26`（`_VIEW_PERM = "company:view"` / `_MANAGE_PERM = "company:manage"`）、`:141`、`:167`；`backend/apps/contracts/views.py:46-48` + `:217-218`（建合同 `_CONTRACT_MANAGE_PERM`）+ `:296-297`（执行 `_CONTRACT_AUDIT_PERM`）；`backend/apps/consumer_demands/views.py:37-38` + `:123-124`（`data:region:edit`）；`backend/apps/messages/views.py:323-324`（发消息 `message:manage`）
  - 运行时校验只认 DB 数组：`backend/apps/common/guards.py:81-85` —— `        perms = user.permissions_list if hasattr(user, "permissions_list") else []` / `        if not has_permission(getattr(user, "role", None), perms, list(required)):` / `            raise PermissionDenied("没有权限执行此操作")`
  - 创建账号不带 `permissions` 时不会补角色默认：`backend/apps/users/serializers.py:103-119`（`create()` 只写显式传入的 JSON 字段）+ `backend/apps/users/models.py:100-105`（`if not self.permissions: return []`）
- **差异说明**：核心结论在 master **完全成立且更重**：① `COMPETITION_ADMIN` 默认不持有 `company:manage`（与原件一致）；② `PLAYER` 的 `grantCeiling` = 17 个 view，无任何写权限（一致）；③ **master 多出一条致命差异——扩展集在设计上无法授予**：`assert_grant_allowed` 把 extras 也算作 violation，且没有任何 `allowExtras` 逃生通道（原件所说的 API 绕过方案在 master 上会直接 400「company:manage 在扩展集中，需超管显式放开」）。因此在 master 上 `company:manage` / `message:manage` / `data:region:edit` / `contractType:manage` / `industryType:manage` **除了 `SUPER_ADMIN` 谁都拿不到**，「一个超管在主席台逐家录数据」由建议变成了唯一可行方式。④ 附带差异：master **没有**「按角色继承权限」的运行时兜底——`ROLE_TEMPLATES.defaultPermissions` 是死数据，用 API 建 `PLAYER`/`COMPETITION_ADMIN` 而不显式传 `permissions` 会得到一个**零权限账号**（连 `*:view` 都没有）；实际能工作只是因为前端 `AccountManagementView.vue:457-485` 总会把派生数组一起提交（见 O-10）。这也是原件 §5 E-1「PLAYER 有 17 个 view」在 master 上**不能直接照搬**的原因。
- **验证命令**：`grep -rn "allowExtras" $SNAP | wc -l; sed -n '326,332p;342,347p;357,365p;369,402p' $SNAP/backend/apps/common/permissions.py; sed -n '81,85p' $SNAP/backend/apps/common/guards.py; grep -rn "ROLE_TEMPLATES" $SNAP/backend`
- **置信度**：已验证（纯读码推导；`allowExtras` 0 命中、extras 计入 violations、运行时只读 `permissions_list` 三处均可直接复核）。原件 §5 E-1 探针结论**未采信**。

### O-06 超管单会话：登录即递增 `token_version` 并断开旧会话 → master 状态：**A（仍存在；master 上对全体角色生效，非超管专属）**

- **原件结论（bugfix-merged）**：`SUPER_ADMIN` 每次登录递增 `token_version` 并强制断开该账号所有旧会话 → 组委会多人无法同时操作；`contract_watcher` 与浏览器用同一账号会互踢循环。证据位置 **(bugfix-merged)**：§4 P1 表 O-06、§2.6 `193` 行、§2.8 `237` 行；`auth/views.py:161-168`、`contract_watcher/contract_watcher.py:607-614`。
- **master 证据**（master@97e117e）：
  - `backend/apps/auth/views.py:156-158` —— `        # 顶号下线：递增 token_version，旧 token 立即失效` / `        user.token_version = (user.token_version or 0) + 1` / `        user.save(update_fields=["token_version", "updated_at"])`（**在 `LoginView.post` 里，对所有角色无条件执行**）
  - `backend/apps/auth/views.py:160-162` —— `        from apps.realtime.emit import emit_to_users` / `        emit_to_users([user.id], "auth:required", {"reason": "token_version_mismatch"})`
  - `backend/apps/auth/authentication.py:110-114` —— `        # 顶号下线：token 中 tv 与用户当前 token_version 不一致` / `        if payload.get("tv") != user.token_version:` / `            raise exceptions.AuthenticationFailed(` / `                "账号已在其他设备登录", code="token_version_mismatch"`
  - `backend/apps/realtime/gateway.py:125-126`、`:139` —— `                "auth:required",` / `                {"reason": "token_version_mismatch"},` 与 `            {"message": "auth_required", "reason": "token_version_mismatch"}`
  - `frontend/src/realtime/socket.ts:75-76` —— `  socket.on("auth:required", (payload: { reason?: string }) => {` / `    if (payload && payload.reason === "token_version_mismatch") {`
  - `README.md:217` —— `改密成功后 `must_change_password` 置为 False，同时**递增 `token_version` 吊销所有旧 token**（安全设计，防旧凭据残留）…**其他设备被正确踢下线**。`（master 自述这是**有意设计**）
- **差异说明**：机制逐条一致（登录写库 + JWT `tv` 比对 + WebSocket 广播三重），**结论 A**。差异在范围：原件把这条限定为「超管单会话」，master 的实现位于通用 `LoginView`，**任何账号登录都会踢掉自己的旧会话**（对选手同样是「换个浏览器就被踢」）。原件提到的 `contract_watcher` 互踢循环在 master 上**无法命中（C）**：master 无 `contract_watcher/` 目录，但同时也没有任何「服务端长驻客户端」，因此该具体冲突场景不存在（属「能力缺失导致的场景消失」，不是「已修」）。
- **验证命令**：`sed -n '152,168p' $SNAP/backend/apps/auth/views.py; sed -n '108,124p' $SNAP/backend/apps/auth/authentication.py; grep -rn "auth:required" $SNAP/backend $SNAP/frontend/src/realtime`
- **置信度**：已验证（`README.md:217` 与三处代码互相印证）。

### O-08 真实角色无账号类型：无产业维度隔离、无只读审计、审计看不到审计日志、无资产评估机构 → master 状态：**A（全部仍存在，另多一处前端路由收紧）**

- **原件结论（bugfix-merged）**：只有 `COMPETITION_ADMIN`，`companyScopes` 只能按公司勾选、无产业维度；`COMPETITION_ADMIN` 默认同时持有 `contract:manage`+`contract:execute`（审计与执行无职责分离）；`GET /api/audit-logs` 要求 `account:manage`（超管专属）→ 审计看不到审计日志；完全没有资产评估机构模型。证据位置 **(bugfix-merged)**：§4 P1 表 O-08、§2.6 `173-181` 行；`common/permissions.py:298-366`、`audit/views.py:25`、`RegionOverviewView.vue:183`、`consumer_demands/views.py:37-38`。
- **master 证据**（master@97e117e）：
  - 角色枚举只有 3 个：`backend/apps/users/models.py:30-34` —— `    ROLE_CHOICES = [` / `        ("SUPER_ADMIN", "SUPER_ADMIN"),` / `        ("COMPETITION_ADMIN", "COMPETITION_ADMIN"),` / `        ("PLAYER", "PLAYER"),` / `    ]`
  - 范围字段全部是「公司 id 列表」，无产业维度：`backend/apps/users/models.py:49-52` —— `    company_scopes = models.TextField(null=True, blank=True)` / `    view_company_scopes = …` / `    contract_view_company_scopes = …` / `    stock_company_scopes = …`；前端派生同样只吃公司数组：`frontend/src/views/account-management/AccountManagementView.vue:258-260` —— `function derivePermissions(` / `  role: string,` / `  companies: number[],`
  - 审计与执行未分离：`backend/apps/common/permissions.py:342-347`（`COMPETITION_ADMIN` 默认同时持 `contract:manage` + `contract:audit` + `contract:execute`）；扩展集 `_COMPETITION_ADMIN_EXTRAS`（`:326-332`）里也没有任何「只读审计」模板
  - 审计日志仅超管：`backend/apps/audit/views.py:20-26` —— `class AuditLogListView(APIView):` / `    permission_classes = _PERM_CLASSES` / `    @require_permissions("account:manage")` / `    def get(self, request):`
  - 前端同向收紧：`frontend/src/router/index.ts:221-226` —— `          path: "audit-logs",` … `          // 审计日志仅超管可查看（后端 GET /audit-logs 要求 account:manage，前端同步收紧为超管）。` / `          meta: { title: "审计日志", requiresSuperAdmin: true },`（守卫 `:289`）
  - 无资产评估机构：`grep -rn "评估|appraisal|valuation|固定资产|折旧" $SNAP/backend` 仅 2 处命中，均为「评估前置检查」动词用法（`backend/apps/contracts/engine.py:11`、`:2040`），无模型/页面/角色
- **差异说明**：四条子结论在 master 上**逐条复现**（行号已重核：`audit/views.py:25` 与原件标注的行号恰好相同，属巧合而非沿用）。master 新增可核证据一条：审计不可见在**前端路由层**也被显式收紧为 `requiresSuperAdmin`（`router/index.ts:225`），即非超管连页面入口都没有，比原件描述更彻底。`companyScopes` 无产业维度同样由「模型字段 + 前端派生签名」双向印证。
- **验证命令**：`sed -n '20,26p' $SNAP/backend/apps/audit/views.py; sed -n '30,34p;49,52p' $SNAP/backend/apps/users/models.py; sed -n '221,226p' $SNAP/frontend/src/router/index.ts; grep -rn "评估\|appraisal\|固定资产\|折旧" $SNAP/backend`
- **置信度**：已验证。

### O-10 账号界面只有 3 个角色、无权限勾选、`derivePermissions` 硬编码；界面保存会静默清掉手工授予的扩展权限 → master 状态：**B（前三项仍存在；第四项在 master 上不可达）**

- **原件结论（bugfix-merged）**：角色下拉只有三项；没有权限勾选框（权限由 `derivePermissions(role, companies)` 硬编码派生）；编辑保存固定提交 `permissions: derived.permissions`，后端用「提交值或既有值」做上限校验即通过 → 之前用 API 授的扩展权限被覆盖删除且无提示。证据位置 **(bugfix-merged)**：§4 P1 表 O-10、§2.6 `183-186` 行；`AccountManagementView.vue:133-142,245-313,457-467`、`users/views.py:36-53,129-134`。
- **master 证据**（master@97e117e）：
  - 角色下拉三项：`frontend/src/views/account-management/AccountManagementView.vue:133-141` —— `        <el-form-item label="角色" prop="role">` / `          <el-select v-model="form.role">` … `            <el-option label="管理员" value="COMPETITION_ADMIN" />` / `            <el-option label="参赛选手" value="PLAYER" />`
  - 硬编码派生：`…AccountManagementView.vue:258-313`（`function derivePermissions(` … `if (role === "SUPER_ADMIN")` … baseView 17 项 `:277-295` … `if (role === "COMPETITION_ADMIN")` `:296-303` … PLAYER `:305-312`）；页面无任何权限勾选控件（同文件 grep `checkbox` 0 命中）
  - 保存固定提交派生值：`…AccountManagementView.vue:457-467` —— `      const derived = derivePermissions(form.role, managedCompanies.value);` … `          permissions: derived.permissions,`；创建同样（`:474`）
  - 后端「提交值或既有值」校验：`backend/apps/users/views.py:118-124` —— `        effective_perms = serializer.validated_data.get(` / `            "permissions", user.permissions_list` / `        )` / `        _assert_grant(request, effective_role, effective_perms)` / `        serializer.save()`
- **差异说明**：三条界面限制在 master 上**逐条成立（A 级）**，行号与原件高度重合但已逐行重核。第四项「静默清掉手工授予的扩展权限」在 master 上**无法复现（C 类）**：扩展权限（`company:manage`/`data:region:edit`/`message:manage`/`contractType:manage`/`industryType:manage`）经 `assert_grant_allowed` 一律拒绝（见 O-01），根本不存在「已经授出去的扩展权限」可被清掉；同时 `COMPETITION_ADMIN`/`PLAYER` 的 `grantCeiling` 与前端派生的集合**完全相等**，界面编辑最多把权限「补齐回角色默认」，不会造成权限丢失（除非有人直接改库）。因此本条的形态在 master 上发生位移：**风险从「自定义权限被界面抹掉」变成「自定义权限压根授不出去」**（后者计入 O-01）。综合判 **B**。
- **验证命令**：`sed -n '133,141p;258,313p;451,485p' "$SNAP/frontend/src/views/account-management/AccountManagementView.vue"; sed -n '113,125p' $SNAP/backend/apps/users/views.py`
- **置信度**：已验证。

### O-12 签到高峰：100 人 ×（bcrypt cost=12 登录 + 强制改密再一次哈希 + 顶号写库）全部串行 → master 状态：**A（仍存在；master 的改密路径是 2 次 bcrypt，比原件更重）**

- **原件结论（bugfix-merged）**：`bcrypt cost=12` 登录、强制改密再一次哈希、每次登录顶号写库，全部串行在单线程；强制改密期间业务接口 401。证据位置 **(bugfix-merged)**：§4 P1 表 O-12；`users/models.py:81-85`、`auth/views.py:161-168`、`README.md:223`。
- **master 证据**（master@97e117e）：
  - `backend/apps/users/models.py:80-87` —— `    def set_password(self, raw_password):` / `        """用 bcrypt cost=12 哈希，与原 bcryptjs 兼容。"""` / `        salt = bcrypt.gensalt(rounds=12)`；`bcrypt==4.1.3`（`backend/requirements.txt`）
  - 登录 1 次 bcrypt + 1 次写库：`backend/apps/auth/views.py:138` —— `        if user is None or not user.check_password(password):`；`:157-158`（`token_version` 递增写库）
  - 改密 **2 次** bcrypt + 1 次写库：`backend/apps/auth/views.py:245` —— `        if not user.check_password(old_password):`；`:250` —— `        user.set_password(new_password)`；`:251-261`（置 `must_change_password=False`、递增 `token_version`、一次 `save`）
  - 强制改密链路：`backend/apps/users/views.py:83-84`（创建账号默认 `mustChangePassword=True`）、`backend/apps/auth/authentication.py:116-122`（`must_change_password` 时除 `/api/auth/change-password` 外全部 401）、`frontend/src/stores/auth.ts:62`（`const needsPasswordChange = computed(() => !!user.value?.mustChangePassword);`）
  - 单线程前提：`backend/backend/settings.py:300` —— `ASGI_APPLICATION = "backend.asgi.application"`（Django 5.0.14 + daphne 4.1.2，同步 `APIView` 经 `sync_to_async(thread_sensitive=True)` 落在**同一个**执行器线程；沿用原件 §5 E-4 的框架语义，master 侧仅复核版本与 ASGI 入口）
- **差异说明**：分类 **A**。三点均成立，且 master 有一个**更重**的细节：改密接口是 `check_password`（1 次 bcrypt 比对）+ `set_password`（1 次 bcrypt 哈希）= **2 次 cost=12 运算**，而原件描述为「再一次哈希」；把「首次登录 → 强制改密」算作一个学生的入场动作，master 上合计 **3 次 cost=12 bcrypt + 2 次 SQLite 写**（登录比对、改密比对、改密哈希；登录与改密各一次 `token_version` 写）。另：master 的改密成功响应直接回签新 token（`auth/views.py:263-269`），前端无需二次 `/login`，因此**没有**额外的第 4 次 bcrypt——这一点与原件「改密后重登」的隐含路径不同，属减轻项，已计入。单线程部分标注为框架语义（非本仓库代码），置信度：推断。
- **验证命令**：`sed -n '80,88p' $SNAP/backend/apps/users/models.py; sed -n '136,140p;244,262p' $SNAP/backend/apps/auth/views.py; grep -n "Django==\|daphne==\|bcrypt==" $SNAP/backend/requirements.txt; grep -n "ASGI_APPLICATION" $SNAP/backend/backend/settings.py`
- **置信度**：bcrypt 次数与写库次数为**已验证**；「全部串行」为**推断**（依赖 Django `thread_sensitive` 框架语义，需目标环境实测并发才能定量）。

### O-27 无赛前「一键自检」入口（准备总览要超管进设置页点三轮）→ master 状态：**C（master 上无法命中；属「能力更缺」而非「没这个问题」）**

- **原件结论（bugfix-merged）**：没有赛前一键自检入口；准备总览要超管进设置页点三轮才能看到。证据位置 **(bugfix-merged)**：§4 P3 表 O-27；`SettingsView.vue:29-31`（对应 bugfix-merged 的 preparation 能力）。
- **master 证据**（master@97e117e）：
  - master **没有** `backend/apps/preparation/`（`Get-ChildItem $SNAP/backend/apps -Directory` 27 项无 preparation），全仓库 `grep -rn "自检|checklist" $SNAP/backend` **0 命中**
  - 未鉴权探针只有两个：`backend/backend/urls.py:16-18` —— `    # 健康检查与版本（无鉴权，对应原 health.controller / version.controller）` / `    path("api/health", HealthView.as_view(), name="health"),` / `    path("api/version", VersionView.as_view(), name="version"),`
  - 设置页实际内容：`frontend/src/views/settings/SettingsView.vue:15-17` —— `      <h3>后端管理</h3>` / `        <el-button type="danger" @click="openAdmin">后端管理界面</el-button>` / `        <el-button type="warning" @click="openLogViewer">日志查看器</el-button>`；其余为「管理更新公告」(`:26`)、「管理控件包」(`:93`) 两个超管弹窗
  - 设置页本身对所有登录用户开放（区块用 `v-if="isSuperAdmin"` 隐藏）：`frontend/src/router/index.ts:212-218` —— `          path: "settings",` … `          meta: { title: "系统设置" },`
- **差异说明**：分类 **C**，并且是**能力更缺**那一侧：原件所说的「准备总览（39 项清单）要点三轮」在 master 上**不存在该功能**——没有 preparation 应用、没有清单、没有任何自检端点，设置页只有「后端管理/日志查看器/更新公告/控件包」四块。也就是说 master 不是「入口藏得深」，而是「连被藏的入口都没有」；运维在 master 上只能靠 `curl /api/health`、`curl /api/version` 与 `docs/OPS.md` §5 的手工 curl（原件 §3 表格里那条「比赛准备清单（39 项）✅ 内容质量高」在 master 上不成立）。这一条应与 M4 的 preparation 类条目合并看，避免把「原件有功能但不好用」误读为「master 也有功能」。
- **验证命令**：`Get-ChildItem $SNAP/backend/apps -Directory | Select-Object -ExpandProperty Name; grep -rn "preparation\|checklist\|自检" $SNAP/backend | wc -l; sed -n '14,20p' $SNAP/frontend/src/views/settings/SettingsView.vue; sed -n '212,219p' $SNAP/frontend/src/router/index.ts`
- **置信度**：已验证（目录枚举 + 全仓 grep 0 命中，均只读可复现）。

### O-28 「未开启财年」等文案无操作指引；学生面对 401/超时只有统一 toast → master 状态：**A（仍存在；提示位置更多一处）**

- **原件结论（bugfix-merged）**：「未开启财年」等文案没有「请等待主席团推进」之类操作指引；学生遇到 401/超时只看到统一 toast。证据位置 **(bugfix-merged)**：§4 P3 表 O-28；`TopBar.vue:33-35`。
- **master 证据**（master@97e117e）：
  - `frontend/src/components/layout/TopBar.vue:35` —— `          <el-tag v-else type="warning" size="small" effect="plain" class="ctx-tag">未开启财年</el-tag>`（仅一个标签，无任何 tooltip/说明文案）
  - 同一文案在侧边栏也出现：`frontend/src/components/layout/Sidebar.vue:30` —— `              <el-tag v-else type="warning" size="small" effect="plain">未开启财年</el-tag>`
  - 无操作指引：`grep -rn "请等待|主席团" $SNAP` 中「主席团」只出现在合同编辑器/合同页的角色名（`ContractTypeGraphEditor.vue:179`、`ContractManageView.vue:61,63,114,179,422,463`），不构成对学生的指引；「请等待」**0 命中**
  - 统一 toast：`frontend/src/api/request.ts:45-72`（`export function getErrorMessage(...)` + 固定文案表 `401: "登录已过期，请重新登录"` `:59`、`504: "网关超时，请稍后重试"` `:67`、兜底 `:71` `  return "网络错误，请检查网络连接或服务器是否启动";`），触发点 `:91`、`:122`、`:145`、`:151` 均为 `ElMessage.error(...)`
- **差异说明**：分类 **A**。文案与提示形态与原件一致；master 的 toast 机制比原件描述**更复杂但结论不变**——`request.ts:74-153` 有「迟到 401 判定」「改密自动重登窗口抑制 401」等分支，最终仍是固定文案表（`getErrorMessage`），没有「该找谁/怎么恢复」的指引；「未开启财年」在 master 上还多出现在 `Sidebar.vue:30`（原件只点了 TopBar），即学生看到的无指引提示位**多一处**。
- **验证命令**：`grep -rn "未开启财年" $SNAP/frontend/src; sed -n '45,72p' $SNAP/frontend/src/api/request.ts; grep -rn "请等待" $SNAP | wc -l`
- **置信度**：已验证。

---

## 汇总统计

| 状态 | 条数 | 条目 |
| --- | --- | --- |
| **A 仍存在** | 4 | O-06、O-08、O-12、O-28 |
| **B 形态/严重度不同** | 2 | O-01（更重：扩展权限不可授予 + 无角色默认兜底）、O-10（第四项不可达，风险位移到 O-01） |
| **C master 无法命中** | 1 | O-27（**能力更缺**：无 preparation/清单/自检端点；非「没这个问题」） |
| **D 已修复** | 0 | — |
| 合计 | 7 | |

补充口径：O-06 的后半条（`contract_watcher` 与浏览器互踢）在 master 上为 **C（能力缺失导致场景消失）**，已在该条内单独说明，未计入 C 计数（该条整体仍为 A）；O-10 的第四项同理记 C 并计入该条差异说明。

## 已核对无问题或无法判定

1. **`PERMISSION_CATALOG` 的 39 个 key 与原件一致**：`ALL_PERMISSION_KEYS`（`permissions.py:204`）逐域计数 = 39（17 个 `*:view` + 其余动作键），与原件 §2.6「39 个权限键」一致 → 该数字可直接沿用。验证：`sed -n '30,204p' $SNAP/backend/apps/common/permissions.py`。
2. **动作蕴含语义与原件一致**：`DEFAULT_ACTION_RANKS`（`permissions.py:12-18`）与合同域 `CONTRACT_ACTION_RANKS`（`:21-26`，`manage ⊇ execute ⊇ audit ⊇ view`）与 `has_permission` 的「域内取最高动作等级比较」实现（`:278-290`）一致；因此 `COMPETITION_ADMIN` 持 `contract:execute`（rank 30）即自动满足 `contract:audit`（rank 20）——这也是 O-08「审计与执行未分离」成立的机制原因。验证：`sed -n '12,26p;253,291p' $SNAP/backend/apps/common/permissions.py`。
3. **超管专属权限不可下放**：`SUPER_ADMIN_ONLY_PERMISSIONS = ["competition:manage","account:manage","stock:manage"]`（`permissions.py:319-323`）+ `assert_grant_allowed:394-396` 直接判 violation → 非超管无法拿到这三个键（含 `account:manage`）。与原件 §2.6「超管隐式全权」一致，未发现越权通道。验证：`sed -n '319,323p;392,401p' $SNAP/backend/apps/common/permissions.py`。
4. **`assert_grant_allowed` 只允许超管改权限**：`permissions.py:375-376` —— `    if actor_role != "SUPER_ADMIN":` / `        return False, ["仅超管可修改权限"]`；调用侧 `users/views.py:35-42` 把 violations 拼成 400。因此「非超管自助提权」不成立。验证：`sed -n '369,382p' $SNAP/backend/apps/common/permissions.py`。
5. **比赛域隔离未被本次对照发现缺口**：`guards.py:112-127`（非超管写操作必须有比赛上下文）、`:130-145`（`create_competition_id` 忽略请求体 `competitionId`，强制用自身比赛）、`:179-195`（`apply_competition_scope` 非超管强制按自身比赛过滤，无上下文返回空集）逻辑闭合，与 O-08「公司范围」是**两个不同维度**（比赛域 vs 公司范围），不要混为一谈。验证：`sed -n '105,195p' $SNAP/backend/apps/common/guards.py`。
6. **审计日志的读取范围**：`AuditLogListView.get` 只支持 `kind/model/operatorId/competitionId` 四个过滤（原件 §2.8 第 2 点），本次只复核了权限门（超管专属，O-08）与查询参数（`audit/views.py:26-56`），未展开其自身缺陷（属其他域）。验证：`sed -n '20,60p' $SNAP/backend/apps/audit/views.py`。
7. **无法判定的一项**：O-12 的「串行」结论需要目标环境实测（原件 §5 E-5 自述「全部为只读、不起服务」），本任务同样未起服务/未连库，故只能给「bcrypt 与写库次数已验证 + 框架语义推断」的组合结论，不能给出「100 人签到耗时」的定量判断。若要定量，需在测试机以并发脚本压 `/api/auth/login` 与 `/api/auth/change-password`（超出本任务只读边界）。
8. **原件 §5 E-1 探针结论未被用作 master 证据**（其 DB/代码均为 bugfix-merged）。master 侧如需同口径矩阵，最小只读复现方式是：在 `$SNAP/backend` 用 `python -c` 加载 `apps.common.permissions.has_permission` 对 `ROLE_TEMPLATES` 三个角色分别跑一遍（纯函数、不连库）；**注意**这只反映「假设账号持有模板默认数组」时的能力，与 master 运行时真源（DB 数组）不同，不可单独作为「账号实际能力」结论——这正是 O-01 差异说明里强调的一点。
